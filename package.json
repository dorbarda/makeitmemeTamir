{
  "name": "tamir-meme-party",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server install --include=dev && npm --prefix client install --include=dev && npm --prefix client run build",
    "test": "npm --prefix server run test -- --run && npm --prefix client run test -- --run",
    "start": "npm --prefix server run start",
    "test:browser": "npm --prefix client run build && node e2e/browser-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
