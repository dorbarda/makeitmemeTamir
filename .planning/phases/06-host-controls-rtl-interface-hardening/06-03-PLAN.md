---
phase: 06-host-controls-rtl-interface-hardening
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: true
requirements: [HEB-01, HEB-02]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The full automated regression suite (server tests, server typecheck, client tests, client typecheck, client build) passes with zero regressions introduced by this phase's host-controls additions, before any human verification begins."
    - { statement: "Every screen reads correctly right-to-left when read aloud in Hebrew word order on a real phone — text alignment, icon/arrow direction, flex/grid ordering, and any left/right CSS property are all confirmed correct, per the UI-SPEC's own systematic RTL Audit Checklist (HEB-01, D-05).", verification: human }
    - { statement: "Hebrew mobile keyboard input produces correctly-ordered text in the caption box on both a real iPhone and a real Android device (HEB-02).", verification: human }
    - { statement: "Long player names in the remove-player list wrap using the existing grapheme-based name-length cap already established in Phase 1 for the roster — confirmed visually with a maximum-length real name during this real-device check, not just assumed from a short test roster.", verification: backstop }
    - { statement: "The remove-player selection list scrolls with the page (no fixed-height inner scroll container) when the roster is long enough to exceed the viewport, confirmed on a real phone with a nearly-full roster during this real-device check.", verification: backstop }
    - { statement: "The remove-player confirmation sentence with an interpolated player name wraps naturally rather than truncating, confirmed with a maximum-length name during this real-device check.", verification: backstop }
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 6 with the systematic, whole-interface RTL audit (HEB-01) and the Hebrew mobile
keyboard confirmation (HEB-02) per `06-CONTEXT.md`'s D-05 — a comprehensive real-device pass, not
a targeted bug hunt, since no specific RTL defect has been reported across Phases 1-5. This is also
where the four backstop items `06-UI-SPEC.md`'s own State Coverage section explicitly deferred to
"this phase's real-device checkpoint" get resolved, and where Plans 06-01/06-02's host-controls
additions get their first real-phone confirmation.

Purpose: this project's own established pattern (every prior phase's SUMMARY) never treats a UI
change as done on a green test suite alone — visual/interaction correctness on a real phone is
confirmed explicitly, every time, and flagged if it cannot be performed.

Output: a full regression pass, plus a completed real-device audit covering every screen's RTL
correctness, both platforms' Hebrew keyboards, and the new Host Controls panel's actual behavior
on a phone.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-host-controls-rtl-interface-hardening/06-CONTEXT.md
@.planning/phases/06-host-controls-rtl-interface-hardening/06-UI-SPEC.md

@server/src/rooms/joinUrl.ts
@server/src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression pass before the audit</name>
  <files>none — verification only, no source changes expected</files>
  <read_first>
    - `.planning/phases/06-host-controls-rtl-interface-hardening/06-01-PLAN.md` and `06-02-PLAN.md` — this task confirms neither plan's work regressed anything
  </read_first>
  <action>
Run the full regression suite, in this order, and fix any root cause before proceeding to Task 2
below — never present the human-check with a broken verification environment: `npm --prefix
server run test -- --run`, `npm --prefix server run typecheck`, `npm --prefix client run test --
--run`, `npm --prefix client run typecheck`, `npm --prefix client run build`. All five must exit 0.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
  </verify>
  <acceptance_criteria>
    - all five commands above exit 0
    - no test file added by 06-01/06-02 was skipped or left failing
  </acceptance_criteria>
  <done>The whole automated suite is green before any human verification begins.</done>
</task>

<task type="auto">
  <name>Task 2: RTL audit, Hebrew keyboard check, and host-controls real-device confirmation (HEB-01, HEB-02)</name>
  <files>none — this task starts the already-built server for a live check; no source changes</files>
  <read_first>
    - `server/src/rooms/joinUrl.ts` — `resolveOrigin`'s LAN-IP-friendly origin resolution, already proven for on-device testing in Phases 1/3/4/5
    - `server/src/config.ts` — `SERVER_PORT` (defaults to 3001)
    - `.planning/phases/06-host-controls-rtl-interface-hardening/06-UI-SPEC.md`'s "RTL Audit Checklist" and "State Coverage" sections — this task's own checklist is lifted directly from there
  </read_first>
  <action>
Start the server (`npm --prefix server run start`, backgrounded, using the client build Task 1
just produced) so a real device on the same WiFi network can reach it — `resolveOrigin` already
builds a correct join URL/QR code from whatever LAN IP the connecting device's own request
carries, with no code change needed (the exact mechanism Phases 3/4/5's own real-phone checks
already used). Confirm the server is up via its `/health` route. Report the machine's LAN IP and
port so the exact `http://<lan-ip>:<port>` address is ready for a real phone.
  </action>
  <verify>
    <automated>node -e "fetch('http://localhost:3001/health').then((r) => r.json()).then((j) => { if (!j.ok) process.exit(1); }).catch(() => process.exit(1));"</automated>
    <fails_when>non-zero exit, meaning the server is not reachable at /health on the expected port</fails_when>
    <human-check>
      On at least one real phone connected to the SAME WiFi network as this machine (an iPhone AND
      an Android device if both are available — HEB-02 requires both platforms):

      **Setup**
      1. Open a browser and navigate to the `http://<lan-ip>:<port>` address reported above.
      2. Create a room with one device, join with a second device if available, and play through
         at least one full round (writing, rating, round-end) so every screen below can be reached.

      **RTL Audit Checklist — systematic, every screen (HEB-01, D-05)**

      Text & Typography:
      - Every heading, paragraph, button label, and placeholder reads correctly in Hebrew word
        order (browser RTL auto-adjustment).
      - No screen has a hardcoded `text-align: left` fighting the RTL layout.
      - No Hebrew text has `letter-spacing` applied (only numeric/embedded-English text may).

      Layout & Directionality:
      - No `float: left`/`float: right` anywhere (this codebase is Flexbox-only).
      - The QR code, meme photo/canvas, and any other centered element stay centered regardless of
        direction (`margin-inline: auto` / `margin: 0 auto`, never `margin-left`/`margin-right`).
      - Every `flex-direction` is `column` (never a `row` that would need RTL flipping).

      Icons & Directional Elements:
      - No directional icon/arrow currently exists in the app; if one is spotted, flag it as a
        deviation rather than silently accepting incorrect direction.
      - The countdown timer is numeric-only text with no directionality concern.

      Form Inputs & Buttons:
      - Labels sit stacked above their input (never side-by-side).
      - Button text reads right-to-left naturally.
      - Placeholder text in every `<input>`/`<textarea>` reads correctly.

      Specific screens — confirm each individually:
      1. Home/Join screens: QR code and room code display read correctly at arm's length.
      2. Lobby: room code, QR code, settings panel (preset buttons wrap correctly, read in RTL
         order), share button/copied toast position, player list order.
      3. Writing phase: photo/canvas centered, caption box RTL text flow, drag handles (if visible)
         don't assume LTR, submission progress count reads correctly, swap-photo button visible
         and readable.
      4. Rating phase: meme image centered, the three rating-tier buttons wrap and read correctly,
         the author's waiting state displays correctly.
      5. Round-end screen: ranked meme list order is correct, score + "נקודות" suffix reads
         correctly, a host-skipped round (Plan 06-01) shows the correct "המנחה ביטל את הסיבוב הזה"
         line — try tapping "skip round" during an active round to confirm this specific message
         (not the "not enough captions" message) appears.
      6. Game-end screen: winner banner, scoreboard, and best-of-night list all read and lay out
         correctly.
      7. Countdown: numeric display and the last-10-seconds urgent state show without layout shift.

      Reconnect & system messages:
      - The reconnecting overlay is centered and readable.
      - Every error message (from `HEBREW_ERRORS`) reads correctly in its modal/inline context.
      - The "(מנותק/ת)" disconnected tag appears after a player's name, not before.

      Edge cases:
      - A long player name wraps correctly without pushing the layout wider than the screen.
      - A caption mixing Hebrew with digits/English renders with correct bidirectional flow both
        on the meme canvas AND in plain text (roster, results) — this re-confirms Phase 5's HEB-03
        canvas work is undisturbed by this phase's changes.

      **Host Controls real-device confirmation (Plans 06-01/06-02, plus the 4 backstop items
      `06-UI-SPEC.md`'s State Coverage section deferred to this exact checkpoint)**
      - As the host, confirm the Host Controls panel appears below the main round content with the
        heading "לא עובד? בעזרת המנחה" and exactly 4 buttons in a 2x2 grid that wraps correctly on
        the phone's screen width.
      - As a non-host player, confirm the Host Controls panel does not appear anywhere on screen.
      - Tap "דילוג על הסיבוב" (skip round): confirm a confirmation modal appears BEFORE anything
        happens; tapping "ביטול" does nothing observable; tapping "אישור" advances the round and
        the round-end screen shows the host-skipped message, not a ranked meme list.
      - Tap "הסרת שחקן" (remove player): if a large roster is available, confirm the selection
        list scrolls with the page rather than being clipped in a fixed-height box (backstop 1);
        with only the host connected, confirm the empty-state message "אין שחקנים אחרים להסרה"
        appears instead of an empty list; select a player with the longest name in the room and
        confirm the confirmation sentence wraps naturally without truncating the name (backstop
        2); if a long name is available, also confirm it wraps correctly inside the selection list
        itself (backstop 3); confirm the targeted player's connection is genuinely dropped
        (their own screen shows the reconnecting overlay) and that they can rejoin normally.
      - Tap "סיום המשחק" (end game): confirm the confirm modal appears, and confirming jumps
        straight to the normal game-end screen with the scores as they stood at that moment.
      - Tap "משחק חדש" (restart): confirm the confirm modal appears, and confirming returns every
        device to the Lobby screen with every score reset to 0 and the same room code.

      **Hebrew keyboard (HEB-02)**
      - On the real iPhone: type a caption using the native Hebrew keyboard in the caption
        textarea; confirm the typed text appears in the correct visual order as you type, with no
        character reordering or reversal.
      - On the real Android device: repeat the same check with its native Hebrew keyboard.
      - On both devices, type a caption mixing Hebrew words with a number or an English word and
        confirm it also appears in the correct order while typing (not just after submission).

      Confirm nothing froze, crashed, showed a blank screen, or showed mismatched content between
      devices at any point during this check.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The server responds {"ok":true} at /health and this task's output states the exact LAN IP and port used
    - Every item in the RTL Audit Checklist above was walked through on a real phone
    - HEB-02's Hebrew keyboard check was confirmed on both a real iPhone and a real Android device
    - The Host Controls panel's host-only visibility, all four confirm flows, and the three backstop items (long-name wrap in the list, full-roster scroll, long-name confirm-sentence wrap) were confirmed on a real device
    - Any defect found is logged as a deviation for a follow-up fix, per this phase's own explicit "the audit's job is to find out" framing — not silently pre-solved or silently ignored
  </acceptance_criteria>
  <done>The whole interface is confirmed reading correctly right-to-left on a real phone, Hebrew keyboard input works on both iPhone and Android, and the new Host Controls panel's full interaction set — including its three previously-flagged backstop items — is confirmed working on a real device, closing out Phase 6.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces no new files, protocol types, components, or CLIENT_EVENTS entries — it is a
pure verification pass over the work Plans 06-01 and 06-02 already shipped. If the audit surfaces
a genuine RTL or host-controls defect, that is logged as a deviation for a follow-up fix, not
pre-solved here (per `06-CONTEXT.md`/`06-UI-SPEC.md`'s own confirmation that no known bug exists
yet — this audit's job is to find out, not to guess in advance).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan adds no new code path — it verifies Plans 06-01/06-02's already-threat-modeled work on a real device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new package — no `package.json`/lockfile change in `files_modified` (empty). |
</threat_model>

<verification>
- `npm --prefix server run test -- --run`, `npm --prefix server run typecheck`, `npm --prefix client run test -- --run`, `npm --prefix client run typecheck`, `npm --prefix client run build` all exit 0
- The server responds `{"ok":true}` at `/health`
- The full RTL Audit Checklist, HEB-02's keyboard check, and the Host Controls real-device confirmation (including all 3 backstop items) are all completed on a real phone
</verification>

<success_criteria>
- Every screen in the app reads correctly right-to-left in Hebrew, confirmed by reading each screen aloud in Hebrew word order on a real phone
- Typing a Hebrew caption works correctly on both a real iPhone keyboard and a real Android keyboard
- The phase's new host-controls work is confirmed correct on a real device, not just in an automated suite
</success_criteria>

<output>
Create `.planning/phases/06-host-controls-rtl-interface-hardening/06-03-SUMMARY.md` when done
</output>
