---
phase: 03-core-loop-checkpoint-real-phones-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/rooms/photos.ts
  - server/src/rooms/Room.ts
  - shared/protocol.ts
  - shared/messages.ts
  - client/src/screens/round/WritingPanel.tsx
  - client/src/screens/round/RatingPanel.tsx
  - client/src/screens/round/RoundEndPanel.tsx
  - client/src/index.css
  - server/test/realContent.integration.test.ts
  - server/test/photos.test.ts
  - server/test/scoring.integration.test.ts
autonomous: true
requirements: [ROUND-01, ROUND-03, VOTE-01, VOTE-02, SCORE-01, SCORE-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every player's writing-phase snapshot carries their own real, distinct photo of Tamir (`yourPhotoUrl`, drawn from client/public/tamir-photos) — never a numbered placeholder — before they type and submit a Hebrew caption within the writing countdown (ROUND-01, ROUND-03, D-01)."
    - "After writing closes, each meme's rating step shows the SAME photo and caption on every player's screen at once, and the three rating buttons read exactly 'דנה מגנזי' (value 3), 'תמיר פטריות' (value 2) and 'תמיר בפאניקה' (value 1) — never digits (VOTE-01, VOTE-02, D-02)."
    - "A player's round score is the exact server-computed sum of the ratings their own meme received, written once inside Room.applyRoundScores before the round-end view is built — no client message can set or influence a score (SCORE-01)."
    - "Between rounds every player sees a ranked scoreboard (name, then score, highest first, no per-round delta) built from LobbySnapshot.players, whose scores accumulate across the whole game session rather than resetting each round (SCORE-03, D-03)."
    - "A room with more players than photos in the pool still assigns every player a real photo, cycling through additional shuffled draws rather than crashing or leaving a gap (D-01 discretion)."
    - "A round that receives fewer than two submitted captions (D-09) skips rating entirely and contributes exactly zero to every player's score, without throwing or stalling the game."
    - "The complete loop — join, write with a real photo, rate meme-by-meme with the real tier names, see a real accumulated score on the scoreboard — has been played start-to-finish on two distinct real phones on the same network with no stalls, crashes, or desyncs (ROADMAP Phase 3 success criterion 4)."
  artifacts:
    - path: "server/src/rooms/photos.ts"
      provides: "PHOTO_FILENAMES, assignPhotos and photoUrl — the pure module that enumerates client/public/tamir-photos once and decides who gets which photo each round"
      exports: ["PHOTO_FILENAMES", "assignPhotos", "photoUrl"]
    - path: "server/src/rooms/Room.ts"
      provides: "photoAssignments/photoUrlFor (D-01) and applyRoundScores (SCORE-01), wired into enterWriting and enterRoundEnd"
      contains: "applyRoundScores"
    - path: "shared/protocol.ts"
      provides: "RatingStepView.photoUrl and LobbySnapshot.yourPhotoUrl, replacing the Phase 2 numbered placeholders"
      contains: "yourPhotoUrl"
    - path: "client/src/screens/round/RatingPanel.tsx"
      provides: "The real photo and the three locked Hebrew tier-name buttons (D-02), replacing digit placeholders"
    - path: "client/src/screens/round/RoundEndPanel.tsx"
      provides: "The between-rounds scoreboard: a ranked name-and-score list with no per-round delta (D-03)"
    - path: "server/test/realContent.integration.test.ts"
      provides: "The tracer's own real-socket proof: distinct real photos assigned, the correct photo riding the rating step, and a rated meme's score landing on the right player"
  key_links:
    - from: "server/src/rooms/Room.ts"
      to: "server/src/rooms/photos.ts"
      via: "enterWriting calls assignPhotos([...this.players.keys()]) and stores the result in photoAssignments every round"
      pattern: "assignPhotos\\("
    - from: "server/src/rooms/Room.ts"
      to: "shared/protocol.ts"
      via: "snapshotFor populates yourPhotoUrl and ratingStep.photoUrl from the private photoUrlFor helper, never a raw filename"
      pattern: "photoUrlFor\\("
    - from: "server/src/rooms/Room.ts"
      to: "server/src/players/Player.ts"
      via: "enterRoundEnd calls applyRoundScores before the phase changes, which is the only place any Player.score is ever mutated"
      pattern: "applyRoundScores\\("
    - from: "client/src/screens/round/RatingPanel.tsx"
      to: "shared/messages.ts"
      via: "TIER_LABELS reads HEBREW_UI.ratingTierMeh/ratingTierFine/ratingTierFunniest instead of the removed digit placeholders"
      pattern: "ratingTierFunniest"
    - from: "client/src/screens/round/RoundEndPanel.tsx"
      to: "shared/protocol.ts"
      via: "the scoreboard section sorts snapshot.players by score, never computing or receiving a score from anywhere but the snapshot"
      pattern: "scoreboard-entry"
  prohibitions:
    - statement: "MUST NOT accept, trust, or derive a player's score from anything a client sends — Player.score is written exclusively inside Room.applyRoundScores, and no socket handler in server/src/socket/handlers.ts ever assigns to .score"
      requirement_id: SCORE-01
      category: integrity
      status: resolved
      verification: review
    - statement: "MUST NOT show a per-round score delta (e.g. a '+' increment) anywhere on the between-rounds scoreboard — D-03 locked a plain ranked name-and-score list only, with a richer version explicitly deferred"
      requirement_id: SCORE-03
      category: scope
      status: resolved
      verification: review
    - statement: "MUST NOT hardcode any photo filename anywhere in server/src/rooms/photos.ts or Room.ts — every filename must come from enumerating client/public/tamir-photos at runtime, since the real files are opaque UUIDs that can change"
      requirement_id: ROUND-01
      category: correctness
      status: resolved
      verification: review
---

<objective>
Play the first real round of Tamir Meme Party: every player writes a caption for their OWN real
photo of Tamir (ROUND-01, ROUND-03, D-01), rates the round's memes one at a time using the three
locked Hebrew tier-name buttons instead of digits (VOTE-01, VOTE-02, D-02), and sees a real,
server-computed score land on a between-rounds scoreboard (SCORE-01, SCORE-03, D-03). Phases 1 and
2 built the whole machine with placeholder content; this phase swaps every remaining placeholder
for the real thing and proves the result on two real phones.

Purpose: this is the first time real players touch real content end to end. Everything after this
phase (no-repeat photos, photo swap, ranked round results, the final winner screen) builds on top
of this checkpoint working, so it has to be proven — not just typechecked — before anything else is
layered on.

Output: a complete, real-content round (write → reveal → rate → score → scoreboard) that a real
executor has played start-to-finish on two real phones with no stalls, crashes or desyncs.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-loop-checkpoint-real-phones-end-to-end/03-CONTEXT.md
@.planning/phases/02-server-authoritative-round-engine/02-PATTERNS.md

@shared/protocol.ts
@shared/messages.ts
@server/src/config.ts
@server/src/rooms/Room.ts
@server/src/rooms/rotation.ts
@server/src/rooms/gameSettings.ts
@server/src/app.ts
@server/src/rooms/joinUrl.ts
@client/src/screens/round/WritingPanel.tsx
@client/src/screens/round/RatingPanel.tsx
@client/src/screens/round/RoundEndPanel.tsx
@client/src/screens/Lobby.tsx
@client/src/index.css
@server/test/setup.ts
@server/test/ratingStep.integration.test.ts
@server/test/neverStalls.integration.test.ts

<interfaces>
Existing contracts this plan extends — do not re-derive them.

From `shared/protocol.ts` (today, before this plan):
```
export type PlayerView = { id: string; name: string; connected: boolean; isHost: boolean; score: number };
export type RatingStepView = {
  index: number; total: number; caption: string; placeholderId: number;
  youAreAuthor: boolean; youMayRate: boolean; youHaveRated: boolean;
  ratedCount: number; eligibleCount: number;
};
export type RoundEndEntry = { authorId: string; authorName: string; caption: string; ratings: RatingValue[]; eligibleAtClose: number };
export type LobbySnapshot = { ...; players: PlayerView[]; youSubmitted: boolean; yourPlaceholderId: number | null; ratingStep: RatingStepView | null; roundEnd: RoundEndView | null; ... };
```
This plan renames `RatingStepView.placeholderId` to `photoUrl: string` and
`LobbySnapshot.yourPlaceholderId` to `yourPhotoUrl: string | null` — the only two protocol edits.
`PlayerView.score` already exists and is already sent on every snapshot regardless of phase; this
plan is the first to ever write a non-zero value into it.

From `server/src/rooms/Room.ts` (today):
`players: Map<string, Player>`, `submissions: Map<string, string>`, `rotation: string[]`,
`ratings: Map<number, Map<string, RatingValue>>`, `eligibleAtClose: Map<number, number>`,
`private enterWriting()` (clears the round's maps, then schedules the writing deadline),
`private enterRoundEnd()` (schedules the between-rounds beat, then either the next round's
`enterWriting()` or `enterGameEnd()`), `snapshotFor(playerId)` (the WRITING branch populates
`progress`/`youSubmitted`/`yourPlaceholderId`; the RATING branch populates `ratingStep`).

From `server/src/rooms/rotation.ts` and `server/src/rooms/gameSettings.ts`: the established
"pure module living beside Room" shape — a handful of exported functions, no class, no side
effects beyond what's explicitly documented — `photos.ts` copies this shape exactly.

From `server/src/app.ts`: `CLIENT_DIST = path.resolve(__dirname, "../../client/dist")`, computed
from `server/src/app.ts`'s own `__dirname` and served via `express.static(CLIENT_DIST)`. Vite
copies `client/public/` verbatim into `client/dist/` at build time, so anything placed under
`client/public/tamir-photos/` is already served at `/tamir-photos/<filename>` with zero server
wiring beyond knowing the filenames exist.

From `server/src/rooms/joinUrl.ts`: `resolveOrigin` already derives a room's join URL from the
connecting socket's own LAN IP when no `PUBLIC_BASE_URL` is set — this is the exact mechanism
Task 3's real-phone check relies on; Phase 1 has already used it for on-device testing.

From `shared/messages.ts` (today): `HEBREW_UI.placeholderContentPrefix`, `ratingTierPlaceholder1`,
`ratingTierPlaceholder2`, `ratingTierPlaceholder3` are the three strings this plan removes — the
comment above them already says "the funny Hebrew tier names are deferred to Phase 3/4."

From `client/src/screens/Lobby.tsx` / `client/src/index.css`: the `.qr-code` rule (`display:
block; width: 60%; max-width: 14rem; height: auto; margin: 0 auto 0.5rem;`) is the established
pattern for an image that must never overflow a phone viewport — `.meme-photo` mirrors it.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end real content — real photo, real tier names, real score reaches the scoreboard</name>
  <reversibility rating="reversible">D-01/D-02/D-03 are all display/data-shape choices (which photo a player sees, what a button says, how a ranked list renders) — none require a different architecture to change later; CONTEXT.md's own D-01 note already calls the photo delivery mechanism reversible.</reversibility>
  <precondition>client/public/tamir-photos contains at least one .jpg file (5 real photos present today, git-tracked — confirmed during planning)</precondition>
  <files>server/src/rooms/photos.ts, server/src/rooms/Room.ts, shared/protocol.ts, shared/messages.ts, client/src/screens/round/WritingPanel.tsx, client/src/screens/round/RatingPanel.tsx, client/src/screens/round/RoundEndPanel.tsx, client/src/index.css, server/test/realContent.integration.test.ts</files>
  <read_first>
    - `server/src/rooms/Room.ts` — `enterWriting`, `enterRoundEnd`, `snapshotFor`'s WRITING and RATING branches, `submissions`, `rotation`, `ratings`, `eligibleAtClose`, and the `schedulePhase`/`collapseDeadline` timer discipline this plan does not touch
    - `server/src/rooms/rotation.ts` and `server/src/rooms/gameSettings.ts` — the pure-module-beside-Room shape `photos.ts` copies
    - `server/src/app.ts` — the `CLIENT_DIST` path-resolution pattern `photos.ts`'s `PHOTOS_DIR` mirrors
    - `shared/protocol.ts` — `RatingStepView.placeholderId`, `LobbySnapshot.yourPlaceholderId`, `PlayerView.score`
    - `shared/messages.ts` — `placeholderContentPrefix`, `ratingTierPlaceholder1/2/3`, and the `// plan 02-0X — ...` grouping convention
    - `client/src/screens/round/WritingPanel.tsx` and `client/src/screens/round/RatingPanel.tsx` — the exact placeholder lines this task replaces
    - `client/src/screens/round/RoundEndPanel.tsx` — the existing `roundEnd.entries` rendering this task adds a scoreboard section beside
    - `client/src/screens/Lobby.tsx` and `client/src/index.css`'s `.qr-code` rule — the relative-width-image pattern `.meme-photo` follows
    - `server/test/ratingStep.integration.test.ts` and `server/test/settingsIntent.integration.test.ts` — the real-socket, test-only-internal-seeding harness this plan's new test file follows
    - `.planning/phases/03-core-loop-checkpoint-real-phones-end-to-end/03-CONTEXT.md` — D-01, D-02, D-03
  </read_first>
  <action>
Create `server/src/rooms/photos.ts` as a pure module beside `Room.ts`, in the same style as
`gameSettings.ts`/`rotation.ts`:
  - Resolve `PHOTOS_DIR` via `path.resolve(__dirname, "../../../client/public/tamir-photos")` —
    one directory deeper than `app.ts`'s `CLIENT_DIST` resolution, since `photos.ts` lives in
    `server/src/rooms/` rather than `server/src/`. This same relative path is correct in dev and
    in a production build with no server wiring change, because Vite copies `client/public/`
    verbatim into `client/dist/`, which `app.ts` already serves statically (D-01).
  - `export const PHOTO_FILENAMES: string[]` — `readdirSync(PHOTOS_DIR)` filtered to filenames
    matching `/\.(jpe?g|png)$/i`. Never hardcode a filename anywhere in this file — the real files
    are opaque UUIDs the host drops in. Throw a descriptive `Error` at module load if the result
    is empty, so an empty photo folder fails loud at server start rather than serving a broken
    writing screen mid-game.
  - `export function photoUrl(filename: string): string` — returns `` `/tamir-photos/${filename}` ``,
    the exact static path `app.ts` already serves.
  - `export function assignPhotos(playerIds: string[], pool: string[] = PHOTO_FILENAMES): Map<string, string>`
    — D-01's photo draw. Build a local `shuffle` helper (Fisher-Yates over `Math.random()`, no
    crypto RNG needed for a party game). Draw by concatenating successive shuffled copies of
    `pool` until the accumulated list is at least as long as `playerIds`, then zip
    `playerIds[i]` to the i-th drawn filename. This guarantees every player gets a DISTINCT photo
    whenever `playerIds.length <= pool.length` (covers every real party size against the real
    photo count), and still assigns a real pool member to every player — never throwing, never
    leaving a gap — when there are more players than photos. `pool` is an injectable parameter
    purely so `server/test/photos.test.ts` (Task 2) can test the cycling behavior deterministically
    without depending on however many real files happen to exist on disk.

Edit `server/src/rooms/Room.ts`:
  - Import `assignPhotos` and `photoUrl` from `./photos.js`.
  - Add `photoAssignments = new Map<string, string>()` (playerId -> filename), documented as this
    round's photo draw, rebuilt every `enterWriting()`.
  - In `enterWriting()`, immediately after the existing `this.eligibleAtClose.clear()` line and
    before `this.schedulePhase(...)`, add `this.photoAssignments = assignPhotos([...this.players.keys()]);`
    — every player currently in the room gets a fresh, distinct-where-possible draw each round.
  - Add `private photoUrlFor(playerId: string): string` — reads `this.photoAssignments.get(playerId)`;
    if absent (a player who joined after `enterWriting()` already ran, which nothing today
    prevents), lazily draws one via `assignPhotos([playerId]).get(playerId)!`, caches it into
    `photoAssignments`, and returns `photoUrl(...)` either way. This guarantees `snapshotFor` never
    has to render an empty `<img src>` for anyone currently in `WRITING` or `RATING`.
  - In `snapshotFor`, replace the line `yourPlaceholderId: inWriting && you ? you.joinedAt + 1 : null,`
    with `yourPhotoUrl: inWriting && you ? this.photoUrlFor(playerId) : null,`.
  - In the `ratingStep` object literal, replace `placeholderId: author ? author.joinedAt + 1 : 0,`
    with `photoUrl: this.photoUrlFor(authorId),` (drop the `author ?` guard — `photoUrlFor` handles
    any id defensively and `authorId` always names a real rotation entry).
  - Add `private applyRoundScores(): void` (SCORE-01) — iterates `this.rotation` by index, reads
    `this.ratings.get(index)`, and if present sums its values (`[...stepRatings.values()].reduce((t, v) => t + v, 0)`)
    onto `this.players.get(authorId).score`. A round skipped for too few captions (D-09) has an
    empty `rotation`, so this is a no-op for that round — no score is invented. Call
    `this.applyRoundScores()` as the very first line of `private enterRoundEnd()`, before
    `this.phase = "ROUND_END"` — this is the ONLY place `Player.score` is ever mutated; no socket
    handler in `server/src/socket/handlers.ts` touches `.score`, so no client message can influence
    it (SCORE-01's server-authority requirement).

Edit `shared/protocol.ts`:
  - `RatingStepView`: rename `placeholderId: number;` to `photoUrl: string;`. Leave the
    surrounding comment about the view carrying no author identity unchanged — it still applies.
  - `LobbySnapshot`: rename `yourPlaceholderId: number | null; // filled by plan 02-03` to
    `yourPhotoUrl: string | null; // filled by plan 03-01 — this player's own assigned photo (D-01)`.

Edit `shared/messages.ts`:
  - Remove the comment line about digits standing in for the tier names, and remove
    `placeholderContentPrefix`, `ratingTierPlaceholder1`, `ratingTierPlaceholder2`,
    `ratingTierPlaceholder3` — all four are fully superseded by this task.
  - Add a new `// plan 03-01 — real content: photos, tier names, scoreboard` group after the
    existing `roundEndTooFewCaptions` line: `ratingTierFunniest: "דנה מגנזי"` (value 3, use exactly
    this text, do not paraphrase — D-02), `ratingTierFine: "תמיר פטריות"` (value 2, exact text,
    D-02), `ratingTierMeh: "תמיר בפאניקה"` (value 1, exact text, D-02), `photoAlt: "תמונה של תמיר"`
    (alt text for the photo shown while writing and while rating), and
    `scoreboardHeading: "טבלת הניקוד"` (the between-rounds scoreboard's heading).

Edit `client/src/screens/round/WritingPanel.tsx`:
  - Replace the line `<p>{HEBREW_UI.placeholderContentPrefix} {snapshot.yourPlaceholderId}</p>`
    with `{snapshot.yourPhotoUrl && <img className="meme-photo" src={snapshot.yourPhotoUrl} alt={HEBREW_UI.photoAlt} />}`.

Edit `client/src/screens/round/RatingPanel.tsx`:
  - Update `TIER_LABELS` to `{ 1: HEBREW_UI.ratingTierMeh, 2: HEBREW_UI.ratingTierFine, 3: HEBREW_UI.ratingTierFunniest }`.
  - Replace the line `<p>{HEBREW_UI.placeholderContentPrefix} {ratingStep.placeholderId}</p>` with
    `<img className="meme-photo" src={ratingStep.photoUrl} alt={HEBREW_UI.photoAlt} />` (no
    conditional needed — `ratingStep.photoUrl` is a non-optional string).

Edit `client/src/screens/round/RoundEndPanel.tsx` (D-03): inside the existing
`<section className="round-end-panel">`, after the entries/too-few-captions block, add a second
block: `const scoreboard = [...snapshot.players].sort((a, b) => b.score - a.score);` computed
before the return, then `<section className="scoreboard"><h2>{HEBREW_UI.scoreboardHeading}</h2><ol
className="scoreboard-list">` mapping `scoreboard` to `<li key={player.id}
className="scoreboard-entry"><span>{player.name}</span><span>{player.score}</span></li>` — a
simple ranked list, name then score, highest first, no per-round delta or "+N" anywhere (D-03).
This sorts what the server already computed; it never sums or derives a score client-side.

Edit `client/src/index.css`: add `.meme-photo` mirroring `.qr-code`'s relative-width pattern
(`display: block; width: 100%; max-width: 24rem; height: auto; margin: 0 auto 0.75rem;
border-radius: 0.5rem;` — never a fixed pixel width) and `.scoreboard`/`.scoreboard-list`/
`.scoreboard-entry` following `.round-end-panel`'s mobile-first, no-fixed-width, RTL shape (a
flex-column list of flex rows with `justify-content: space-between`, each row bordered and padded
like the existing global `li` rule).

Create `server/test/realContent.integration.test.ts` — real sockets, real timers (matches
`ratingStep.integration.test.ts`'s real-socket describe and `settingsIntent.integration.test.ts`'s
test-only internal seeding): host + b + c join a room; seed `room.settings.writingSeconds = 1` and
`room.settings.ratingSeconds = 1` before `start-game` (test-only internal seeding — the protocol
path that rejects non-preset values is proven in plan 02-02); start the game; assert host's and
b's `state.yourPhotoUrl` are both non-null, both start with `/tamir-photos/`, are DIFFERENT from
each other, and each filename (the part after the prefix) is a member of the real
`PHOTO_FILENAMES` imported directly from `../src/rooms/photos.js`; host and b submit captions, c
never submits (mirrors the established two-of-three pattern that already clears
`MIN_SUBMISSIONS_TO_RATE`); reach `RATING` step 0, identify its author via `youAreAuthor`, and
assert `ratingStep.photoUrl` equals `photoUrl(room.photoAssignments.get(authorId))` read directly
off `server.roomManager.findRoom(roomCode)`; have c (never an author) rate step 0 with value 3 and
step 1 with value 1; reach `ROUND_END` and assert, from the resulting `players` array, that step
0's author has `score === 3` and step 1's author has `score === 1` — the exact single rating each
meme received, proving `SCORE-01`'s sum is real math, not a placeholder; assert a SECOND client's
own next snapshot (e.g. b's) reports the identical score for both authors, proving the number is
the server's own persisted value and not something computed per-viewer.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/realContent.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
    <automated>grep -cE "דנה מגנזי|תמיר פטריות|תמיר בפאניקה" shared/messages.ts</automated>
    <fails_when>the command's output is not exactly 3, meaning one or more of the three locked tier-name strings is missing or duplicated in shared/messages.ts</fails_when>
    <automated>grep -cE "ratingTierFunniest|ratingTierFine|ratingTierMeh" client/src/screens/round/RatingPanel.tsx</automated>
    <fails_when>the command's output is not exactly 3, meaning RatingPanel.tsx is not reading all three real tier-name keys</fails_when>
  </verify>
  <acceptance_criteria>
    - `server/src/rooms/photos.ts` exports `PHOTO_FILENAMES`, `assignPhotos` and `photoUrl`, and contains no hardcoded photo filename
    - `server/src/rooms/Room.ts` contains `photoAssignments`, `photoUrlFor` and `applyRoundScores`
    - `enterRoundEnd` calls `applyRoundScores()` before setting `phase = "ROUND_END"`
    - `shared/protocol.ts` declares `photoUrl: string` on `RatingStepView` and `yourPhotoUrl: string | null` on `LobbySnapshot`
    - `shared/messages.ts` no longer declares `placeholderContentPrefix` or any `ratingTierPlaceholder*` key
    - `client/src/screens/round/RoundEndPanel.tsx` renders a `.scoreboard` section sourced from `snapshot.players`, sorted by score descending, with no per-round delta
    - `npm --prefix server run test -- --run test/realContent.integration.test.ts` exits 0
    - `npm --prefix client run build` exits 0
  </acceptance_criteria>
  <done>A real photo reaches every player's writing screen and the matching rating step, the three rating buttons carry the user's own locked Hebrew tier names, and a rated meme's score is a real server-computed sum that appears on the between-round scoreboard — proven end to end over real sockets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hardening — photo-pool cycling, a late joiner, and score accumulation across rounds</name>
  <files>server/src/rooms/photos.ts, server/src/rooms/Room.ts, server/test/photos.test.ts, server/test/scoring.integration.test.ts</files>
  <read_first>
    - `server/src/rooms/photos.ts` and `Room.ts`'s `photoUrlFor`/`applyRoundScores` as written in Task 1
    - `server/test/rotation.test.ts` and `server/test/gameSettings.test.ts` — the pure-module unit-test style `photos.test.ts` copies
    - `server/test/neverStalls.integration.test.ts` and `server/test/writingPhase.integration.test.ts` — the bare-`Room` fake-timer harness, and the documented pitfall (2026-09-06 SUMMARY notes) that submitting from EVERY connected player inside a fake-timer test triggers the writing/rating early-finish collapse and cascades a single large `vi.advanceTimersByTime` through multiple rounds at once — submit fewer than the full roster whenever a test needs the full window to elapse
    - `server/src/config.ts` — `MIN_SUBMISSIONS_TO_RATE`
  </read_first>
  <behavior>
    - `assignPhotos(["p1","p2","p3"], pool)` with a 3-item pool returns a 3-entry map whose values, as a set, equal the pool exactly (every distinct pool member used once)
    - `assignPhotos(["p1","p2"], pool)` with a larger pool returns 2 distinct values, both members of the pool
    - `assignPhotos([7 player ids], pool)` with a 3-item pool returns 7 filenames, every one a member of the pool, and every pool member appears at least once among the 7 (more players than photos still covers the whole pool, never throws)
    - `photoUrl("x.jpg")` returns exactly `/tamir-photos/x.jpg`
    - The real, non-injected `PHOTO_FILENAMES` array is non-empty and every entry matches `/\.(jpe?g|png)$/i` — the real `client/public/tamir-photos` directory is enumerated correctly at the moment the suite runs
    - A player who joins a room after `enterWriting()` has already run still receives a non-null `yourPhotoUrl` starting with `/tamir-photos/` on their next snapshot
    - A player's `score` after two rounds equals the exact sum of both rounds' rating contributions to their own meme — never reset, never overwritten between rounds
    - A round with fewer than `MIN_SUBMISSIONS_TO_RATE` submissions (D-09) reaches `ROUND_END` with every player's `score` exactly unchanged — no invented score for a round that never rated anything
  </behavior>
  <action>
Write `server/test/photos.test.ts` as a pure unit test over `assignPhotos`/`photoUrl`, using a
small local injected pool (e.g. three short literal filenames) so the test is deterministic and
independent of however many real files exist on disk at any given moment, plus one light
real-filesystem smoke check against the real, non-injected `PHOTO_FILENAMES` export. Cover every
behavior line above for `assignPhotos`/`photoUrl`.

Write `server/test/scoring.integration.test.ts` as a bare-`Room` test with `vi.useFakeTimers()`
(construct in `beforeEach`, `room.dispose()` + `vi.useRealTimers()` in `afterEach`), following
`neverStalls.integration.test.ts`'s harness style:
  - Late-joiner test: start a room with three players, advance into `WRITING`, `addPlayer` a
    fourth mid-phase, and assert `room.snapshotFor(fourthId).yourPhotoUrl` is non-null.
  - Two-round accumulation test (SCORE-03): seed `room.settings.rounds = 2`, drive round 1 with
    exactly two submitters (never the full roster — see the read_first pitfall note), have one
    known non-author rate the resulting single meaningful step with a known value, advance through
    the round-end beat into round 2, repeat with a different known rating value, and assert the
    round-1 author's `player.score` reflects round 1's contribution both immediately after round 1
    AND still after round 2 has added its own contribution to whichever player it rated — the
    total is the sum of both rounds' individual contributions, never reset and never overwritten.
  - D-09 skip-round test: drive a round with zero or one submission straight to `ROUND_END` and
    assert every player's `score` is still exactly 0.

Modify `server/src/rooms/photos.ts` or `Room.ts` ONLY if one of these tests exposes a defect in
Task 1's implementation. Do not restructure otherwise-correct code to make a test read more
nicely.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/photos.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run test -- --run test/scoring.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `server/test/photos.test.ts` proves `assignPhotos` returns distinct pool members when player count is at or below pool size, and never returns a filename absent from the pool when player count exceeds it
    - `server/test/scoring.integration.test.ts` proves a player's score after two rounds equals the exact sum of both rounds' contributions
    - `server/test/scoring.integration.test.ts` proves a round skipped for too few captions leaves every player's score unchanged
    - `server/test/scoring.integration.test.ts` proves a player who joins mid-WRITING still receives a non-null `yourPhotoUrl`
    - `npm --prefix server run test -- --run` exits 0 with no regressions in the existing suite
  </acceptance_criteria>
  <done>Photo assignment and scoring both behave correctly outside the common case — more players than photos, a mid-round late joiner, two rounds in a row, and a round that skipped rating entirely — backed by tests, not assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Start the server for a real two-phone check and confirm the full loop end-to-end</name>
  <files>none — this task starts the already-built server for a live check; no source changes</files>
  <read_first>
    - `server/src/rooms/joinUrl.ts` — `resolveOrigin`'s LAN-IP-friendly origin resolution, already proven for on-device testing in Phase 1
    - `server/src/config.ts` — `SERVER_PORT` (defaults to 3001)
    - `.planning/phases/03-core-loop-checkpoint-real-phones-end-to-end/03-CONTEXT.md` and `.planning/ROADMAP.md`'s Phase 3 success criterion 4 — this task's own definition of done
  </read_first>
  <action>
Build the production client (`npm --prefix client run build`) and start the server
(`npm --prefix server run start`, backgrounded) so a real device on the same WiFi network can
reach it — the server already serves the built client and the Socket.IO endpoint from the same
port, and `resolveOrigin` already builds a correct join URL/QR code from whatever LAN IP the
connecting device's own request carries, with no code change needed. Confirm the server is up by
checking its `/health` route. Determine and report the machine's LAN IP (e.g. via `hostname -I` or
the platform equivalent) and the port so the exact `http://<lan-ip>:<port>` address is ready to
hand to two real phones.
  </action>
  <verify>
    <automated>node -e "fetch('http://localhost:3001/health').then((r) => r.json()).then((j) => { if (!j.ok) process.exit(1); }).catch(() => process.exit(1));"</automated>
    <fails_when>non-zero exit, meaning the server is not reachable at /health on the expected port</fails_when>
    <human-check>
      On two real phones connected to the SAME WiFi network as this machine:
      1. Open a browser and navigate to the http://&lt;lan-ip&gt;:&lt;port&gt; address reported by this task.
      2. Phone A creates a room; phone B joins with the printed code, or by scanning the QR code shown on phone A.
      3. Phone A (host) starts the game with the default settings.
      4. On BOTH phones: confirm a real photo of Tamir appears (not a placeholder number or generic image) and type and submit a distinct Hebrew caption before the writing countdown reaches zero.
      5. When memes reveal one at a time: confirm the exact SAME photo and caption appear on both phones at once, the meme's own author sees a waiting message with no rating buttons, and the OTHER phone sees three buttons labeled exactly "דנה מגנזי", "תמיר פטריות" and "תמיר בפאניקה" — never digits.
      6. Tap a rating on each meme that isn't your own.
      7. After the round ends, confirm a scoreboard appears listing both names with a plain numeric score each (no "+N" delta), and that each author's score visibly matches what was actually tapped for their meme.
      8. Confirm nothing froze, crashed, showed an error, or showed mismatched content between the two phones at any step.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The server responds `{"ok":true}` at `/health`
    - This task's own output states the exact LAN IP and port for the real-phone check
    - Two distinct real devices completed one full round (write with a real photo → rate with the real tier names → see a real accumulated score) with no stall, crash or desync, confirmed by the human-check above
  </acceptance_criteria>
  <done>The complete real-content loop — real photo, real Hebrew captions, real tier-name ratings, real accumulated score — has been played start-to-finish on two real phones with no stalls, crashes, or desyncs, satisfying ROADMAP Phase 3's own success criterion 4.</done>
</task>

</tasks>

<planner_discretion_notes>
Judgment calls made inside CONTEXT.md's own "Claude's Discretion" area, not decided by any locked
decision:

- **Photo-assignment algorithm**: a shuffled-pool draw, with additional shuffled passes appended
  only when the player count exceeds the pool size. D-01 explicitly leaves the exact mechanic to
  discretion, and this satisfies "each player gets a distinct photo for their own caption each
  round" for every realistic party size against the real photo count.
- **Late-joiner fallback**: a player who joins after `enterWriting()` has already run gets a photo
  lazily assigned on first read (`photoUrlFor`) rather than being left with an empty `<img src>`.
  Nothing in CONTEXT.md addresses a mid-round joiner directly (mid-game joining itself is out of
  scope — SOCL-01, v2), but the fallback costs nothing and avoids a broken image element.
- **Two new Hebrew strings not dictated by any locked decision**: `photoAlt` ("תמונה של תמיר") for
  the photo's alt text, and `scoreboardHeading` ("טבלת הניקוד") for the scoreboard's heading. If
  either wording doesn't fit, they are one-line changes in `shared/messages.ts`.
- **Scoreboard placement**: rendered as a second `<section>` inside the existing
  `RoundEndPanel.tsx`, below the round's entries list, per D-03's explicit steer to reuse existing
  infrastructure rather than build a parallel screen.
</planner_discretion_notes>

<artifacts_this_phase_produces>
Symbols created or renamed by this plan (new to the codebase, or replacing a Phase 2 placeholder):

**New module:** `server/src/rooms/photos.ts` exporting `PHOTO_FILENAMES`, `assignPhotos`, `photoUrl`.

**Room members:** `Room.photoAssignments`, `Room.photoUrlFor`, `Room.applyRoundScores`.

**Protocol renames (`shared/protocol.ts`):** `RatingStepView.placeholderId` -> `RatingStepView.photoUrl`;
`LobbySnapshot.yourPlaceholderId` -> `LobbySnapshot.yourPhotoUrl`.

**Hebrew string keys added (`shared/messages.ts` `HEBREW_UI`):** `ratingTierFunniest`,
`ratingTierFine`, `ratingTierMeh`, `photoAlt`, `scoreboardHeading`. Removed:
`placeholderContentPrefix`, `ratingTierPlaceholder1`, `ratingTierPlaceholder2`,
`ratingTierPlaceholder3`.

**Client:** `.meme-photo`, `.scoreboard`, `.scoreboard-list`, `.scoreboard-entry` CSS classes;
`RoundEndPanel.tsx`'s scoreboard section.

**New file paths:** `server/src/rooms/photos.ts`, `server/test/realContent.integration.test.ts`,
`server/test/photos.test.ts`, `server/test/scoring.integration.test.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client socket -> `submit-caption`/`submit-rating` handlers | Unchanged mechanism from Phase 2; still the only way a client influences round state, and neither payload carries a score or a photo field |
| `Room` -> `Player.score` | The sole write path is `applyRoundScores`, reached only from the server's own `enterRoundEnd` timer — no socket handler touches `.score` |
| server filesystem -> `photos.ts` | `client/public/tamir-photos` is populated only by the developer via git, never by a runtime upload path — no attacker-controlled write access exists |
| `Room` -> per-player snapshot | `yourPhotoUrl` exposes only the recipient's own photo; `ratingStep.photoUrl` exposes only the current step's single meme — no other player's photo or a future step's photo ever rides along |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `Player.score` authority | high | mitigate | `applyRoundScores` is the only site that ever mutates `Player.score`; no `CLIENT_EVENTS` member carries a score value and no handler forwards one, so a forged score has no code path to travel through |
| T-03-02 | Denial of Service | `photos.ts` throwing at import if the photo directory is empty | medium | mitigate | The module throws a descriptive `Error` at import time when `PHOTO_FILENAMES` enumerates to zero entries, failing loud before any room can be created rather than serving a broken writing screen mid-game |
| T-03-03 | Tampering | a room with more connected players than photos in the pool | low | mitigate | `assignPhotos` draws additional shuffled passes rather than throwing or leaving an entry undefined; `photoUrlFor`'s lazy-assignment fallback further guarantees a real pool member for a player who joins after the round's initial draw |
| T-03-04 | Information Disclosure | learning another player's assigned photo URL early | low | accept | Photo filenames are opaque UUIDs with no player-identifying data, all photos are of the same interchangeable subject, and `yourPhotoUrl` only ever exposes the recipient's own assignment — no cross-player photo field exists on the wire |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new package — `files_modified` contains no `package.json` and no lockfile |
</threat_model>

<verification>
- `npm --prefix server run test -- --run` exits 0
- `npm --prefix server run typecheck` exits 0
- `npm --prefix client run build` exits 0
- The two-real-phone walkthrough in Task 3's human-check confirms a real photo on both writing
  screens, the same meme with the real tier-name buttons during rating, and a real accumulated
  score on the between-rounds scoreboard, with no stall, crash or desync
</verification>

<success_criteria>
- Every player writes against their own real photo of Tamir, never a placeholder
- The three rating buttons read exactly "דנה מגנזי" / "תמיר פטריות" / "תמיר בפאניקה" — never digits
- A rated meme's score is a real server-computed sum, never client-supplied or invented
- A ranked scoreboard (name, score, highest first, no delta) appears between rounds
- The full loop has been played start-to-finish on two real phones with no stalls, crashes, or desyncs
</success_criteria>

<output>
Create `.planning/phases/03-core-loop-checkpoint-real-phones-end-to-end/03-01-SUMMARY.md` when done
</output>
