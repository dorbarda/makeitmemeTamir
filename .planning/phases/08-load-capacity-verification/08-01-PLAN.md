---
phase: 08-load-capacity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/test/load.integration.test.ts
autonomous: true
requirements: [DEPLOY-04, LIVE-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A single automated test run spins up a real, in-process Socket.IO server (D-01 — no dependency on the unreachable deployed Render URL, per D-02's confirmed connectivity failure) and drives it through a full 3-round game — the room's true, unmodified DEFAULT_ROUND_COUNT, never an artificially shortened round count — with exactly 12 concurrently-connected scripted socket.io-client sockets (D-03's literal 12-player floor, LIVE-01), completing writing, one-at-a-time meme rating, and round-end for every round with no client ever receiving an error, timing out, or hanging (DEPLOY-04's 'without crashing, hanging, or requiring a restart')."
    - "Every one of the 12 players' caption submissions and every one of their rating submissions across all 3 rounds (36 captions, 396 ratings) is confirmed received by the server with zero silently lost — verified from three independent angles: each round's post-writing progress count, each round's ROUND_END entries array length, and each entry's per-meme ratings array length and eligibleAtClose count (LIVE-01's 'no submission lost')."
    - "Every individual caption/rating submission's round-trip latency (from the client's emit call to that same client's own confirming state broadcast) is measured and asserted under an explicit, documented threshold (500ms) rather than 'no noticeable slowdown' being left subjective (CONTEXT.md's Claude's Discretion item 3, resolved by this plan)."
    - "After the full 12-player, 3-round game reaches GAME_END and every scripted client disconnects, the exact same server process (no restart) accepts and completes a brand-new room creation from a fresh client — concrete proof the server survived the load without needing a restart."
    - "The test file follows this codebase's established socket.io-client integration-test harness (startTestServer/connectClient/waitFor from server/test/setup.ts, fakeMeme from server/test/fixtures/meme.ts, the waitForPhase/waitForRatingStep helper convention) rather than inventing a new tool, framework, or standalone script (CONTEXT.md's Claude's Discretion item 1, resolved in favor of a vitest integration test per 08-PATTERNS.md's recommendation — the least-new-tooling option)."
    - "A minimal MIN_PLAYERS_TO_START-player, 1-round tracer proof exists and passes before the full 12-player/3-round scenario is attempted, so an architectural wiring mistake in the load-test harness itself is caught on the cheapest possible slice first."
  artifacts:
    - path: "server/test/load.integration.test.ts"
      provides: "The load/capacity test proving DEPLOY-04 and LIVE-01 — a tracer slice plus the full 12-player/3-round scenario, both real-socket, both against a locally started in-process server."
      contains: "LOAD_TEST_PLAYER_COUNT"
  key_links:
    - from: "server/test/load.integration.test.ts"
      to: "server/src/rooms/Room.ts"
      via: "direct real-socket exercise of startGame/submitCaption/submitRating under genuine 12-way concurrency (Promise.all, not sequential awaits)"
      pattern: "submitRating|submitCaption"
    - from: "server/test/load.integration.test.ts"
      to: "server/test/setup.ts"
      via: "startTestServer()/connectClient()/waitFor() reused verbatim, one fresh server instance per describe block"
      pattern: "startTestServer"
  prohibitions:
    - statement: "MUST NOT point the test at the deployed Render URL, PUBLIC_BASE_URL, or any other non-local base URL — only the in-process startTestServer()-returned localhost URL (D-01/D-02's explicit reasoning for testing locally)."
      requirement_id: DEPLOY-04
      category: scope
      status: resolved
      verification: review
    - statement: "MUST NOT seed room.settings.writingSeconds/ratingSeconds/rounds in the 12-player/3-round scenario (Task 2) — that scenario must run against the room's true, unmodified default settings so the proof reflects real event-night pacing, not a test-only shortcut (CONTEXT.md's Claude's Discretion item 2, resolved by extending to the real DEFAULT_ROUND_COUNT rather than the literal minimum of 1 round)."
      requirement_id: LIVE-01
      category: precision
      status: resolved
      verification: review
---

<objective>
Prove the deployed server's architecture holds a full game with 12+ simultaneous players without
degrading, using a real Socket.IO integration test run against a locally started in-process server
(D-01/D-02 — the deployed Render URL is unreachable from this sandbox). A minimal 3-player tracer
proves the harness end-to-end first; the full 12-player, 3-round scenario then closes the gap
CONTEXT.md itself flagged (a single round never exercises a real round-transition under load, and
D-03's literal "1 round" isn't even a value a real host could pick — ROUND_COUNT_PRESETS has no
preset of 1) by playing the room's actual, unmodified default settings (3 rounds) to a fully
deterministic GAME_END.

Purpose: this phase's whole goal is DEPLOY-04 (a scripted load test) and LIVE-01 (12+ players,
no degradation) — both requirements are satisfied entirely by this one new test file; nothing else
in the codebase needs to change for a capacity *proof* (Established Patterns in 08-CONTEXT.md: the
room data structure already supports up to ROOM_CAPACITY=20 players structurally — this plan proves
*simultaneous load*, not room-size capacity).

Output: `server/test/load.integration.test.ts` — a tracer slice (3 players, 1 round) plus the real
capacity proof (12 concurrently-connected players, 3 rounds at true default pacing), with an
explicit, documented latency threshold and a final "server survives without restart" sanity check.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-load-capacity-verification/08-CONTEXT.md
@.planning/phases/08-load-capacity-verification/08-PATTERNS.md

@server/src/config.ts
@server/src/rooms/Room.ts
@server/src/rooms/rotation.ts
@shared/protocol.ts
@server/test/setup.ts
@server/test/capacity.integration.test.ts
@server/test/fullLoop.integration.test.ts
@server/test/hostActions.integration.test.ts
@server/test/fixtures/meme.ts

<interfaces>
Existing contracts this plan exercises — do not re-derive them.

`server/test/setup.ts`: `startTestServer(): Promise<TestServer>` returns `{ url, io, roomManager,
close }` — `url` is an ephemeral `http://localhost:<port>`, never a fixed port, never an env var.
`connectClient(url, token?): Promise<ClientSocket>` uses `transports: ["websocket"], reconnection:
false, forceNew: true` — copy exactly for every simulated player. `waitFor<T>(socket, eventName,
timeoutMs=5000): Promise<T>` resolves on the NEXT occurrence of that event (unfiltered) — safe only
for a fresh socket's first-ever `state` event or a genuinely single-shot event; unsafe during a
multi-client submission burst where several `state` broadcasts can arrive before the one you want
(that is exactly why Task 2 needs a NEW predicate-based helper — see below).

`shared/protocol.ts`: `CLIENT_EVENTS.createRoom/joinRoom/startGame/submitCaption/submitRating/
requestResync` and `SERVER_EVENTS.state/session/error` are the only events this plan needs.
`LobbySnapshot.progress: { submitted, total, submittedPlayerIds } | null` is non-null only while
`phase === "WRITING"`. `LobbySnapshot.ratingStep: { index, total, youAreAuthor, youMayRate,
youHaveRated, ratedCount, eligibleCount } | null` is non-null only while `phase === "RATING"`.
`LobbySnapshot.roundEnd: { entries: { authorId, ratings: RatingValue[], eligibleAtClose, score }[],
skippedByHost } | null` is non-null only while `phase === "ROUND_END"`. `LobbySnapshot.gameEnd: {
winners: PlayerView[], bestOfNight }` is non-null only at `phase === "GAME_END"`, where `deadlineAt`
is always `null`. `RatingValue = 1 | 2 | 3`.

`server/src/rooms/rotation.ts`: `buildRotation(submissions)` is literally `[...submissions.keys()]`
— rotation length equals the SUBMISSION count for that round, one rating step per submitted
caption (12 submitted captions => 12 rating steps). `eligibleRaters(players, authorId)` returns
every CONNECTED player except the author — with all 12 players connected throughout, every step has
exactly 11 eligible raters.

`server/src/config.ts`: `MIN_PLAYERS_TO_START = 3`; `DEFAULT_ROUND_COUNT = ROUND_COUNT_PRESETS[0]`
(= 3, `ROUND_COUNT_PRESETS = [3, 5, 7]` — no preset of 1); `DEFAULT_WRITING_SECONDS` (60),
`DEFAULT_RATING_SECONDS` (10); `WRITING_COLLAPSE_MS` (3000), `RATING_COLLAPSE_MS` (2000),
`BETWEEN_PHASES_MS` (3000), `BETWEEN_MEMES_MS` (2000) — the server's own fixed, non-configurable
pacing beats. Because every player in this plan's tests submits/rates promptly, the room's early
finish collapse (`maybeCollapseWriting`/`maybeCollapseRating` in `Room.ts`) always fires long before
`writingSeconds`/`ratingSeconds` would — real elapsed time per round is dominated by these
collapse/pacing constants, not the nominal deadline length, which is why Task 2 can safely leave
`writingSeconds`/`ratingSeconds` at their true defaults without risking a slow test.

`server/test/fixtures/meme.ts`: `fakeMeme(marker = "x"): string` — a validly base64-shaped
placeholder that clears `Room.submitCaption`'s validation without needing a real rasterized PNG.

`server/test/fullLoop.integration.test.ts` (lines 30-64): the established `waitForPhase(socket,
phase)` and `waitForRatingStep(socket, index)` helpers — copy verbatim, do not reinvent. Every
`Room` phase transition (writing close, reveal break, rating step open/close, round end, game end)
triggers exactly one `room.broadcast(io)` call synchronously from `server/src/socket/handlers.ts`
(wired via `room.onStateChanged = () => room.broadcast(io);`), and every successful
`submitCaption`/`submitRating` handler also calls `room.broadcast(io)` synchronously right after —
this is why a submitting client's OWN confirmation always arrives as a real, timely `state` event,
never something that needs polling.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tracer — 3 players, 1 round, join to GAME_END over real sockets</name>
  <reversibility rating="reversible">D-01/D-02 — the test target is a locally started in-process server, purely a `baseUrl` parameter (`server.url`) inside the test file itself; pointing this same harness at the deployed URL later (if the sandbox ever gains network access) is a one-line change, never an architectural one.</reversibility>
  <files>server/test/load.integration.test.ts</files>
  <read_first>
    - `server/test/setup.ts` — `startTestServer`, `connectClient`, `waitFor` (exact signatures, copy verbatim)
    - `server/test/fullLoop.integration.test.ts` lines 1-64 — file-level import shape, `waitForPhase`/`waitForRatingStep` helpers to copy verbatim, and the per-describe `beforeAll`/`afterAll` server-isolation convention
    - `server/test/capacity.integration.test.ts` lines 1-54 — the host-create-room-then-loop-join pattern, and the `}, <timeout>);` explicit per-`it()` timeout convention
    - `server/test/hostActions.integration.test.ts` lines 128-170 (`playRoundOneWithOneKnownScore`) — the `server.roomManager.findRoom(roomCode)` internal-seeding technique for a non-preset `settings.rounds` value, including its "throw if room not found" guard
    - `server/src/rooms/Room.ts` — `startGame`, `closeWriting`/`buildRotation` (D-08: rotation length = submission count), `submitRating`'s refusal order (`CANNOT_RATE_OWN` before `RATING_OUT_OF_RANGE`), `enterGameEnd` (terminal, `deadlineAt = null`)
  </read_first>
  <action>
Create `server/test/load.integration.test.ts`. Import `describe`, `it`, `expect`, `beforeAll`,
`afterAll` from `vitest`; `CLIENT_EVENTS`, `SERVER_EVENTS`, and the types `LobbySnapshot`,
`SessionIssued` from `@shared/protocol.js`; `startTestServer`, `connectClient`, `waitFor`, and the
type `TestServer` from `./setup.js`; `fakeMeme` from `./fixtures/meme.js`; and `MIN_PLAYERS_TO_START`
from `../src/config.js`.

Copy `waitForPhase(socket, phase)` and `waitForRatingStep(socket, index)` verbatim from
`fullLoop.integration.test.ts` (lines 30-64) as file-level (not per-describe) function
declarations — Task 2 reuses both unchanged.

Open one top-level `describe("a minimal MIN_PLAYERS_TO_START-player, 1-round game completes
end-to-end over real sockets (tracer)", () => { ... })` block with its own `let server: TestServer;`
plus `beforeAll(async () => { server = await startTestServer(); })` /
`afterAll(async () => { await server.close(); })` pair — one fresh server instance dedicated to this
`describe` block, never shared with Task 2's.

Inside a single `it("MIN_PLAYERS_TO_START scripted players join, write, rate meme-by-meme, and
reach GAME_END", async () => { ... }, 30000)` (explicit 30-second timeout — at 3 players and 3
rating steps this round collapses in well under 20s real time: 3000ms WRITING_COLLAPSE_MS, then a
3000ms BETWEEN_PHASES_MS break before step 0, then 3 rating steps each closing on the 2000ms
RATING_COLLAPSE_MS, the later two preceded by a 2000ms BETWEEN_MEMES_MS break instead):

1. Connect the host via `connectClient(server.url)`; register `waitFor<LobbySnapshot>(host,
   SERVER_EVENTS.state)` before emitting `CLIENT_EVENTS.createRoom` with `{ name: "מארח" }`;
   capture `roomCode` from the resolved snapshot.
2. Connect and join exactly `MIN_PLAYERS_TO_START - 1` (2) more players in a plain sequential `for`
   loop (named `שחקן1`/`שחקן2`) — concurrency stress is Task 2's job, not this tracer's. Collect all
   three sockets into `const allClients = [host, playerB, playerC];`.
3. Seed the round count via `const room = server.roomManager.findRoom(roomCode); if (!room) throw
   new Error("room not found for internal seeding"); room.settings.rounds = 1;` — a test-only
   internal seeding of a non-preset value (1 is not in `ROUND_COUNT_PRESETS`), matching the
   established technique in `fullLoop.integration.test.ts`/`hostActions.integration.test.ts`, used
   here ONLY to keep this tracer's single round fast. Do NOT seed `writingSeconds`/`ratingSeconds`
   — leave them at the room's true defaults; every player here submits/rates promptly so the
   natural collapse always wins well before either real deadline.
4. Host emits `CLIENT_EVENTS.startGame` with `{}`; before emitting, register
   `Promise.all([host, playerB, playerC].map((c) => waitFor<LobbySnapshot>(c, SERVER_EVENTS.state)))`
   and await it, asserting each resolved snapshot's `phase === "WRITING"`.
5. All three clients submit a caption sequentially (`CLIENT_EVENTS.submitCaption` with `{ meme:
   fakeMeme(<a distinct marker per client>) }`), each followed by an `await` on the other two
   clients' next `state` event (mirroring `hostActions.integration.test.ts`'s own two-submission
   pattern) — this is the tracer, not the concurrency proof.
6. Await `waitForRatingStep(host, 0)`. Determine step 0's author: whichever of the three resolved
   `ratingStep` payloads (poll each client's own state via a quick `request-resync` if needed, or
   simply check `youAreAuthor` on the payload each client itself would see — the simplest correct
   approach is to call `waitForRatingStep` on ALL THREE clients via `Promise.all` and find the one
   whose `ratingStep.youAreAuthor === true`) is the author; the other two are step 0's raters. Both
   raters emit `CLIENT_EVENTS.submitRating` with `{ stepIndex: 0, value: 2 }`; await
   `waitForRatingStep(host, 1)` (registered on all three clients via `Promise.all`, same
   author-detection technique) to confirm the step advanced and identify step 1's author/raters.
   Repeat for step 1, then for step 2 — except after step 2's raters submit, await
   `waitForPhase(host, "ROUND_END")` instead of a step 3 (a 3-submission round has exactly 3 rating
   steps, indices 0-2).
7. Await `waitForPhase(host, "GAME_END")` (since `settings.rounds === 1`, `finishRound`'s own
   `BETWEEN_PHASES_MS` continuation goes straight to `enterGameEnd()`, never back to `enterWriting`).
   Assert the resolved snapshot's `phase === "GAME_END"` and `deadlineAt === null`.
8. Close all three sockets: `allClients.forEach((s) => s.close());`.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/load.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>grep -c "waitForRatingStep" server/test/load.integration.test.ts</automated>
    <fails_when>the command's output is less than 2 (the function declaration plus at least one call site)</fails_when>
  </verify>
  <acceptance_criteria>
    - server/test/load.integration.test.ts exists, imports startTestServer/connectClient/waitFor from ./setup.js and fakeMeme from ./fixtures/meme.js
    - waitForPhase and waitForRatingStep are declared at file level, copied verbatim from fullLoop.integration.test.ts
    - Exactly MIN_PLAYERS_TO_START (3) scripted clients connect, one creates the room, two join, and the host starts a 1-round game seeded via server.roomManager.findRoom(roomCode).settings.rounds = 1
    - All 3 players submit a caption, every rating step is rated by both of its eligible (non-author) raters, and the test asserts phase reaches GAME_END with deadlineAt === null
    - npm --prefix server run test -- --run test/load.integration.test.ts exits 0
    - npm --prefix server run typecheck exits 0
  </acceptance_criteria>
  <done>A minimal 3-player, 1-round game completes join to caption to meme-by-meme rating to GAME_END entirely over real sockets against a local in-process server, proving the load test's harness and event sequencing are wired correctly before scaling to the full 12-player scenario.</done>
</task>

<task type="auto">
  <name>Task 2: The real capacity proof — 12 concurrent players, 3 rounds, latency + no-loss assertions, no-restart sanity check</name>
  <reversibility rating="reversible">Extending beyond D-03's literal "1 round" to the room's real DEFAULT_ROUND_COUNT (3) is a test-script parameter, explicitly invited by CONTEXT.md's Claude's Discretion item 2 ("the planner should flag if this leaves an obvious gap") — nothing here is architectural or one-way; the round count and player count are both local constants in the test file.</reversibility>
  <files>server/test/load.integration.test.ts</files>
  <read_first>
    - This same file's Task 1 output — `waitForPhase`/`waitForRatingStep`, the `connectClient`/`startTestServer` import shape, the per-describe server-isolation convention
    - `server/test/capacity.integration.test.ts` lines 12-54 — the closest existing analog for a many-client loop (though Task 2 must parallelize with `Promise.all` where that file uses a sequential `for` loop, since genuine concurrency is the point of this task)
    - `server/src/rooms/Room.ts` — `snapshotFor` (exactly which LobbySnapshot fields are non-null in which phase — `progress` only in WRITING, `ratingStep` only in RATING, `roundEnd` only in ROUND_END), `maybeCollapseWriting`/`maybeCollapseRating` (why leaving writingSeconds/ratingSeconds at their true defaults is safe here), `finishRound` (the ROUND_END -> next WRITING or GAME_END continuation this task's round loop drives through 3 times)
    - `shared/protocol.ts` — `RatingValue`, `GameEndView` (`winners: PlayerView[]`), `RoundEndEntry` (`ratings`, `eligibleAtClose`, `score`)
  </read_first>
  <action>
Append a second top-level `describe("LOAD_TEST_PLAYER_COUNT concurrently-connected scripted
players complete a full DEFAULT_ROUND_COUNT-round game against the server without crashing,
hanging, losing a submission, or requiring a restart (DEPLOY-04, LIVE-01)", () => { ... })` block to
the same file, with its OWN `let server: TestServer;` plus `beforeAll`/`afterAll` pair — a fresh
server instance, never shared with Task 1's tracer describe block.

Extend this file's existing `@shared/protocol.js` import to also bring in the type `RatingValue`,
and this file's existing `../src/config.js` import to also bring in `DEFAULT_ROUND_COUNT` (alongside
the already-imported `MIN_PLAYERS_TO_START`).

At module scope (above both `describe` blocks, or immediately below the imports), declare three
constants: `const LOAD_TEST_PLAYER_COUNT = 12;` with a comment citing D-03's literal "12+" floor and
LIVE-01; `const LOAD_TEST_RATING_VALUE: RatingValue = 2;` with a comment explaining every rater
casting the same value makes the final score fully deterministic, so a single assertion can prove
zero ratings were lost or double-counted; `const LOAD_TEST_MAX_LATENCY_MS = 500;` with a comment
explaining this is a generous ceiling for a same-process, loopback, ephemeral-port round-trip
(client emit -> that same client's own confirming state broadcast) — `WRITING_COLLAPSE_MS`/
`RATING_COLLAPSE_MS`/`BETWEEN_PHASES_MS`/`BETWEEN_MEMES_MS` are the product's own intentional
pacing beats and are never counted toward this per-submission figure.

Add one new file-level helper, placed directly below `waitForRatingStep`: `function
waitForOwnState(socket: Awaited<ReturnType<typeof connectClient>>, predicate: (snapshot:
LobbySnapshot) => boolean): Promise<LobbySnapshot>` — identical shape to `waitForPhase`/
`waitForRatingStep` (register `socket.on(SERVER_EVENTS.state, onState)`; inside the listener, call
`predicate(snapshot)` instead of a hardcoded phase/index check; `off` and `resolve` on a match). This
is genuinely new territory (08-PATTERNS.md's own "No Analog Found" note — no existing test measures
a specific client's own confirmation under concurrent load) and is why a bare `waitFor` is unsafe
here: during a 12-way concurrent submission burst, several `state` broadcasts (from OTHER players'
submissions) can arrive at a given socket before that socket's OWN confirming one.

Inside one `it("12 concurrent players complete a full 3-round game with zero lost submissions,
sub-500ms latency, and the server stays up for a brand-new room afterward", async () => { ... },
300000)` (explicit 300-second/5-minute timeout — with the room's real, unmodified default settings
(DEFAULT_ROUND_COUNT=3 rounds, 60s writing, 10s rating) and every player submitting/rating promptly,
the natural collapse timing works out to roughly 50-55s per round x 3 rounds plus two 3-second
between-round breaks, so budget generously above the ~165s expected wall-clock time):

1. Connect the host, create the room exactly as Task 1's step 1, capturing `roomCode`.
2. Connect the remaining `LOAD_TEST_PLAYER_COUNT - 1` (11) joiners CONCURRENTLY: `const joiners =
   await Promise.all(Array.from({ length: LOAD_TEST_PLAYER_COUNT - 1 }, () =>
   connectClient(server.url)));` — this is the genuine concurrency stress LIVE-01 calls for, unlike
   Task 1's sequential tracer loop or `capacity.integration.test.ts`'s own sequential loop.
3. Have all 11 joiners emit `CLIENT_EVENTS.joinRoom` CONCURRENTLY: for each joiner at index `i`,
   register `waitFor<LobbySnapshot>(joiner, SERVER_EVENTS.state)` (safe here specifically because it
   is that fresh socket's FIRST-EVER `state` event) BEFORE emitting `{ roomCode, name:
   \`שחקן${i}\` }`, then `Promise.all` the whole array of 11 such emit+await pairs. Build `const
   allClients = [host, ...joiners];` (length `LOAD_TEST_PLAYER_COUNT`).
4. Host emits `CLIENT_EVENTS.startGame` with `{}` — do NOT seed `room.settings` at all in this task
   (unlike Task 1 and every other integration test file in this codebase): the real capacity proof
   must run the exact game a real host would configure on party night, not a synthetic shortcut.
   Register `Promise.all(allClients.map((c) => waitFor<LobbySnapshot>(c, SERVER_EVENTS.state)))`
   before the emit; await it, asserting every resolved snapshot's `phase === "WRITING"`.
5. Declare `const latencies: number[] = [];`. For `roundIndex` 1 through `DEFAULT_ROUND_COUNT` (a
   plain `for` loop):
   a. All 12 clients submit a caption CONCURRENTLY: for each client, capture `const t0 =
      Date.now();`, register `waitForOwnState(client, (s) => s.youSubmitted === true)` BEFORE
      emitting `CLIENT_EVENTS.submitCaption` with `{ meme: fakeMeme(<client's index>) }`, and on
      resolution push `Date.now() - t0` into `latencies`; `Promise.all` all 12 such pairs.
   b. Immediately after (well before `WRITING_COLLAPSE_MS` elapses), emit
      `CLIENT_EVENTS.requestResync` on the host, register `waitFor<LobbySnapshot>(host,
      SERVER_EVENTS.state)` first, and assert the resolved snapshot's `progress?.submitted ===
      LOAD_TEST_PLAYER_COUNT` and `progress?.total === LOAD_TEST_PLAYER_COUNT` — the first of three
      independent no-loss checks for this round.
   c. For `stepIndex` 0 through `LOAD_TEST_PLAYER_COUNT - 1` (11, inclusive — 12 steps total,
      matching 12 submitted captions): register `Promise.all(allClients.map((c) =>
      waitForRatingStep(c, stepIndex)))` and await it; find the author via
      `snapshots.findIndex((s) => s.ratingStep?.youAreAuthor)`; build `const raters =
      allClients.filter((_, idx) => idx !== authorIdx);` (the 11 non-author eligible raters). All 11
      raters submit CONCURRENTLY exactly like step 5a but with `CLIENT_EVENTS.submitRating` / `{
      stepIndex, value: LOAD_TEST_RATING_VALUE }` and the predicate `(s) => s.ratingStep?.index ===
      stepIndex && s.ratingStep?.youHaveRated === true`, pushing each latency into the same
      `latencies` array.
   d. Once `stepIndex` reaches `LOAD_TEST_PLAYER_COUNT - 1` (the last of the 12 steps), instead of
      registering the next `waitForRatingStep`, await `waitForPhase(host, "ROUND_END")`. Assert the
      resolved snapshot's `roundEnd?.entries.length === LOAD_TEST_PLAYER_COUNT`, and for every
      entry: `entry.ratings.length === LOAD_TEST_PLAYER_COUNT - 1`, `entry.eligibleAtClose ===
      LOAD_TEST_PLAYER_COUNT - 1`, and `entry.score === (LOAD_TEST_PLAYER_COUNT - 1) *
      LOAD_TEST_RATING_VALUE` (22) — the second and third independent no-loss checks for this round.
   e. If `roundIndex < DEFAULT_ROUND_COUNT`, register `Promise.all(allClients.map((c) =>
      waitForPhase(c, "WRITING")))` and await it (the `BETWEEN_PHASES_MS` continuation
      `finishRound` already schedules) before the loop's next iteration.
6. After the loop, register `Promise.all(allClients.map((c) => waitForPhase(c, "GAME_END")))` and
   await it. Assert every resolved snapshot's `deadlineAt === null`; assert
   `gameEndSnapshot.gameEnd?.winners.length === LOAD_TEST_PLAYER_COUNT` (every player deterministically
   tied); compute `const expectedScorePerPlayer = DEFAULT_ROUND_COUNT * (LOAD_TEST_PLAYER_COUNT - 1)
   * LOAD_TEST_RATING_VALUE;` (never hardcode this — it must be derived from the same three
   constants so a future config change cannot silently desync the assertion) and assert every entry
   in `gameEndSnapshot.players` has `score === expectedScorePerPlayer`.
7. Assert `Math.max(...latencies) < LOAD_TEST_MAX_LATENCY_MS` — the single latency-threshold
   assertion covering all 36 caption-submission and 396 rating-submission round trips collected
   across the whole 3-round loop.
8. Close all 12 sockets: `allClients.forEach((s) => s.close());`. Then connect one brand-new `const
   sanityClient = await connectClient(server.url);`; register `waitFor<LobbySnapshot>(sanityClient,
   SERVER_EVENTS.state)`; emit `CLIENT_EVENTS.createRoom` with `{ name: "בדיקת שפיות" }`; assert the
   resolved snapshot's `phase === "LOBBY"` and its `roomCode` differs from the load game's
   `roomCode` — concrete proof the exact same server process that just carried the full 12-player,
   3-round load is still fully responsive and needs no restart. Close `sanityClient`.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/load.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary output contains a line with "failed"</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>grep -c "LOAD_TEST_PLAYER_COUNT = 12" server/test/load.integration.test.ts</automated>
    <fails_when>the command's output is 0, meaning the 12-player load scenario constant was never added</fails_when>
    <automated>grep -c "sanityClient" server/test/load.integration.test.ts</automated>
    <fails_when>the command's output is 0, meaning the post-load "server survives without restart" sanity room check was never added</fails_when>
    <automated>grep -c "waitForOwnState" server/test/load.integration.test.ts</automated>
    <fails_when>the command's output is less than 2 (the function declaration plus at least one call site), meaning the predicate-based confirmation helper this plan's latency measurement depends on was never added</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly LOAD_TEST_PLAYER_COUNT (12) scripted clients connect — 11 joiners connected and joined via Promise.all, not a sequential await loop — and complete a full DEFAULT_ROUND_COUNT-round (3-round) game using the room's real, unmodified default settings (no writingSeconds/ratingSeconds/rounds seeding anywhere in this task)
    - Every one of the 36 caption submissions and 396 rating submissions is confirmed via a predicate-based waitForOwnState wait, and every confirmation's latency is asserted under LOAD_TEST_MAX_LATENCY_MS (500ms)
    - Each round's ROUND_END snapshot shows entries.length === 12, every entry's ratings.length === 11, eligibleAtClose === 11, and score === 22 (zero lost submissions, checked independently for all 3 rounds)
    - The GAME_END snapshot shows every player's score === expectedScorePerPlayer (computed, not hardcoded) and gameEnd.winners has length 12 (a full deterministic tie)
    - After every one of the 12 load-test sockets is closed, a brand-new sanityClient connects to the SAME server instance and successfully creates a new room, proving no restart was needed
    - npm --prefix server run test -- --run test/load.integration.test.ts exits 0
    - npm --prefix server run test -- --run (the full suite) exits 0 with zero regressions
    - npm --prefix server run typecheck exits 0
  </acceptance_criteria>
  <done>12 concurrently-connected scripted clients complete a full 3-round game (the room's real default settings) with zero lost captions or ratings, every submission's round-trip latency under 500ms, a fully deterministic tied final score confirming no rating was double-counted or dropped, and the same server instance proven still responsive to a brand-new room afterward — DEPLOY-04 and LIVE-01 both satisfied.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by this plan (new to the codebase):

**New file:** `server/test/load.integration.test.ts`.

**File-level helpers (copied/new within that file):** `waitForPhase` and `waitForRatingStep`
(copied verbatim from `fullLoop.integration.test.ts`, per this codebase's established convention of
every integration test file defining its own local copy rather than importing a shared one);
`waitForOwnState` (genuinely new — a predicate-based state-wait helper, needed because a bare
`waitFor` cannot safely distinguish "my own submission was confirmed" from "some other player's
concurrent submission just broadcast" during a 12-way concurrent burst).

**Test-file-local constants:** `LOAD_TEST_PLAYER_COUNT` (12), `LOAD_TEST_RATING_VALUE` (2),
`LOAD_TEST_MAX_LATENCY_MS` (500).

**No new npm script.** Reuses the existing `npm --prefix server run test -- --run <path>` invocation
pattern already used by every other integration test file, per CONTEXT.md's "least new tooling"
discretion resolution.

**No production code changed.** `server/src/config.ts`, `server/src/rooms/Room.ts`,
`shared/protocol.ts`, and every other non-test file are read-only references for this plan — the
room's structural capacity (ROOM_CAPACITY=20) and settings presets already support 12 players and a
3-round game with zero code changes (08-CONTEXT.md's own "Established Patterns" note).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| test process (this repo's own dev/CI machine) -> in-process ephemeral-port Socket.IO server | same-process, same-machine — not a real network boundary, but the new file's scripted "clients" exercise the exact same handler code (`server/src/socket/handlers.ts`) untrusted real phones would hit |
| 12 (or 3) scripted client sockets -> server socket handlers (`submitCaption`/`submitRating`/`joinRoom`) | many-to-one concurrent input, the same shape a real 12-phone room's traffic would produce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | the base URL the test connects to | high | mitigate | The test exclusively connects to `server.url`, the ephemeral `http://localhost:<port>` returned by this file's own `startTestServer()` call in each `beforeAll` — never a hardcoded `https://makeitmemetamir.onrender.com` and never any `PUBLIC_BASE_URL`/env-var-derived host (D-01/D-02's explicit reasoning for testing locally, not against the real production server); this is also this plan's own `prohibitions` entry. |
| T-08-02 | Denial of Service | 12 concurrent sockets + timers against the dev/CI machine | low | accept | Bounded to exactly 12 short-lived sockets (plus one final sanity socket in Task 2), all explicitly `.close()`d at the end of each `it()` and via each `describe` block's own `afterAll(() => server.close())`; a generous but finite per-`it()` vitest timeout (30s / 300s) caps the worst-case hang — the same bounded pattern `capacity.integration.test.ts`'s existing 20-player join loop already runs today with no incident. |
| T-08-03 | Information Disclosure | session tokens / meme payloads used in the test | low | accept | `fakeMeme()` produces a synthetic placeholder string and session tokens come from `randomUUID()`-based server-side issuance at connect time — nothing in this new file hardcodes or logs a real credential, API key, or production secret. |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs no new package — `socket.io-client` is an existing `devDependency` already used by every other `*.integration.test.ts` file; `package.json`/lockfiles are not in `files_modified`. |

</threat_model>

<verification>
- `npm --prefix server run test -- --run test/load.integration.test.ts` exits 0 (both the tracer and the full 12-player/3-round scenario pass)
- `npm --prefix server run test -- --run` (the full existing server suite) exits 0 with zero regressions
- `npm --prefix server run typecheck` exits 0
- `server/test/load.integration.test.ts` proves: 12 concurrent players complete a full 3-round game against a locally started server with zero lost captions/ratings, every submission's round-trip latency under the documented 500ms threshold, and the same server instance survives to serve a brand-new room afterward with no restart
</verification>

<success_criteria>
- A simulated test using 12+ scripted clients (not real guests) completes a full game against the server without crashing, hanging, or requiring a restart (ROADMAP Phase 8 success criterion 1)
- During that test, 12+ simultaneous players submit captions and ratings within the same round with no submission lost and no noticeable slowdown, backed by an explicit, documented latency threshold rather than a subjective judgment (ROADMAP Phase 8 success criterion 2)
- CONTEXT.md's three Claude's Discretion items are each explicitly resolved in this plan: client implementation (a vitest socket.io-client integration test, least new tooling), round-count sufficiency (extended from D-03's literal 1 round to the room's real DEFAULT_ROUND_COUNT of 3, closing the round-transition gap CONTEXT.md itself flagged), and the latency threshold (500ms, documented and justified)
</success_criteria>

<output>
Create `.planning/phases/08-load-capacity-verification/08-01-SUMMARY.md` when done
</output>