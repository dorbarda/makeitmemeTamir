---
phase: 04-full-round-rating-scoring-completion
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - shared/protocol.ts
  - shared/messages.ts
  - server/src/rooms/Room.ts
  - client/src/screens/Round.tsx
  - client/src/screens/round/RoundEndPanel.tsx
  - client/src/screens/round/GameEndPanel.tsx
  - client/src/index.css
  - server/test/gameEnd.integration.test.ts
  - server/test/bestOfNight.integration.test.ts
autonomous: true
requirements: [SCORE-02, SCORE-04, MEME-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each round's score keeps accumulating into every player's running total exactly as it already did (applyRoundScores, unchanged) across a full multi-round game, with no reset and no special backfill logic for a late joiner (SCORE-02)."
    - "Once the last round ends, every player tied for the single highest score is shown as a winner — all of them, never just one, when there is a tie (SCORE-04, D-03)."
    - "A 'best of the night' screen shows the top 3 highest-scoring individual memes from across the whole game — photo, caption, author, and score — built incrementally round by round rather than recomputed by re-scanning history at game end (MEME-02, D-04)."
  artifacts:
    - path: "shared/protocol.ts"
      provides: "GameEndView, BestOfEntry, and LobbySnapshot.gameEnd"
      contains: "GameEndView"
    - path: "server/src/rooms/Room.ts"
      provides: "bestOfNight, updateBestOfNight, and buildGameEndView"
      contains: "updateBestOfNight"
    - path: "client/src/screens/round/GameEndPanel.tsx"
      provides: "the winner banner, the final scoreboard, and the best-of-night list, mounted only for GAME_END"
    - path: "server/test/gameEnd.integration.test.ts"
      provides: "the end-to-end proof that a played-out game's real winner(s) and real best-of memes reach the client"
  key_links:
    - from: "server/src/rooms/Room.ts (enterRoundEnd)"
      to: "server/src/rooms/Room.ts (bestOfNight)"
      via: "enterRoundEnd calls updateBestOfNight() immediately after applyRoundScores(), every round"
      pattern: "updateBestOfNight\\("
    - from: "server/src/rooms/Room.ts (snapshotFor)"
      to: "shared/protocol.ts"
      via: "snapshotFor populates gameEnd only when phase === \"GAME_END\", from buildGameEndView()"
      pattern: "buildGameEndView\\("
    - from: "client/src/screens/Round.tsx"
      to: "client/src/screens/round/GameEndPanel.tsx"
      via: "Round.tsx mounts GameEndPanel only for phase === \"GAME_END\", replacing RoundEndPanel there"
      pattern: "GameEndPanel"
  prohibitions:
    - statement: "MUST NOT recompute bestOfNight by re-scanning every round's history at game end — it must already be the running list updateBestOfNight maintained incrementally (D-04)"
      requirement_id: MEME-02
      category: performance
      status: resolved
      verification: review
    - statement: "MUST NOT apply any tiebreaker to gameEnd.winners — every player whose score equals the max must appear (D-03)"
      requirement_id: SCORE-04
      category: correctness
      status: resolved
      verification: test
---

<objective>
Close out the game loop's ending, on top of Plan 04-01's swap/no-repeat/round-ranking work: the
running score total keeps accumulating coherently across a full multi-round game with no redesign
(SCORE-02), a final winner screen shows every player tied for the highest score with no tiebreaker
(SCORE-04, D-03), and a "best of the night" screen shows the real top 3 highest-scoring memes from
the whole game, tracked incrementally round by round rather than reconstructed at game end
(MEME-02, D-04).

Purpose: this is the last piece of Phase 4's own goal — "the round loop and endgame reach the
complete game spec." Everything here reads values Plan 04-01 already made trustworthy
(`Player.score` via `applyRoundScores`, a real per-meme `score` via `buildRoundEndView`); nothing
here invents a new source of truth.

Output: a dedicated `GameEndPanel` shown only once the game ends, replacing `RoundEndPanel`'s
generic round-end view there, with a real winner banner (including ties), the existing player
scoreboard, and a real, incrementally-tracked top-3 best-of-the-night list.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md
@.planning/phases/02-server-authoritative-round-engine/02-PATTERNS.md

@shared/protocol.ts
@shared/messages.ts
@server/src/config.ts
@server/src/rooms/Room.ts
@client/src/screens/Round.tsx
@client/src/screens/round/RoundEndPanel.tsx
@client/src/index.css
@server/test/setup.ts
@server/test/fullLoop.integration.test.ts
@server/test/scoring.integration.test.ts

<interfaces>
Existing contracts this plan extends — do not re-derive them. These are stated as they exist AFTER
Plan 04-01 has landed (this plan's own `depends_on: ["04-01"]` guarantees that ordering).

From `shared/protocol.ts` (after Plan 04-01):
```
export type RoundEndEntry = { authorId: string; authorName: string; caption: string; ratings: RatingValue[]; eligibleAtClose: number; score: number; };
export type RoundEndView = { entries: RoundEndEntry[] };
export type LobbySnapshot = { ...; players: PlayerView[]; roundEnd: RoundEndView | null; ... };
```
This plan adds `BestOfEntry`, `GameEndView`, and `LobbySnapshot.gameEnd`. No existing field is
renamed or removed.

From `server/src/rooms/Room.ts` (after Plan 04-01): `applyRoundScores()` (the sole `Player.score`
mutation site, called first inside `enterRoundEnd()`), `photoUrlFor(playerId)` (returns this
player's current-round photo URL, drawing/caching one if absent), `enterRoundEnd()` (calls
`applyRoundScores()`, sets `phase = "ROUND_END"`, schedules the between-phases beat that either
starts the next round's `enterWriting()` or calls `enterGameEnd()`), `enterGameEnd()` (terminal:
`phase = "GAME_END"`, clears the phase timer, `deadlineAt = null`), `snapshotFor(playerId)`
(currently: `roundEnd: this.phase === "ROUND_END" || this.phase === "GAME_END" ? this.buildRoundEndView() : null,`
— this plan changes that condition to `"ROUND_END"` only and adds a new `gameEnd` field for
`"GAME_END"`).

From `client/src/screens/Round.tsx` (after Plan 04-01, unchanged by that plan): mounts
`RoundEndPanel` for both `"ROUND_END"` and `"GAME_END"` today:
```
{(snapshot.phase === "ROUND_END" || snapshot.phase === "GAME_END") && (<RoundEndPanel snapshot={snapshot} />)}
```
`PANEL_ALREADY_LISTS_PLAYERS` is a `ReadonlySet<RoomPhase>` already containing both `"ROUND_END"`
and `"GAME_END"` — the baseline player-roster list at the bottom of this screen is already
suppressed for both phases and needs no change.

From `client/src/screens/round/RoundEndPanel.tsx` (after Plan 04-01): already computes
`const scoreboard = [...snapshot.players].sort((a, b) => b.score - a.score);` and renders it as a
`.scoreboard`/`.scoreboard-list`/`.scoreboard-entry` section — `GameEndPanel.tsx` copies this exact
same three-line computation and the same three CSS classes rather than importing anything from
`RoundEndPanel.tsx` (these stay two independent small components, per this codebase's existing
one-component-per-file convention).
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end game conclusion — winner(s) with ties and the incrementally-tracked best-of-the-night</name>
  <reversibility rating="reversible">D-03/D-04 are display/aggregation choices (show every tied winner, keep a top-3 running list) sitting behind Room's existing read-computed-view pattern (buildRoundEndView/snapshotFor) — none require a different architecture to change later.</reversibility>
  <files>shared/protocol.ts, server/src/rooms/Room.ts, shared/messages.ts, client/src/screens/Round.tsx, client/src/screens/round/RoundEndPanel.tsx, client/src/screens/round/GameEndPanel.tsx, client/src/index.css, server/test/gameEnd.integration.test.ts</files>
  <read_first>
    - `server/src/rooms/Room.ts` — `enterRoundEnd`, `enterGameEnd`, `applyRoundScores`, `buildRoundEndView`, `snapshotFor`'s `roundEnd`/`gameEnd` lines, exactly as they stand after Plan 04-01
    - `shared/protocol.ts` — the current `RoundEndEntry`/`RoundEndView`/`LobbySnapshot` shapes listed in this plan's `<interfaces>` block
    - `client/src/screens/Round.tsx` — the current phase-to-panel wiring and `PANEL_ALREADY_LISTS_PLAYERS` set
    - `client/src/screens/round/RoundEndPanel.tsx` — the current scoreboard section this task's `GameEndPanel.tsx` reuses (by copying, not importing)
    - `server/test/fullLoop.integration.test.ts` — the real-socket, multi-client pattern for driving a room all the way to `GAME_END`, including its own `waitForPhase` helper shape
  </read_first>
  <action>
Extend `shared/protocol.ts`:
  - Add two new exported types, placed immediately after `RoundEndView`:
    `BestOfEntry = { authorId: string; authorName: string; caption: string; photoUrl: string; score: number; }`
    (MEME-02/D-04 — everything the client needs to render one best-of entry without a second lookup)
    and `GameEndView = { winners: PlayerView[]; bestOfNight: BestOfEntry[]; }` (SCORE-04/D-03's tied
    winners plus MEME-02/D-04's running top-3 list).
  - Add `gameEnd: GameEndView | null;` to `LobbySnapshot`, immediately after the existing
    `roundEnd: RoundEndView | null;` line, with a comment noting it is populated only in `GAME_END`.

Extend `server/src/rooms/Room.ts`:
  - Import `type BestOfEntry` and `type GameEndView` alongside the other `@shared/protocol.js` type
    imports.
  - Add a new public field `bestOfNight: BestOfEntry[] = [];`, placed beside the existing
    `photoAssignments` field (MEME-02/D-04 — the running top-3 highest-scoring memes across the whole
    game so far, always length <= 3, always sorted highest-score-first; never cleared between
    rounds, never recomputed from round history).
  - Add `private updateBestOfNight(): void` — walks `this.rotation` the same way `applyRoundScores`
    does: for each `[authorId, index]` pair where `this.ratings.get(index)` exists, compute that
    step's total the same way `applyRoundScores` does (`[...stepRatings.values()].reduce((sum, value) => sum + value, 0)`),
    push a new entry onto `this.bestOfNight` shaped
    `{ authorId, authorName: this.players.get(authorId)?.name ?? "", caption: this.submissions.get(authorId) ?? "", photoUrl: this.photoUrlFor(authorId), score }`,
    then after the loop, `this.bestOfNight.sort((a, b) => b.score - a.score);` and, if
    `this.bestOfNight.length > 3`, `this.bestOfNight.length = 3;`. `Array.prototype.sort`'s
    stability means a later round's meme with a score EQUAL to an already-surviving entry's score
    never displaces it — the earlier-inserted survivor keeps the #3 slot, matching D-03's own
    no-tiebreaker philosophy applied to this list's eviction boundary. This method must only ever be
    called from `enterRoundEnd()` (never later), since it calls `photoUrlFor(authorId)` while
    `photoAssignments` still holds THIS round's draw, before the next round's `enterWriting()` can
    overwrite it.
  - In `enterRoundEnd()`, add `this.updateBestOfNight();` immediately after the existing
    `this.applyRoundScores();` line — same chokepoint, same ordering rule (compute from the room's
    own current-round state before the phase transitions).
  - Add `private buildGameEndView(): GameEndView` — computed fresh every call, never cached:
    `const players = [...this.players.values()];`,
    `const maxScore = players.reduce((max, p) => Math.max(max, p.score), 0);`,
    `const winners: PlayerView[] = players.filter((p) => p.score === maxScore).map((p) => ({ id: p.id, name: p.name, connected: p.connected, isHost: p.id === this.hostId, score: p.score }));`,
    return `{ winners, bestOfNight: this.bestOfNight };` (SCORE-04/D-03 — every player whose score
    equals the max, never just one on a tie; `bestOfNight` is only READ here, never recomputed).
  - In `snapshotFor`, change the existing line
    `roundEnd: this.phase === "ROUND_END" || this.phase === "GAME_END" ? this.buildRoundEndView() : null,`
    to `roundEnd: this.phase === "ROUND_END" ? this.buildRoundEndView() : null,` and add a new line
    immediately after it: `gameEnd: this.phase === "GAME_END" ? this.buildGameEndView() : null,`.
    Update the comment immediately above this pair to describe the new split: `roundEnd` is now
    `ROUND_END`-only, `gameEnd` is `GAME_END`'s own dedicated view.

Extend `shared/messages.ts`: add a new comment-grouped section to `HEBREW_UI`, after the existing
`roundResultsPointsSuffix` entry, with three keys: `winnerHeading: "המנצחים"`,
`bestOfNightHeading: "המצחיקים של הערב"`, and
`bestOfNightEmpty: "עדיין אין מספיק דירוגים כדי לבחור את המצחיקים של הערב"`.

Create `client/src/screens/round/GameEndPanel.tsx` as a new component, following the exact
prop/import shape of `RoundEndPanel.tsx` (`type GameEndPanelProps = { snapshot: LobbySnapshot };`,
guard first with `if (!snapshot.gameEnd) return null;`): compute
`const scoreboard = [...snapshot.players].sort((a, b) => b.score - a.score);` (copy this line from
`RoundEndPanel.tsx` — do not import it, these stay two independent small components), then render
three sections inside a `<section className="game-end-panel">`:
  1. `<section className="winner-banner">` with `<h2>{HEBREW_UI.winnerHeading}</h2>` and a
     `<ul className="winner-list">` mapping `snapshot.gameEnd.winners` to
     `<li key={winner.id} className="winner-entry">{winner.name} — {winner.score} {HEBREW_UI.roundResultsPointsSuffix}</li>`
     (D-03 — every tied winner listed, never just one).
  2. `<section className="scoreboard">` with `<h2>{HEBREW_UI.scoreboardHeading}</h2>` and an
     `<ol className="scoreboard-list">` mapping `scoreboard` to
     `<li key={player.id} className="scoreboard-entry"><span>{player.name}</span><span>{player.score}</span></li>`
     — reuses the existing `.scoreboard`/`.scoreboard-list`/`.scoreboard-entry` CSS classes verbatim,
     no new scoreboard styling needed.
  3. `<section className="best-of-night">` with `<h2>{HEBREW_UI.bestOfNightHeading}</h2>`, then
     either `<p>{HEBREW_UI.bestOfNightEmpty}</p>` when `snapshot.gameEnd.bestOfNight.length === 0`,
     or an `<ol className="best-of-list">` mapping `snapshot.gameEnd.bestOfNight` (with the array's
     own `index`, since the same author can appear more than once) to
     `<li key={\`${entry.authorId}-${index}\`} className="best-of-entry"><img className="meme-photo" src={entry.photoUrl} alt={HEBREW_UI.photoAlt} /><p>{entry.caption}</p><p className="best-of-meta">{entry.authorName} — {entry.score} {HEBREW_UI.roundResultsPointsSuffix}</p></li>`
     (D-04 — real photo, real caption, real author, real score, top 3 only).

Edit `client/src/screens/Round.tsx`: import `GameEndPanel` from `./round/GameEndPanel`; replace the
single combined line
`{(snapshot.phase === "ROUND_END" || snapshot.phase === "GAME_END") && (<RoundEndPanel snapshot={snapshot} />)}`
with two separate lines: `{snapshot.phase === "ROUND_END" && <RoundEndPanel snapshot={snapshot} />}`
and `{snapshot.phase === "GAME_END" && <GameEndPanel snapshot={snapshot} />}`. Leave
`PANEL_ALREADY_LISTS_PLAYERS` unchanged — it already contains both phases, and `GameEndPanel` also
lists players (winners + scoreboard), so the baseline roster must stay suppressed there too. Update
this component's own doc comment: it currently says `RoundEndPanel` mounts for both `"ROUND_END"`
and `"GAME_END"` — rewrite that sentence to say `RoundEndPanel` now mounts for `ROUND_END` only and
`GameEndPanel` mounts for `GAME_END`.

Edit `client/src/screens/round/RoundEndPanel.tsx`: this component's own doc comment currently
describes it as covering both `ROUND_END` and `GAME_END`, anticipating a future dedicated winner
screen — rewrite that sentence to say `RoundEndPanel` now covers `ROUND_END` only, and the dedicated
winner/best-of screen it anticipated is `GameEndPanel.tsx`. No behavioral change to this file —
comment only.

Extend `client/src/index.css`: add, after the existing `.scoreboard-entry` rule: `.game-end-panel { margin: 1rem 0; }`;
`.winner-banner { margin: 0 0 1.25rem; padding: 0.875rem 1rem; border: 2px solid #1f6feb; border-radius: 0.5rem; }`
(visually set apart so a tie's multiple winners read as a celebration, not a plain list item);
`.winner-list { margin: 0.5rem 0 0; }`; `.winner-entry { font-weight: 700; font-size: 1.125rem; }`;
`.best-of-night { margin: 1.25rem 0 0; }`; `.best-of-list { margin: 0.5rem 0 0; }`;
`.best-of-entry { margin: 0 0 1rem; }`; `.best-of-meta { font-weight: 600; word-spacing: 0.1em; }`
(matching `.writing-progress`'s "important number" emphasis convention — no letter-spacing on
Hebrew text).

Create `server/test/gameEnd.integration.test.ts` using the real-socket harness from
`fullLoop.integration.test.ts` (`startTestServer`/`connectClient`/`waitFor`, a local `waitForPhase`
helper matching that file's own pattern): host + b + c join a room, seed `room.settings.rounds = 1`
and generous writing/rating seconds via test-only internal seeding, start the game. Host and b
submit distinct captions with c never submitting (clears `MIN_SUBMISSIONS_TO_RATE` with exactly 2
submitters). Reach `RATING` step 0, identify its author via `youAreAuthor`, have the non-author
players rate it with the value 3. Reach step 1 and rate it with the value 1. Wait for `GAME_END` on
all three clients. Assert: `gameEndSnapshot.gameEnd!.winners` has exactly one entry, its `id`
matches whichever author scored 3, and its `score` equals exactly 3; `gameEndSnapshot.gameEnd!.bestOfNight`
has exactly 2 entries (both memes from this one-round game), sorted with the score-3 meme first,
and every entry's `photoUrl`/`caption`/`authorName`/`score` matches the real values submitted and
rated earlier in this same test — proving `winners` and `bestOfNight` both reach the real client
with real content, not just a shape check.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/gameEnd.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
    <automated>grep -c "GameEndPanel" client/src/screens/Round.tsx</automated>
    <fails_when>the command's output is less than 2, meaning GameEndPanel was not both imported and mounted in Round.tsx</fails_when>
  </verify>
  <acceptance_criteria>
    - shared/protocol.ts declares BestOfEntry, GameEndView, and LobbySnapshot.gameEnd
    - server/src/rooms/Room.ts contains bestOfNight, updateBestOfNight, and buildGameEndView
    - enterRoundEnd calls updateBestOfNight immediately after applyRoundScores
    - snapshotFor populates roundEnd only for ROUND_END and gameEnd only for GAME_END
    - client/src/screens/round/GameEndPanel.tsx renders a winner banner, the scoreboard, and the best-of-night list from snapshot.gameEnd
    - client/src/screens/Round.tsx mounts GameEndPanel only for GAME_END and RoundEndPanel only for ROUND_END
    - npm --prefix server run test -- --run test/gameEnd.integration.test.ts exits 0
    - npm --prefix client run build exits 0
  </acceptance_criteria>
  <done>Once the last round ends, every tied top scorer is shown as a winner and the real top memes from the whole game appear on a dedicated best-of-the-night screen — proven end to end over real sockets with real photo/caption/score content, not placeholder data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hardening — ties, the best-of eviction order, and the zero-memes edge case</name>
  <files>server/src/rooms/Room.ts, server/test/bestOfNight.integration.test.ts</files>
  <read_first>
    - `server/src/rooms/Room.ts`'s `updateBestOfNight`/`buildGameEndView` exactly as written by Task 1
    - `server/test/scoring.integration.test.ts` — the multi-round bare-`Room` fake-timer harness this task's own test copies directly (submit exactly two of three players each round, rate with a specific known value, to make every round's score fully controllable)
    - `server/src/config.ts` — `MIN_SUBMISSIONS_TO_RATE` (D-09's skip-round case, relevant to the zero-memes edge case)
  </read_first>
  <behavior>
    - Two players who both finish the game with the same (non-zero) highest score both appear in gameEnd.winners; a third player with a lower score does not
    - Every player finishing with score 0 (every round skipped for too few captions, D-09) still produces a gameEnd.winners array containing every player, never an empty array and never a crash
    - bestOfNight never exceeds length 3, even after more than 3 rounds each producing a rated meme with a strictly higher score than the last
    - When a new candidate's score exactly ties the current lowest (#3) survivor's score, the earlier-inserted entry keeps the #3 slot and the new tied candidate is dropped
    - A game where every round is skipped for too few captions (D-09) reaches GAME_END with bestOfNight exactly empty, never padded or fabricated
  </behavior>
  <action>
Create `server/test/bestOfNight.integration.test.ts` as a bare-`Room` test with
`vi.useFakeTimers()` (construct in `beforeEach`, `room.dispose()` + `vi.useRealTimers()` in
`afterEach`), following `scoring.integration.test.ts`'s harness style and its own documented pitfall
(submit fewer than the full connected roster whenever a test needs a full deadline window to
elapse). Drive multiple rounds directly against a `room.settings.rounds` seeded high enough for
each scenario, each round submitting exactly two of three players' captions and having the third
(non-author, never-submitting) player rate the resulting step(s) with specific, test-chosen values
— the same technique `scoring.integration.test.ts`'s two-round accumulation test already uses,
extended across more rounds. Cover every behavior line above:
  - Tie-for-winner test: engineer two players to finish with an identical non-zero total (by
    choosing each round's rating values) and a third to finish lower; assert
    `room.snapshotFor(anyPlayerId).gameEnd!.winners` contains exactly the two tied players' ids (in
    either order) and the lower-scoring player is absent.
  - All-zero test: seed a room where every round has fewer than `MIN_SUBMISSIONS_TO_RATE`
    submissions (D-09's skip case) for its entire duration, drive it to `GAME_END`, and assert
    `gameEnd!.winners` contains every player in the room, each with `score === 0`.
  - Best-of cap test: drive at least 4 rounds, each contributing exactly one rated meme with a
    strictly increasing score across rounds (e.g. 1, 2, 3, 4), and assert `room.bestOfNight` always
    has length <= 3 after each round and, once all 4 rounds have completed, contains exactly the 3
    highest-scored memes (scores 2, 3, 4) with the score-1 meme evicted.
  - Best-of tie-at-boundary test: drive rounds so a later round's meme score exactly TIES the
    current lowest (#3) survivor's score, and assert the ORIGINAL (earlier-round) survivor is still
    present in `room.bestOfNight` afterward and the new tied candidate was NOT added — proving the
    stable-sort insertion-order tiebreak is real, not incidental.
  - Empty-best-of test: drive a game where every round is skipped for too few captions, reach
    `GAME_END`, and assert `room.bestOfNight` is an empty array.

Modify `server/src/rooms/Room.ts` ONLY if one of these tests exposes a real defect in Task 1's
implementation. Do not restructure otherwise-correct code purely to make a test read more nicely.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/bestOfNight.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - gameEnd.winners includes every player tied for the max score and excludes every player below it, including the all-zero edge case
    - bestOfNight never exceeds length 3 and correctly evicts the lowest-scoring entry as higher-scoring memes arrive
    - a tie at the #3 boundary keeps the earlier-inserted entry, never the newer one
    - a game with no rated memes at all reaches GAME_END with an empty bestOfNight, never a crash
    - npm --prefix server run test -- --run exits 0 with no regressions
  </acceptance_criteria>
  <done>The winner screen and the best-of-night list both behave correctly outside the common case — a tie for first, an all-zero game, more than 3 candidate memes, and a tie at the eviction boundary — backed by tests, not assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression, start the server, and a real-device check across the whole phase</name>
  <files>none — this task starts the already-built server for a live check; no source changes</files>
  <read_first>
    - `server/src/rooms/joinUrl.ts` — `resolveOrigin`'s LAN-IP-friendly origin resolution, already proven for on-device testing in Phase 1 and Phase 3
    - `server/src/config.ts` — `SERVER_PORT` (defaults to 3001)
    - `.planning/ROADMAP.md`'s Phase 4 success criteria and `.planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md`'s D-01 through D-04 — this task's own definition of done
  </read_first>
  <action>
Run the full regression suite first, in this order, and fix any root cause before proceeding to the
server start below: `npm --prefix server run test -- --run`, `npm --prefix server run typecheck`,
`npm --prefix client run typecheck`, `npm --prefix client run test -- --run`, and
`npm --prefix client run build`. All five must exit 0 — never present the human-check below with a
broken verification environment.

Start the server (`npm --prefix server run start`, backgrounded, using the client build just
produced) so a real device on the same WiFi network can reach it — the server already serves the
built client and the Socket.IO endpoint from the same port, and `resolveOrigin` already builds a
correct join URL/QR code from whatever LAN IP the connecting device's own request carries, with no
code change needed (the exact mechanism Phase 3's own real-phone check already used). Confirm the
server is up by checking its `/health` route. Determine and report the machine's LAN IP and the
port so the exact `http://<lan-ip>:<port>` address is ready to hand to a real phone.
  </action>
  <verify>
    <automated>node -e "fetch('http://localhost:3001/health').then((r) => r.json()).then((j) => { if (!j.ok) process.exit(1); }).catch(() => process.exit(1));"</automated>
    <fails_when>non-zero exit, meaning the server is not reachable at /health on the expected port</fails_when>
    <human-check>
      On at least one real phone connected to the SAME WiFi network as this machine (two phones if
      available, matching Phase 3's own two-device check):
      1. Open a browser and navigate to the http://&lt;lan-ip&gt;:&lt;port&gt; address reported by this task.
      2. Create a room, join with a second device if available, and start a game (the smallest rounds preset keeps this check quick).
      3. During WRITING, on at least one device: tap the swap-photo button and confirm the photo changes INSTANTLY with no preview step (D-01); confirm the button disappears once you submit your caption (D-02); if you can catch it, confirm a second tap before submitting is refused or the button is already gone (D-02's one-swap-per-round lock).
      4. After a round closes, confirm the round-results screen shows memes ordered with the HIGHEST point total first, and each entry shows a plain number of points (never the old "X of Y people rated this" phrasing).
      5. Play until the last round ends and confirm a winner screen appears; if you can arrange a tie (rate memes so two players finish with equal scores), confirm BOTH tied players are shown as winners, not just one (D-03).
      6. Confirm a "best of the night" section shows real photos and real captions from memes actually played this game, each with a visible score, and never more than 3 entries (D-04).
      7. Confirm nothing froze, crashed, showed a blank screen, or showed mismatched content between devices at any step.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The full regression suite (server tests, server typecheck, client typecheck, client tests, client build) all exit 0 before the human-check begins
    - The server responds {"ok":true} at /health
    - This task's own output states the exact LAN IP and port for the real-device check
    - The swap button, ranked round results, the winner screen (including a tie if one could be arranged), and the best-of-night screen were all confirmed working on at least one real phone, per the human-check above
  </acceptance_criteria>
  <done>The full Phase 4 feature set — swap, no-repeat photos, ranked round results, winner(s) with ties, and best-of-the-night — passes the complete automated suite and has been confirmed working on a real phone, closing out the phase per this project's own established pattern of never trusting a UI change to a green test suite alone.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created or changed by this plan (new to the codebase, or extending a Plan 04-01 symbol):

**Protocol additions (`shared/protocol.ts`):** `BestOfEntry`, `GameEndView`,
`LobbySnapshot.gameEnd`.

**New Room members (`server/src/rooms/Room.ts`):** `bestOfNight`, `updateBestOfNight`,
`buildGameEndView`.

**Hebrew string keys added (`shared/messages.ts` `HEBREW_UI`):** `winnerHeading`,
`bestOfNightHeading`, `bestOfNightEmpty`.

**Client:** `client/src/screens/round/GameEndPanel.tsx` (new component); `.game-end-panel`,
`.winner-banner`, `.winner-list`, `.winner-entry`, `.best-of-night`, `.best-of-list`,
`.best-of-entry`, `.best-of-meta` CSS classes; `Round.tsx`'s split `ROUND_END`/`GAME_END` mounting.

**New file paths:** `server/test/gameEnd.integration.test.ts`,
`server/test/bestOfNight.integration.test.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Room` -> `gameEnd` snapshot field | Winners/bestOfNight computed entirely server-side from the room's own player/rating state; no client input shapes either list |
| `Room.bestOfNight` running list | In-memory only, mutated exclusively inside `updateBestOfNight()`, called only from the server's own `enterRoundEnd()` timer — no socket handler touches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `Player.score` used to compute `gameEnd.winners` | high | mitigate | `buildGameEndView` reads `Player.score`, which is still written exclusively by `applyRoundScores` (Phase 3's T-03-01 mitigation, unchanged by this plan) — the winner computation inherits that same score-integrity guarantee with no new write path |
| T-04-06 | Tampering | `Room.bestOfNight` | medium | mitigate | `updateBestOfNight()` is a private method called only from `enterRoundEnd()`, itself only reachable from the server's own phase timers — no `CLIENT_EVENTS` member or handler can push an entry onto or reorder this list |
| T-04-07 | Information Disclosure | `BestOfEntry.photoUrl`/`caption`/`authorName` exposed to every player at GAME_END | low | accept | This is MEME-02's own intended reveal — the whole point of "best of the night" is showing everyone's best memes with authorship, mirroring the round-end reveal's existing `authorName` pattern Phase 2 already shipped; no player-identifying data beyond a display name already visible on every roster |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new package — no `package.json`/lockfile appears in `files_modified` |
</threat_model>

<verification>
- `npm --prefix server run test -- --run` exits 0
- `npm --prefix server run typecheck` exits 0
- `npm --prefix client run typecheck` exits 0
- `npm --prefix client run test -- --run` exits 0
- `npm --prefix client run build` exits 0
- `server/test/gameEnd.integration.test.ts` proves a real game's winner(s) and best-of-night reach the client with real content
- `server/test/bestOfNight.integration.test.ts` proves ties, the eviction cap, the eviction boundary tiebreak, and the zero-memes edge case all behave correctly
- The real-device check in Task 3 confirms the swap button, ranked round results, the winner screen, and the best-of-night screen all work on a real phone
</verification>

<success_criteria>
- Round scores keep accumulating into a running total across the whole game, unchanged in mechanism from Phase 3
- Every player tied for the highest score is shown as a winner once the last round ends
- A best-of-the-night screen shows the real top 3 highest-scoring memes from the whole game, tracked incrementally
- The complete Phase 4 feature set has been confirmed working on a real phone
</success_criteria>

<output>
Create `.planning/phases/04-full-round-rating-scoring-completion/04-02-SUMMARY.md` when done
</output>
