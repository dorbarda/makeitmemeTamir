---
phase: 04-full-round-rating-scoring-completion
plan: 01
subsystem: infra
tags: [node, fs, vitest, tamir-photos, photo-pool]

# Dependency graph
requires: []
provides:
  - "20 distinct placeholder Tamir photo files under client/public/tamir-photos/ (D-08)"
  - "server/src/rooms/photoPool.ts: TAMIR_PHOTOS_DIR, listPhotoFiles, loadPhotoManifest, pickPhoto"
  - "server/scripts/generate-placeholder-photos.mjs — reproducible placeholder generator"
affects: [04-02]

# Actuals (#2632)
actuals:
  tokens: 3550
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Small pure module beside Room (no class, no side effects beyond one directory read), following rotation.ts/gameSettings.ts"
    - "Memoized module-level manifest cache so the photo directory is scanned exactly once per server process"

key-files:
  created:
    - client/public/tamir-photos/01.svg through 20.svg
    - server/src/rooms/photoPool.ts
    - server/test/photoPool.test.ts
    - server/scripts/generate-placeholder-photos.mjs
  modified:
    - server/package-lock.json

key-decisions:
  - "Placeholder photos are minimal SVGs (colored square + number) generated by a committed script rather than binary images, so they are tiny, diffable, and trivially regenerable"
  - "pickPhoto treats an unrecorded lastUsedSeq entry as -1 so it always outranks any recorded (non-negative) seq in the LRU fallback, per plan D-06 spec"

patterns-established:
  - "photoPool.ts follows the existing rotation.ts/gameSettings.ts convention: plain exported functions, no class, pure logic separated from Room"

requirements-completed: [ROUND-01, ROUND-02, ROUND-06]

coverage:
  - id: D1
    description: "20 distinct placeholder Tamir photo files exist under client/public/tamir-photos/, loadable as static images, .gitkeep removed"
    requirement: "ROUND-01"
    verification:
      - kind: other
        ref: "shell verify: ls count == 20 && no .gitkeep && grep '<svg'/'<text' match all 20 && md5sum uniqueness check (20 distinct)"
        status: pass
    human_judgment: false
  - id: D2
    description: "loadPhotoManifest()/listPhotoFiles() correctly scan, filter, and sort the photo directory, matching the on-disk placeholder set"
    requirement: "ROUND-02"
    verification:
      - kind: unit
        ref: "server/test/photoPool.test.ts#listPhotoFiles / #loadPhotoManifest"
        status: pass
    human_judgment: false
  - id: D3
    description: "pickPhoto implements the unused-pool-first / least-recently-used-fallback selection rule (D-05/D-06), including tie-breaking and the empty-manifest throw"
    requirement: "ROUND-06"
    verification:
      - kind: unit
        ref: "server/test/photoPool.test.ts#pickPhoto"
        status: pass
    human_judgment: false

duration: 4min
completed: 2026-09-07
status: complete
---

# Phase 4 Plan 1: Placeholder Photo Set + photoPool.ts Summary

**20 SVG placeholder photos plus a pure `photoPool.ts` module (manifest loader + unused-first/LRU-fallback `pickPhoto`) ready for plan 04-02's Room.ts to import.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-09-07T10:55:59Z
- **Completed:** 2026-09-07T10:58:24Z
- **Tasks:** 2/2 completed
- **Files modified:** 24 (22 created + 1 removed + 1 lockfile normalization)

## Accomplishments
- Generated 20 distinct placeholder Tamir photos (`client/public/tamir-photos/01.svg`..`20.svg`), each a colored square with its number, via a committed one-off generator script; removed the now-unneeded `.gitkeep`
- Implemented `server/src/rooms/photoPool.ts` exporting `TAMIR_PHOTOS_DIR`, `listPhotoFiles`, `loadPhotoManifest` (memoized), and `pickPhoto` implementing D-05/D-06's unused-pool-first, least-recently-used-fallback selection algorithm
- Added `server/test/photoPool.test.ts` with 12 unit tests covering extension filtering, sorting, missing-directory safety, the real 20-file manifest, unused-first selection, LRU fallback (including the "-1 default seq" rule and exact-tie ordering), and the empty-manifest throw

## Task Commits

Each task was committed atomically:

1. **Task 1: Twenty placeholder Tamir photos (D-08)** - `1a739d4` (feat)
2. **Task 2: photoPool.ts — manifest loader + pure selection algorithm (D-05, D-06)** - `af5ba2b` (feat)

**Plan metadata:** (this commit, following SUMMARY.md write)

## Files Created/Modified
- `client/public/tamir-photos/01.svg` .. `20.svg` - 20 placeholder photo files (D-08)
- `server/scripts/generate-placeholder-photos.mjs` - one-off generator script, kept committed for reproducibility
- `server/src/rooms/photoPool.ts` - manifest loader + pickPhoto selection algorithm
- `server/test/photoPool.test.ts` - unit tests for all four exports
- `server/package-lock.json` - normalized by `npm install` restoring missing `node_modules` (see Deviations)
- `client/public/tamir-photos/.gitkeep` - removed (no longer needed)

## Decisions Made
- Placeholder photos are generated SVGs, not stock/binary images — matches the plan's explicit spec (colored square + centered number) and keeps the repo diff small and inspectable
- `pickPhoto`'s LRU fallback defaults an unrecorded `lastUsedSeq` entry to `-1`, exactly as the plan specified, verified by a dedicated test asserting the unrecorded entry wins against two recorded (non-negative) entries

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Ran `npm install` to restore missing `node_modules` in server/**
- **Found during:** Task 2 (running the `npm --prefix server run test` / `run typecheck` verify commands)
- **Issue:** `server/node_modules` did not exist in this environment, so `vitest` and `tsc` were both unresolvable (`sh: 1: vitest: not found`), blocking the required verification steps
- **Fix:** Ran `npm install` in `server/` using only the versions already pinned in `package.json` — no new packages added, no version changes
- **Files modified:** `server/package-lock.json` (only stale `"dev": true` flag metadata normalized by the newer npm; no dependency version changed)
- **Verification:** `npm --prefix server run test -- --run test/photoPool.test.ts` → 12/12 passed; `npm --prefix server run typecheck` → clean
- **Committed in:** `af5ba2b` (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to run the plan's own required verification commands. No scope creep — no new dependencies were introduced.

## Issues Encountered

Running the full server test suite (`npm --prefix server run test -- --run`, not scoped to this plan's file) surfaced one pre-existing, unrelated failure: `test/qrJoinUrl.test.ts`'s "returns 200 and HTML for a cold request to /join/4827" fails with 404 because `client/dist` has never been built in this environment. This is out of scope for this plan (photoPool.ts and the placeholder photos never touch the client build or `app.ts`'s static-serving routes) and is logged in `.planning/phases/04-full-round-rating-scoring-completion/deferred-items.md` per the scope-boundary rule rather than fixed here.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `client/public/tamir-photos/` now has 20 real (placeholder) files and `server/src/rooms/photoPool.ts` exports everything plan 04-02 needs (`loadPhotoManifest()` as the default value of `Room.ts`'s `photoManifest` property, plus `pickPhoto` for round-time photo assignment)
- No blockers for 04-02. The one pre-existing unrelated test failure (client build not present) does not affect 04-02's scope either, since it also lives entirely in `server/src/rooms/`.

---
*Phase: 04-full-round-rating-scoring-completion*
*Completed: 2026-09-07*

## Self-Check: PASSED

All claimed files found on disk (client/public/tamir-photos/01.svg, 20.svg, server/src/rooms/photoPool.ts, server/test/photoPool.test.ts, server/scripts/generate-placeholder-photos.mjs), `.gitkeep` confirmed removed, and both task commits (`1a739d4`, `af5ba2b`) confirmed present in git history.
