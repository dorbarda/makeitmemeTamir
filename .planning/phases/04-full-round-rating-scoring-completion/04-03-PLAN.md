---
phase: 04-full-round-rating-scoring-completion
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - client/src/screens/round/WritingPanel.tsx
  - client/src/screens/round/RatingPanel.tsx
  - client/src/screens/round/RoundEndPanel.tsx
  - client/src/index.css
autonomous: true
requirements: [ROUND-01, ROUND-03, ROUND-06, VOTE-01, VOTE-02, VOTE-03, VOTE-05, VOTE-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The writing screen shows the player's actual assigned photo (never a placeholder number) with a working swap button that disables itself after one use or after the caption is submitted (D-07, ROUND-01, ROUND-06)."
    - "The rating screen shows the actual meme photo and the three real Hebrew tier labels — מת מצחוק / חייכתי / אה, בסדר — to every eligible rater (D-04, VOTE-01, VOTE-02)."
    - "The meme's author sees a plain wait message and no rating buttons while their own meme is on screen (VOTE-03, D-09)."
    - "The round results list renders in the server-provided rank order with a visible rank number and point total per meme, revealed row-by-row with a staggered animation (VOTE-06, D-10)."
    - {statement: "Long Hebrew captions in the round results list wrap rather than overflowing the screen.", verification: backstop}
  artifacts:
    - client/src/screens/round/WritingPanel.tsx
    - client/src/screens/round/RatingPanel.tsx
    - client/src/screens/round/RoundEndPanel.tsx
    - client/src/index.css
  key_links:
    - "WritingPanel's swap button emits CLIENT_EVENTS.swapPhoto and waits for the next state/error exactly like handleSubmit's existing round-trip — no optimistic photo swap."
    - "RoundEndPanel renders roundEnd.entries in the exact order Room.buildRoundEndView() (plan 04-02) returned them — no client-side re-sort or re-summation."
---

<objective>
Complete the vertical slice plan 04-02 started on the server: make the writing, rating, and
round-end screens render the real photo, real Hebrew tier names, the author's wait state, and the
server-ranked round results — the client half of the same tracer, now that the server contract it
depends on exists.

Purpose: this is what makes plan 04-02's server work observable to an actual player — without
this plan, the app does not build (see 04-02's objective note) and nothing changed is visible.
Output: three updated round-phase panels and their supporting CSS, all reading only fields the
server snapshot already provides — no client-side derivation of a score, a rank, or a photo
choice.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md
@.planning/phases/04-full-round-rating-scoring-completion/04-UI-SPEC.md
@shared/protocol.ts
@shared/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WritingPanel — real photo + swap button (ROUND-01, ROUND-06, D-07)</name>
  <files>client/src/screens/round/WritingPanel.tsx</files>
  <read_first>
    client/src/screens/round/WritingPanel.tsx (full file — the existing handleSubmit round-trip
    pattern this task's handleSwap must mirror exactly); client/src/screens/Lobby.tsx
    (handleChangeSetting — the same once/error/state cleanup pattern, for a button rather than a
    form); shared/protocol.ts and shared/messages.ts as updated by plan 04-02 (`yourPhotoFile`,
    `canSwapPhoto`, `CLIENT_EVENTS.swapPhoto`, `HEBREW_UI.swapPhotoButton`,
    `HEBREW_UI.photoLoadingPlaceholder`); .planning/phases/04-full-round-rating-scoring-completion/
    04-UI-SPEC.md section 1 ("Photo Assignment & Swap Screen") and section 2 ("Caption Writing
    Screen").
  </read_first>
  <action>
    Remove the `&lt;p&gt;{HEBREW_UI.placeholderContentPrefix} {snapshot.yourPlaceholderId}&lt;/p&gt;`
    line (both symbols no longer exist after plan 04-02). In its place, render the assigned photo:
    when `snapshot.yourPhotoFile` is truthy, an `&lt;img&gt;` with `className="round-photo"`,
    `src={\`/tamir-photos/${snapshot.yourPhotoFile}\`}`, and an empty `alt=""` (the photo is
    decorative relative to the caption task, not informational content needing a text alternative);
    otherwise render `&lt;p&gt;{HEBREW_UI.photoLoadingPlaceholder}&lt;/p&gt;` (the D-06 defensive
    empty-state backstop). Inside the existing `!snapshot.youSubmitted` branch, above the caption
    `&lt;form&gt;`, add a swap button: `&lt;button type="button" className="swap-photo-button"
    disabled={!snapshot.canSwapPhoto} onClick={handleSwap}&gt;{HEBREW_UI.swapPhotoButton}&lt;/button&gt;`.
    Add a `handleSwap` function using the exact same
    once-error/once-state/cleanup/emit-with-no-payload shape `handleSubmit` already uses for
    `submitCaption`, but emitting `CLIENT_EVENTS.swapPhoto` with no payload and setting/clearing a
    dedicated `swapError` state (do not reuse the caption form's `error` state — a swap failure and
    a caption-submit failure are distinct events that must not stomp each other if both are
    mid-flight). Render `{swapError && &lt;p role="alert"&gt;{swapError}&lt;/p&gt;}` beneath the
    swap button. The button's own `disabled` state (from `canSwapPhoto`) is the only affordance for
    "already swapped" — no separate note text, matching the project's existing minimal-copy
    convention (compare the start-game button's `disabled={!snapshot.canStart}` with no adjacent
    explanatory text).
  </action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>tsc exits non-zero, or output contains the string "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - WritingPanel.tsx no longer references `yourPlaceholderId` or `placeholderContentPrefix`
      (grep -q "yourPlaceholderId\|placeholderContentPrefix" client/src/screens/round/WritingPanel.tsx
      exits non-zero).
    - The swap button's `disabled` prop is driven exclusively by `snapshot.canSwapPhoto`, never a
      local `useState` mirror of swap status.
    - `handleSwap` never optimistically updates any local photo state — the displayed photo only
      ever changes when a new snapshot arrives.
  </acceptance_criteria>
  <done>The writing screen shows the player's real assigned photo and a working, correctly
  disabled one-time swap button.</done>
</task>

<task type="auto">
  <name>Task 2: RatingPanel — real photo, real tier names, author wait state (VOTE-01, VOTE-02, VOTE-03, D-04, D-09)</name>
  <files>client/src/screens/round/RatingPanel.tsx</files>
  <read_first>
    client/src/screens/round/RatingPanel.tsx (full file); shared/messages.ts as updated by plan
    04-02 (`HEBREW_UI.ratingTier1/2/3`, `HEBREW_UI.authorWaitState`,
    `HEBREW_UI.photoLoadingPlaceholder` — `yourMemeWaiting` and `ratingTierPlaceholder1/2/3` no
    longer exist); shared/protocol.ts's `RatingStepView.photoFile` (replaces `placeholderId`);
    .planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md D-04 and D-09;
    .planning/phases/04-full-round-rating-scoring-completion/04-UI-SPEC.md section 3
    ("Per-Meme Rating Screen").
  </read_first>
  <action>
    Update `TIER_LABELS` so value 1 maps to `HEBREW_UI.ratingTier1` ("אה, בסדר"), value 2 maps to
    `HEBREW_UI.ratingTier2` ("חייכתי"), and value 3 maps to `HEBREW_UI.ratingTier3` ("מת מצחוק") —
    D-04's mapping is 3 = funniest = מת מצחוק, 1 = meh = אה, בסדר. Remove the
    `&lt;p&gt;{HEBREW_UI.placeholderContentPrefix} {ratingStep.placeholderId}&lt;/p&gt;` line. In its
    place, render the meme's real photo the same way WritingPanel now does: an `&lt;img
    className="round-photo"&gt;` with `src={\`/tamir-photos/${ratingStep.photoFile}\`}` when
    `ratingStep.photoFile` is truthy, else the `HEBREW_UI.photoLoadingPlaceholder` fallback
    paragraph. Replace the `youAreAuthor` branch's `&lt;p&gt;{HEBREW_UI.yourMemeWaiting}&lt;/p&gt;`
    with `&lt;div className="author-wait-state"&gt;&lt;p&gt;{HEBREW_UI.authorWaitState}&lt;/p&gt;&lt;/div&gt;`
    (the caption is already rendered unconditionally above this branch in the existing JSX — do not
    duplicate it inside the wait-state div, per the UI-Spec's own "photo + caption, no buttons"
    description, where the caption is satisfied by the paragraph already above).
  </action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>tsc exits non-zero, or output contains the string "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - RatingPanel.tsx no longer references `placeholderId`, `placeholderContentPrefix`, or
      `yourMemeWaiting` (grep -q "placeholderId\|placeholderContentPrefix\|yourMemeWaiting"
      client/src/screens/round/RatingPanel.tsx exits non-zero).
    - `TIER_LABELS[3]` renders the literal string "מת מצחוק" and `TIER_LABELS[1]` renders "אה,
      בסדר" (source-read check, not a snapshot test — this project has no React component test
      harness).
    - The `youAreAuthor` branch renders the `.author-wait-state` div and no `&lt;button&gt;` element.
  </acceptance_criteria>
  <done>The rating screen shows the real meme photo, the three real Hebrew tier buttons, and a
  plain wait state for the meme's own author with no buttons.</done>
</task>

<task type="auto">
  <name>Task 3: RoundEndPanel + CSS — ranked results, staggered reveal, tie display (VOTE-06, D-10)</name>
  <files>client/src/screens/round/RoundEndPanel.tsx, client/src/index.css</files>
  <read_first>
    client/src/screens/round/RoundEndPanel.tsx (full file, including its own doc comment — this
    task must correct that comment, which currently says ranking/scoring is "explicitly deferred");
    client/src/index.css (the `.round-end-panel`/`.round-end-entry`/`.rating-tier`/`.writing-panel`
    rules this task extends, and the `.player--disconnected`/`.roster-check` minimal-class-naming
    convention); shared/messages.ts's `HEBREW_UI.rankLabel`/`pointsLabel` (added by plan 04-02);
    .planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md D-10;
    .planning/phases/04-full-round-rating-scoring-completion/04-UI-SPEC.md section 4 ("Round
    Results Screen") and its "Animation Timing (D-10)" / "Tie Handling" executor notes.
  </read_first>
  <action>
    In RoundEndPanel.tsx, update the file's own doc comment: it currently states ranking is
    deferred to "Phase 4" and that a placeholder must not sum ratings — both are now done, in
    Room.buildRoundEndView() (plan 04-02), so correct the comment to say the entries arrive
    pre-ranked and pre-scored and this component only renders them in the given order. In the
    `roundEnd.entries.map` body, render each entry as: an `&lt;img className="round-photo
    round-photo--thumb"&gt;` with `src={\`/tamir-photos/${entry.photoFile}\`}`; a line showing
    `{HEBREW_UI.rankLabel} {entry.rank}`; the existing `authorName` and `caption` paragraphs
    (unchanged); a line showing `{entry.score} {HEBREW_UI.pointsLabel}` in place of the current
    `{entry.ratings.length} {HEBREW_UI.ofSeparator} {entry.eligibleAtClose}` line (the raw
    ratings-count-vs-eligible display is no longer the primary content — VOTE-06 wants the total
    score visible, not a raw count). Add `className="round-end-entry round-end-entry--reveal"` to
    each `&lt;li&gt;` (keeping the existing `round-end-entry` class so its overflow-wrap rule still
    applies) and an inline `style={{ animationDelay: \`${index * 150}ms\` }}` — entries already
    arrive in rank order (server-sorted, rank 1 first), so index 0's shorter delay makes the
    top-ranked meme appear first, per D-10's "fastest-to-slowest" framing.

    In index.css, add: `.round-photo { max-width: 100%; height: auto; border-radius: 0.5rem;
    display: block; margin: 0 0 0.75rem; }` (shared by WritingPanel/RatingPanel/RoundEndPanel — a
    single rule, no per-screen duplication); `.round-photo--thumb { max-width: 40%; }` (a smaller
    variant for the list-row thumbnails in round results, layered on top of `.round-photo`'s base
    rule since both classes are applied together); `.author-wait-state { margin: 0.75rem 0;
    font-weight: 600; }`; a `@keyframes round-end-reveal` rule animating from `opacity: 0;
    transform: translateY(8px);` to `opacity: 1; transform: none;`; and
    `.round-end-entry--reveal { animation: round-end-reveal 300ms ease-out both; }`. Also add
    `.swap-photo-button { margin-bottom: 0.75rem; }` (referenced by plan 04-03's own Task 1, which
    runs before this task in the same plan) — the global `button` rule already gives it the 44px
    minimum tap target, this only adds spacing before the caption form beneath it.
  </action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>tsc exits non-zero, or output contains the string "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>vite build (via tsc -b) exits non-zero, or the output contains the string "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - RoundEndPanel.tsx's doc comment no longer says ranking/scoring is deferred to a later phase.
    - Each rendered `&lt;li&gt;` carries both `round-end-entry` and `round-end-entry--reveal` classes
      and a distinct `animationDelay` proportional to its position.
    - index.css defines `.round-photo`, `.round-photo--thumb`, `.author-wait-state`,
      `@keyframes round-end-reveal`, `.round-end-entry--reveal`, and `.swap-photo-button`.
    - `npm --prefix client run build` succeeds — this is the first point in the phase where the
      full client compiles again after plan 04-02's intentional server-only break.
  </acceptance_criteria>
  <done>The round results screen shows a ranked, tie-safe list with real photos, point totals, and
  a staggered reveal animation; the whole client builds again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server snapshot -> client render | These three components render `photoFile`/`score`/`rank` strings/numbers the server already validated and produced (plan 04-02) — no new trust boundary, purely a rendering change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | `&lt;img src={...ratingStep.photoFile}&gt;` etc. | low | accept | `photoFile` values only ever originate from the server's own directory scan (plan 04-01/04-02), never from any client-writable state — there is no path by which a player's browser could substitute an arbitrary `src` value into another player's screen. |
</threat_model>

<verification>
Run all three tasks' automated verify commands in order. Confirm `npm --prefix client run build`
succeeds on Task 3 — this is the plan's own proof that plan 04-02's server-only break is now fully
resolved.
</verification>

<success_criteria>
- WritingPanel, RatingPanel, and RoundEndPanel all render real photos, real Hebrew tier names, the
  author wait state, and ranked round-end results — no placeholder-era field or string remains
  referenced anywhere in client/src.
- `npm --prefix client run build` and `npm --prefix client run typecheck` both succeed.
</success_criteria>

<output>
Create `.planning/phases/04-full-round-rating-scoring-completion/04-03-SUMMARY.md` when done.
</output>

## Artifacts This Plan Produces

- `client/src/screens/round/WritingPanel.tsx`: real photo `&lt;img&gt;`, `handleSwap` function,
  `swapError` state, swap `&lt;button className="swap-photo-button"&gt;`.
- `client/src/screens/round/RatingPanel.tsx`: updated `TIER_LABELS`, real photo `&lt;img&gt;`,
  `.author-wait-state` div.
- `client/src/screens/round/RoundEndPanel.tsx`: rank/score/photo rendering, staggered reveal.
- `client/src/index.css`: `.round-photo`, `.round-photo--thumb`, `.author-wait-state`,
  `@keyframes round-end-reveal`, `.round-end-entry--reveal`, `.swap-photo-button`.
