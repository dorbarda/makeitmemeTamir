---
phase: 04-full-round-rating-scoring-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/rooms/photos.ts
  - server/src/rooms/Room.ts
  - shared/protocol.ts
  - shared/messages.ts
  - server/src/socket/handlers.ts
  - client/src/screens/round/WritingPanel.tsx
  - client/src/screens/round/RoundEndPanel.tsx
  - client/src/index.css
  - server/test/photoSwap.integration.test.ts
  - server/test/photos.test.ts
  - server/test/noRepeatPhotos.integration.test.ts
  - server/test/ratingPrivacy.integration.test.ts
autonomous: true
requirements: [ROUND-02, ROUND-06, VOTE-03, VOTE-05, VOTE-06]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No player is assigned a photo they've already been shown earlier in the SAME game, until they have seen every photo currently in the pool at least once — at which point their own seen-set resets and repeats are allowed again rather than the round stalling (ROUND-02, Claude's Discretion)."
    - "During WRITING, before submitting a caption, a player can tap a swap button and their assigned photo is replaced instantly with a different photo — no preview, no confirm step (ROUND-06, D-01)."
    - "A player who has already submitted this round's caption, or who has already used this round's one swap, can no longer swap — the button's own availability and the server's refusal both enforce this (ROUND-06, D-02)."
    - "A meme's author still cannot rate their own meme and sees the existing waiting state while it is on screen (VOTE-03, verified not rebuilt)."
    - "While a meme's rating step is open, no other player's individual rating value is present anywhere in any player's snapshot — only a live rated/eligible count — and the meme's real total score is revealed only once that step closes (VOTE-05, verified with a new structural test)."
    - "Once a round's memes have all been rated, the round-results screen ranks them by their real total score, highest first, with author identity still visible (VOTE-06)."
  artifacts:
    - path: "server/src/rooms/photos.ts"
      provides: "assignPhotosFromEligiblePools and drawOnePhoto — the per-player eligible-pool draw and single-photo picker ROUND-02/ROUND-06 build on, alongside the unchanged assignPhotos"
      exports: ["assignPhotosFromEligiblePools", "drawOnePhoto"]
    - path: "server/src/rooms/Room.ts"
      provides: "photosSeenByPlayer, swapUsed, eligiblePoolFor, markPhotoSeen, drawPhotosForRound, and swapPhoto"
      contains: "swapPhoto"
    - path: "shared/protocol.ts"
      provides: "CLIENT_EVENTS.swapPhoto, ErrorCode.SWAP_ALREADY_USED, LobbySnapshot.youCanSwapPhoto, and RoundEndEntry.score"
      contains: "youCanSwapPhoto"
    - path: "client/src/screens/round/WritingPanel.tsx"
      provides: "the swap button, wired to CLIENT_EVENTS.swapPhoto, visible only when snapshot.youCanSwapPhoto is true"
    - path: "client/src/screens/round/RoundEndPanel.tsx"
      provides: "entries ranked by entry.score descending, replacing the rater-count placeholder"
    - path: "server/test/ratingPrivacy.integration.test.ts"
      provides: "the structural proof that VOTE-05 holds (no individual rating leaks while a step is open, the real total is revealed only once it closes)"
  key_links:
    - from: "server/src/rooms/Room.ts"
      to: "server/src/rooms/photos.ts"
      via: "enterWriting, photoUrlFor, and swapPhoto all route their draws through the new drawPhotosForRound helper, which calls assignPhotosFromEligiblePools"
      pattern: "drawPhotosForRound\\("
    - from: "server/src/socket/handlers.ts"
      to: "server/src/rooms/Room.ts"
      via: "the new swap-photo handler delegates to room.swapPhoto(data.playerId) then room.broadcast(io) on success"
      pattern: "swapPhoto\\("
    - from: "client/src/screens/round/WritingPanel.tsx"
      to: "shared/protocol.ts"
      via: "the swap button emits CLIENT_EVENTS.swapPhoto and gates its own visibility on snapshot.youCanSwapPhoto"
      pattern: "youCanSwapPhoto"
    - from: "client/src/screens/round/RoundEndPanel.tsx"
      to: "server/src/rooms/Room.ts"
      via: "rankedEntries sorts entry.score, a value buildRoundEndView already summed server-side — the client never computes it"
      pattern: "entry.score"
  prohibitions:
    - statement: "MUST NOT let swapPhoto succeed for a player who has already submitted this round's caption — a locked-in caption locks in its photo too (D-02)"
      requirement_id: ROUND-06
      category: correctness
      status: resolved
      verification: test
    - statement: "MUST NOT expose any individual player's rating value to another player anywhere in the wire snapshot while that meme's rating step is still open — only the aggregate ratedCount/eligibleCount and, after close, the aggregate score"
      requirement_id: VOTE-05
      category: integrity
      status: resolved
      verification: test
    - statement: "MUST NOT compute or sort a meme's score on the client — buildRoundEndView on the server must remain the sole place a meme's score is summed"
      requirement_id: VOTE-06
      category: integrity
      status: resolved
      verification: review
---

<objective>
Complete the round loop to full spec: no photo repeats across the WHOLE game (ROUND-02), a one-time
instant photo swap that locks the moment a caption is submitted (ROUND-06, D-01, D-02),
confirmation that a meme's author still cannot rate their own meme (VOTE-03, verify not rebuild),
a structural proof that individual ratings stay hidden until a step closes (VOTE-05, verify not
rebuild), and a round-results screen that finally ranks memes by their REAL point total instead of
the current rater-count placeholder (VOTE-06). Extends Phase 3's `photoAssignments` /
`applyRoundScores` / `buildRoundEndView` directly — nothing here is rebuilt from scratch.

Purpose: this is the round loop's last mile before the game-ending screens (Plan 04-02) can be
built on top of a trustworthy per-round score. VOTE-06's real score is exactly what Plan 04-02's
winner and best-of-the-night screens will read.

Output: a swap button that works end to end, per-player photo history that never repeats within a
game (except by graceful, documented design once a player has seen the whole pool), and a
round-results screen that ranks that round's real memes by their real points.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md
@.planning/phases/02-server-authoritative-round-engine/02-PATTERNS.md

@shared/protocol.ts
@shared/messages.ts
@server/src/config.ts
@server/src/rooms/Room.ts
@server/src/rooms/photos.ts
@server/src/rooms/rotation.ts
@server/src/socket/handlers.ts
@client/src/screens/round/WritingPanel.tsx
@client/src/screens/round/RatingPanel.tsx
@client/src/screens/round/RoundEndPanel.tsx
@client/src/screens/Round.tsx
@client/src/index.css
@server/test/setup.ts
@server/test/ratingStep.integration.test.ts
@server/test/captionPrivacy.integration.test.ts
@server/test/scoring.integration.test.ts
@server/test/photos.test.ts

<interfaces>
Existing contracts this plan extends — do not re-derive them.

From `shared/protocol.ts` (today, before this plan):
```
export type ErrorCode = ... | "CANNOT_RATE_OWN" | "RATING_OUT_OF_RANGE";
export const CLIENT_EVENTS = { ...; submitRating: "submit-rating", ... } as const;
export type RoundEndEntry = { authorId: string; authorName: string; caption: string; ratings: RatingValue[]; eligibleAtClose: number; };
export type LobbySnapshot = { ...; youSubmitted: boolean; yourPhotoUrl: string | null; ratingStep: RatingStepView | null; roundEnd: RoundEndView | null; ... };
```
This plan adds `"SWAP_ALREADY_USED"` to `ErrorCode`, `swapPhoto: "swap-photo"` to `CLIENT_EVENTS`,
`score: number` to `RoundEndEntry`, and `youCanSwapPhoto: boolean` to `LobbySnapshot`. No existing
field is renamed or removed.

From `server/src/rooms/photos.ts` (today):
`export const PHOTO_FILENAMES: string[]`, `export function photoUrl(filename: string): string`,
`export function assignPhotos(playerIds: string[], pool: string[] = PHOTO_FILENAMES): Map<string, string>`
(a shared-pool, same-round distinct-where-possible draw), and a private, unexported `shuffle<T>`
helper. This plan adds two new exports beside these and leaves `assignPhotos`/`photoUrl`/
`PHOTO_FILENAMES` completely unchanged — `server/test/photos.test.ts`'s existing coverage of
`assignPhotos` must keep passing untouched.

From `server/src/rooms/Room.ts` (today): `photoAssignments = new Map<string, string>()` (rebuilt
every `enterWriting()` via `assignPhotos([...this.players.keys()])`), `private photoUrlFor(playerId)`
(reads `photoAssignments`, lazily draws via `assignPhotos([playerId])` for a late joiner),
`private applyRoundScores()` (the sole `Player.score` mutation site, called first inside
`enterRoundEnd()`), `private buildRoundEndView()` (maps `rotation` to `RoundEndEntry[]`, currently
carrying raw `ratings: RatingValue[]` and `eligibleAtClose` but NOT a reduced score — its own doc
comment currently says summing to a score is a Phase 4 decision this engine "must not pre-empt";
this plan is that decision), `snapshotFor(playerId)` (the WRITING branch populates
`progress`/`youSubmitted`/`yourPhotoUrl`). `submissions`, `rotation`, `ratings`, `eligibleAtClose`,
and `photoAssignments` are all plain PUBLIC fields on `Room` (not `private`) — this plan's two new
fields (`photosSeenByPlayer`, `swapUsed`) follow that exact same public-field convention so
`server/test/noRepeatPhotos.integration.test.ts` (Task 2) can read them directly.

From `server/src/socket/handlers.ts` (today): every mutating handler follows one shape —
check `data.playerId`/`data.roomCode`, `emitError` with `NOT_IN_ROOM` if missing; look up the room,
`emitError` with `ROOM_NOT_FOUND` if absent; delegate to a `Room` method that returns a typed
`{ ok: true; ... } | { ok: false; error: ... }` outcome, `emitError` on `!result.ok`, otherwise
`room.broadcast(io)`. The `start-game` handler is the exact no-payload analog for this plan's new
`swap-photo` handler (no fields to destructure from the emitted event).

From `client/src/screens/round/WritingPanel.tsx` (today): `handleSubmit` is the established
once-listener/cleanup pattern (`socket.once(SERVER_EVENTS.error, onError)` /
`socket.once(SERVER_EVENTS.state, onState)`, both calling a shared `cleanup()`) every new mutating
action in this codebase copies — no optimistic state, the UI only changes once the next snapshot
says so.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end photo integrity — no-repeat tracking, one-time swap, real ranked round results</name>
  <reversibility rating="reversible">D-01/D-02 are interaction-flow choices (instant vs. preview, lock timing) behind Room's existing typed-outcome pattern, and VOTE-06's ranking is a client-side sort of an already-server-computed value — none require a different architecture to change later.</reversibility>
  <precondition>client/public/tamir-photos already contains the 5 real photo files PHOTO_FILENAMES enumerates at server start (git-tracked, confirmed working since Phase 3).</precondition>
  <files>server/src/rooms/photos.ts, server/src/rooms/Room.ts, shared/protocol.ts, shared/messages.ts, server/src/socket/handlers.ts, client/src/screens/round/WritingPanel.tsx, client/src/screens/round/RoundEndPanel.tsx, client/src/index.css, server/test/photoSwap.integration.test.ts</files>
  <read_first>
    - `server/src/rooms/photos.ts` — the exact current `assignPhotos`/`photoUrl`/private `shuffle` implementations this task extends beside, not inside
    - `server/src/rooms/Room.ts` — `enterWriting`, `photoUrlFor`, `applyRoundScores`, `buildRoundEndView`, `snapshotFor`'s WRITING branch, and the existing `SubmitRatingOutcome`/`SubmitCaptionOutcome` typed-outcome shapes this task's `SwapPhotoOutcome` copies
    - `shared/protocol.ts` — the current `ErrorCode`, `CLIENT_EVENTS`, `RoundEndEntry`, `LobbySnapshot` shapes listed in this plan's `<interfaces>` block
    - `shared/messages.ts` — `HEBREW_ERRORS`'s existing entry style (e.g. `ALREADY_SUBMITTED: "כבר שלחת כיתוב לסיבוב הזה"`) and `HEBREW_UI`'s `// plan 0X-0Y — ...` grouping convention
    - `server/src/socket/handlers.ts` — the `startGame` handler (the no-payload analog) and the `submitRating` handler (the "pass identity from socket.data only" rule)
    - `client/src/screens/round/WritingPanel.tsx` — the exact current `handleSubmit` once-listener/cleanup pattern this task's `handleSwap` copies
    - `client/src/screens/round/RoundEndPanel.tsx` — the current unranked entries list and rater-count placeholder line this task replaces
    - `client/src/index.css` — `.roster-check`, `.round-end-entry`, `.writing-progress` — the rules this task's new CSS is inserted beside
    - `server/test/captionPrivacy.integration.test.ts` and `server/test/ratingStep.integration.test.ts` — the real-socket, test-only-internal-seeding harness this task's new test file follows
    - `.planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md` — D-01, D-02, and the no-repeat-photos/round-results Claude's Discretion sections
  </read_first>
  <action>
Extend `server/src/rooms/photos.ts` (a pure module, no class — same shape as `rotation.ts`/
`gameSettings.ts`) with two new exported functions, added after the existing `assignPhotos`:
  - `drawOnePhoto(pool: string[]): string` — returns `pool[Math.floor(Math.random() * pool.length)]`,
    a single uniform random pick, no shuffle needed. Every caller in `Room.ts` guarantees a
    non-empty `pool`.
  - `assignPhotosFromEligiblePools(eligiblePoolsByPlayer: ReadonlyMap<string, string[]>): Map<string, string>`
    — ROUND-02's per-player draw. For each `[playerId, eligiblePool]` entry, in the map's own
    iteration order, shuffle that player's own `eligiblePool` with the existing private `shuffle`
    helper, then take the first filename in the shuffled result not already present in a local
    `usedThisRound` Set (falling back to the shuffled result's own first entry if every one of this
    player's eligible photos was already claimed by an earlier player in this same call — the
    accepted degradation once players outnumber the eligible pool, never a gap, never a crash), add
    the chosen filename to `usedThisRound`, and record `playerId -> filename` in the returned map.
    This generalizes `assignPhotos`'s existing same-round distinct-where-possible intent to
    per-player eligible pools instead of one shared pool. Leave `assignPhotos` itself completely
    unchanged.

Extend `server/src/rooms/Room.ts`:
  - Change the `photos.js` import line to `import { PHOTO_FILENAMES, assignPhotosFromEligiblePools, drawOnePhoto, photoUrl } from "./photos.js";`
    — `assignPhotos` is no longer called from this file (it stays exported and unit-tested directly
    in `photos.test.ts`), so it is dropped from this import to avoid an unused import.
  - Add two new PUBLIC fields beside the existing `photoAssignments` field (not `private` — matching
    `photoAssignments`/`submissions`/`rotation`/`ratings`/`eligibleAtClose`'s existing public-field
    convention): `photosSeenByPlayer = new Map<string, Set<string>>();` (ROUND-02 — every photo
    filename a player has ever been shown this game, across every round; never cleared at
    `enterWriting()`) and `swapUsed = new Set<string>();` (ROUND-06/D-02 — the playerIds who have
    already used this round's one swap; cleared every `enterWriting()`, exactly like `submissions`).
  - Add `private eligiblePoolFor(playerId: string, pool: string[] = PHOTO_FILENAMES): string[]` —
    reads `this.photosSeenByPlayer.get(playerId)`; if absent or empty, returns `pool` unchanged;
    otherwise filters `pool` to filenames not in that player's seen set; if the filtered result is
    empty (the player has now seen every photo currently in `pool`), calls `.clear()` on that
    player's own seen `Set` (CONTEXT.md's Claude's Discretion: once a player has exhausted the whole
    pool, reset their tracking and allow repeats again rather than crashing or stalling the round)
    and returns `pool` unchanged.
  - Add `private markPhotoSeen(playerId: string, filename: string): void` — gets or creates that
    player's `Set` inside `photosSeenByPlayer` (creating a new empty `Set` and storing it if absent)
    and adds `filename` to it.
  - Add `private drawPhotosForRound(playerIds: string[]): Map<string, string>` — builds a
    `Map<string, string[]>` of `playerId -> this.eligiblePoolFor(playerId)` for every id in
    `playerIds`, passes that map to `assignPhotosFromEligiblePools`, then calls
    `this.markPhotoSeen(playerId, filename)` for every resulting pair before returning the
    assignment map.
  - In `enterWriting()`, add `this.swapUsed.clear();` beside the existing `this.eligibleAtClose.clear();`
    line, and replace `this.photoAssignments = assignPhotos([...this.players.keys()]);` with
    `this.photoAssignments = this.drawPhotosForRound([...this.players.keys()]);`.
  - In `photoUrlFor(playerId)`, replace the lazy-fallback line
    `filename = assignPhotos([playerId]).get(playerId)!;` with
    `filename = this.drawPhotosForRound([playerId]).get(playerId)!;` so a late joiner honors the
    same seen-photo tracking as a normal round draw.
  - Add a new exported type `SwapPhotoOutcome`, placed beside the existing `SubmitRatingOutcome`
    type, shaped identically to the other outcome types: `{ ok: true; photoUrl: string } | { ok: false; error: "WRONG_PHASE" | "ALREADY_SUBMITTED" | "SWAP_ALREADY_USED" }`.
  - Add `swapPhoto(playerId: string): SwapPhotoOutcome` as a new public method (ROUND-06, D-01, D-02):
    return `{ ok: false, error: "WRONG_PHASE" }` if `this.phase !== "WRITING"`; return
    `{ ok: false, error: "ALREADY_SUBMITTED" }` if `this.submissions.has(playerId)` (D-02 — a
    locked-in caption locks in its photo too); return `{ ok: false, error: "SWAP_ALREADY_USED" }` if
    `this.swapUsed.has(playerId)`. Otherwise: read
    `const currentFilename = this.photoAssignments.get(playerId);`, compute
    `const eligible = this.eligiblePoolFor(playerId);`, compute
    `const withoutCurrent = eligible.filter((filename) => filename !== currentFilename);` (excluding
    the player's own current photo so a swap is never a no-op), pick
    `const pool = withoutCurrent.length > 0 ? withoutCurrent : eligible;` (the fallback covers the
    degenerate case where the current photo is the player's only eligible option), draw
    `const filename = drawOnePhoto(pool);`, then run
    `this.photoAssignments.set(playerId, filename); this.markPhotoSeen(playerId, filename); this.swapUsed.add(playerId);`
    and return `{ ok: true, photoUrl: photoUrl(filename) }` (D-01 — instant replace, no preview: the
    new photo is simply what the very next snapshot carries).
  - In `snapshotFor`, add a new field immediately after the existing
    `youSubmitted: inWriting && this.submissions.has(playerId),` line:
    `youCanSwapPhoto: inWriting && !this.submissions.has(playerId) && !this.swapUsed.has(playerId),`
    (ROUND-06/D-02 — true only during WRITING, before this player has submitted or already used this
    round's one swap).
  - In `buildRoundEndView()`, compute each step's rating values once
    (`const values = stepRatings ? [...stepRatings.values()] : [];`) and reuse them for both the
    existing `ratings: values` field and a new `score: values.reduce((sum, value) => sum + value, 0)`
    field (VOTE-06 — the server-computed total). Update this method's own doc comment: it currently
    says it deliberately does not reduce ratings to a score because that summing decision belongs to
    a future phase — rewrite that paragraph to say VOTE-06 has now made that decision (sum, highest
    first) and this method computes it once here, so no other file ever re-derives a meme's score
    from its raw ratings array.

Extend `shared/protocol.ts`:
  - Add `"SWAP_ALREADY_USED"` to the `ErrorCode` union, immediately after `"RATING_OUT_OF_RANGE"`.
  - Add `swapPhoto: "swap-photo", // {}  — host-blind, no payload; ROUND-06 (added in plan 04-01)` to
    `CLIENT_EVENTS`, after the existing `submitRating` entry.
  - Add `youCanSwapPhoto: boolean;` to `LobbySnapshot`, immediately after the existing
    `youSubmitted: boolean;` line, with a comment noting it is ROUND-06/D-01/D-02's swap
    availability, true only during WRITING before submission or the round's one swap is used.
  - Add `score: number;` to `RoundEndEntry`, immediately after the existing
    `eligibleAtClose: number;` line, with a comment noting it is VOTE-06's server-computed sum of
    the ratings array, added once inside buildRoundEndView and never re-derived client-side.

Extend `shared/messages.ts`:
  - Add `SWAP_ALREADY_USED: "כבר החלפת תמונה בסיבוב הזה",` to `HEBREW_ERRORS`, after the existing
    `RATING_OUT_OF_RANGE` entry — the same phrasing pattern `ALREADY_SUBMITTED` already uses.
  - Add a new comment-grouped section to `HEBREW_UI`, after the existing `roundEndTooFewCaptions`
    entry, with two keys: `swapPhotoButton: "החלפת תמונה"` and `roundResultsPointsSuffix: "נקודות"`.

Extend `server/src/socket/handlers.ts`: add a new handler for `CLIENT_EVENTS.swapPhoto`, placed
immediately after the existing `submitRating` handler and before the `rejoin` handler, in the exact
same shape as every other mutating handler in this file: check `data.playerId`/`data.roomCode` and
`emitError` with `NOT_IN_ROOM` if either is missing; look up the room via
`roomManager.findRoom(data.roomCode)` and `emitError` with `ROOM_NOT_FOUND` if absent; call
`const result = room.swapPhoto(data.playerId);`; on `!result.ok`,
`emitError(socket, { code: result.error, messageHe: HEBREW_ERRORS[result.error] });` and return; on
success, `room.broadcast(io);`. There is no payload to destructure — the callback takes no
arguments, matching the `startGame` handler's own no-payload shape.

Extend `client/src/screens/round/WritingPanel.tsx`:
  - Add a `handleSwap()` function using the exact same once-listener/cleanup shape as this
    component's existing `handleSubmit` (reuse the same `error`/`setError` state already declared —
    do not add a second error state): `setError(undefined)`, get the socket, register one-shot
    `SERVER_EVENTS.error`/`SERVER_EVENTS.state` listeners that both call a shared `cleanup()`, then
    `socket.emit(CLIENT_EVENTS.swapPhoto, {});` — no optimistic mutation; the swap only becomes
    visible once the next snapshot's `yourPhotoUrl` changes.
  - In the JSX, immediately after the existing `{snapshot.yourPhotoUrl && (<img ... />)}` block, add
    a button that renders only `{snapshot.youCanSwapPhoto && (...)}`: a `type="button"` element with
    `className="swap-photo-button"`, `onClick={handleSwap}`, and text `{HEBREW_UI.swapPhotoButton}`.
    This button naturally disappears once `youCanSwapPhoto` goes false (after submitting or after
    the one swap is used), giving D-02's lock a visible client-side signal on top of the server's
    own refusal.

Extend `client/src/screens/round/RoundEndPanel.tsx` (VOTE-06):
  - Add, immediately before the `return`, `const rankedEntries = [...roundEnd.entries].sort((a, b) => b.score - a.score);`
    — sorts what the server already summed; never computes a score itself.
  - Change the `roundEnd.entries.length === 0` check and the subsequent `.map(...)` call to both use
    `rankedEntries` instead of `roundEnd.entries`.
  - Replace the per-entry line `<p>{entry.ratings.length} {HEBREW_UI.ofSeparator} {entry.eligibleAtClose}</p>`
    with `<p className="round-end-score">{entry.score} {HEBREW_UI.roundResultsPointsSuffix}</p>` — the
    real point total, replacing the rater-count placeholder.
  - Update this component's own top-of-file doc comment: it currently describes ranking memes by
    total points as future Phase 4 work — rewrite that sentence to say this component now does
    exactly that, ranking entries by `entry.score` descending.

Extend `client/src/index.css`: add a `.swap-photo-button` rule immediately after the existing
`.roster-check` rule — an outlined secondary button (`background: transparent; border: 1px solid currentColor; color: inherit; margin: 0 0 0.75rem;`)
so it reads as visually secondary to the primary filled "send caption" button and is never tapped by
mistake, while still inheriting the global button rule's 44px tap-target floor. Add a
`.round-end-score` rule immediately after the existing `.round-end-entry` rule —
`font-weight: 600; word-spacing: 0.1em;`, matching `.writing-progress`'s existing "important number"
emphasis style (no letter-spacing on Hebrew/digit text, per project convention).

Create `server/test/photoSwap.integration.test.ts` — real sockets, real timers (same style as
`captionPrivacy.integration.test.ts`/`ratingStep.integration.test.ts`'s real-transport describe
block): host + b + c join a room; seed `room.settings.writingSeconds` generously (e.g. 5) via
test-only internal seeding on the room fetched from `server.roomManager.findRoom(roomCode)` before
`start-game` (the protocol path rejecting non-preset values is already proven in plan 02-02); start
the game. Assert: (1) emitting `CLIENT_EVENTS.swapPhoto` from host and awaiting the next `state`
event yields a `yourPhotoUrl` that starts with `/tamir-photos/`, names a filename that is a member
of the real `PHOTO_FILENAMES` (imported directly from `../src/rooms/photos.js`), and is DIFFERENT
from host's pre-swap `yourPhotoUrl`; (2) that same resulting snapshot's `youCanSwapPhoto` is now
`false`; (3) emitting `swap-photo` again from host yields a `SERVER_EVENTS.error` with
`code === "SWAP_ALREADY_USED"`; (4) host then submits a caption, b submits a caption, c never
submits (clears `MIN_SUBMISSIONS_TO_RATE` with exactly 2 submitters, the established pattern); (5)
once the round reaches `RATING` step 0, identify its author via `youAreAuthor`, have the eligible
non-author players rate it with the value 3; (6) once step 1 opens, rate it with the DIFFERENT value
1; (7) once the round reaches `ROUND_END`, sort the resulting `roundEnd.entries` by `score`
descending and assert the step-0 author (score 3) comes BEFORE the step-1 author (score 1) —
proving VOTE-06's ranking reflects real, distinguishable scores, not incidental ordering; (8)
separately, have c (who never submitted a caption and therefore has no photo left to swap once
`WRITING` has closed) emit `swap-photo` and assert the resulting error is `"WRONG_PHASE"` — proving
the swap window is bounded to `WRITING`.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/photoSwap.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
    <automated>grep -c "swapPhotoButton" client/src/screens/round/WritingPanel.tsx</automated>
    <fails_when>the command's output is not exactly 1, meaning the swap button was not wired into WritingPanel.tsx</fails_when>
  </verify>
  <acceptance_criteria>
    - server/src/rooms/photos.ts exports drawOnePhoto and assignPhotosFromEligiblePools alongside the unchanged assignPhotos
    - server/src/rooms/Room.ts contains photosSeenByPlayer, swapUsed, eligiblePoolFor, markPhotoSeen, drawPhotosForRound, and swapPhoto
    - shared/protocol.ts declares SWAP_ALREADY_USED on ErrorCode, swapPhoto on CLIENT_EVENTS, youCanSwapPhoto on LobbySnapshot, and score on RoundEndEntry
    - client/src/screens/round/WritingPanel.tsx renders the swap button only when snapshot.youCanSwapPhoto is true
    - client/src/screens/round/RoundEndPanel.tsx ranks entries by entry.score descending
    - npm --prefix server run test -- --run test/photoSwap.integration.test.ts exits 0
    - npm --prefix client run build exits 0
  </acceptance_criteria>
  <done>A player can swap their photo instantly during WRITING (once, before submitting), and once a round closes, its memes appear ranked by their real server-computed point total — proven end to end over real sockets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hardening — no-repeat photos across more rounds than photos, and swap edge cases</name>
  <files>server/src/rooms/photos.ts, server/src/rooms/Room.ts, server/test/photos.test.ts, server/test/noRepeatPhotos.integration.test.ts</files>
  <read_first>
    - `server/src/rooms/photos.ts` and `Room.ts`'s `eligiblePoolFor`/`markPhotoSeen`/`drawPhotosForRound`/`swapPhoto` exactly as written by Task 1
    - `server/test/scoring.integration.test.ts` and `server/test/neverStalls.integration.test.ts` — the bare-`Room` fake-timer harness and the documented pitfall that submitting from EVERY connected player inside a fake-timer test triggers the writing-phase early-finish collapse — submit fewer than the full roster whenever a test needs a full deadline window to elapse
    - `server/src/config.ts` — `MIN_SUBMISSIONS_TO_RATE`
  </read_first>
  <behavior>
    - assignPhotosFromEligiblePools, given two players whose eligible pools do not overlap at all, assigns each player a filename from their OWN eligible pool only
    - assignPhotosFromEligiblePools, given two players who share an identical single-filename eligible pool, still assigns a real pool member to both (the accepted degradation — a repeat within one round is preferable to a gap)
    - drawOnePhoto(pool), called repeatedly against a multi-item pool, always returns a member of that pool
    - A single player run through more consecutive rounds than PHOTO_FILENAMES.length never repeats a photo until every real photo has been shown to them once, and repeats become possible again afterward (the graceful reset) rather than the room throwing or stalling
    - swapPhoto draws its replacement from the player's own not-yet-seen pool, excluding photos already recorded in their photosSeenByPlayer set wherever the pool allows it
    - swapPhoto refuses SWAP_ALREADY_USED on a second attempt in the same round, and photoAssignments is unchanged by the refused attempt
    - swapPhoto refuses ALREADY_SUBMITTED once submitCaption has succeeded for that player, even if their one swap was never used
  </behavior>
  <action>
Extend `server/test/photos.test.ts` with a new `describe("assignPhotosFromEligiblePools", ...)` block
and a new `describe("drawOnePhoto", ...)` block, in the same pure-unit style as the existing
`assignPhotos`/`photoUrl` blocks (small local injected pools, deterministic, independent of the real
filesystem). Cover every `assignPhotosFromEligiblePools`/`drawOnePhoto` behavior line above.

Create `server/test/noRepeatPhotos.integration.test.ts` as a bare-`Room` test with
`vi.useFakeTimers()` (construct in `beforeEach`, `room.dispose()` + `vi.useRealTimers()` in
`afterEach`), following `neverStalls.integration.test.ts`'s harness style and its documented pitfall
(never submit from every connected player in the same tick unless the test specifically wants the
early-finish collapse):
  - Multi-round exhaustion test: start a room with exactly 2 real players. Read the real, non-injected
    `PHOTO_FILENAMES` array's own `.length` at the top of the test (imported directly from
    `../src/rooms/photos.js` — never hardcode the count). Run `PHOTO_FILENAMES.length + 2` consecutive
    rounds, each time submitting only ONE player's caption (never the full roster — the documented
    pitfall) and letting the writing deadline expire naturally so the round advances (a round that
    receives only one submission skips rating per D-09 and goes straight to the next round's
    `enterWriting()`, which is exactly what this test needs). After each round, read that player's
    current `photoAssignments` entry directly off the room and assert it was NOT already present in
    their `photosSeenByPlayer` set as it stood at the START of that round (proving no repeat before
    exhaustion). Once every real photo has been assigned to this player at least once, assert a LATER
    round DOES repeat a photo the player has already seen (the graceful reset in action), rather than
    the test ever timing out or the room throwing.
  - Swap-avoids-seen test: start a room with 3 players, advance into `WRITING`, read the target
    player's `photosSeenByPlayer` set immediately before calling `room.swapPhoto(playerId)`, then
    assert the returned `photoUrl`'s filename is not present in that pre-swap set (it excludes both
    the player's current photo and, wherever the real 5-photo pool allows it, every other
    previously-seen photo).
  - Swap-refusal tests: call `room.swapPhoto(playerId)` twice in the same round for the same player
    and assert the second call returns `{ ok: false, error: "SWAP_ALREADY_USED" }` with
    `photoAssignments.get(playerId)` unchanged from the first call's result; separately, in a fresh
    room, have a player `submitCaption(...)` successfully, then call `room.swapPhoto(playerId)` for
    that same player and assert `{ ok: false, error: "ALREADY_SUBMITTED" }`.

Modify `server/src/rooms/photos.ts` or `Room.ts` ONLY if one of these tests exposes a real defect in
Task 1's implementation. Do not restructure otherwise-correct code purely to make a test read more
nicely.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/photos.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run test -- --run test/noRepeatPhotos.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - server/test/photos.test.ts proves assignPhotosFromEligiblePools never assigns a filename outside a player's own eligible pool
    - server/test/noRepeatPhotos.integration.test.ts proves a single player run through more rounds than PHOTO_FILENAMES.length sees no repeat until every real photo has been shown once, then gracefully allows a repeat
    - server/test/noRepeatPhotos.integration.test.ts proves swapPhoto excludes the player's own previously-seen photos from its draw wherever the pool allows it
    - server/test/noRepeatPhotos.integration.test.ts proves swapPhoto refuses a second swap in the same round and refuses a swap after that player has submitted
    - npm --prefix server run test -- --run exits 0 with no regressions in the existing suite
  </acceptance_criteria>
  <done>The no-repeat photo guarantee and the one-time swap both behave correctly outside the common case — more rounds than photos, a swap that must avoid the seen set, and both of the swap's refusal paths — backed by tests, not assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Verify VOTE-03 still holds and prove VOTE-05's hidden-until-close guarantee structurally</name>
  <files>server/test/ratingPrivacy.integration.test.ts</files>
  <read_first>
    - `server/src/rooms/Room.ts`'s `snapshotFor` RATING branch and `buildRoundEndView` as they stand after Task 1 (the new `score` field)
    - `server/test/ratingStep.integration.test.ts`'s existing `CANNOT_RATE_OWN` coverage (VOTE-03 — do not duplicate this; only smoke-check it inline as part of this task's own test)
    - `server/test/captionPrivacy.integration.test.ts`'s real-socket privacy-proof pattern — the model for this task's own test, including its own documented reasoning for never mixing fake timers into a real-socket test (Socket.IO's own ping timers would be faked along with the room's)
  </read_first>
  <action>
Create `server/test/ratingPrivacy.integration.test.ts` using the real-socket, real-timer harness from
`captionPrivacy.integration.test.ts` (`startTestServer`/`connectClient`/`waitFor` from `./setup.js`
— do not mix in fake timers): host + b + c join a room, seed `room.settings.writingSeconds` and
`room.settings.ratingSeconds` generously (e.g. 5 and 8) via test-only internal seeding, start the
game, have host and b submit distinct captions (c never submits), reach the first `RATING` step.

VOTE-05 structural proof: from the author's own next snapshot AND from a non-author's next
snapshot, assert `Object.keys(snapshot.ratingStep!).sort()` equals exactly
`["caption", "eligibleCount", "index", "photoUrl", "ratedCount", "total", "youAreAuthor", "youHaveRated", "youMayRate"]`
(sorted) — proving no per-rater map or individual rating value has been added to the wire shape.
Have the eligible non-author rater(s) submit a rating for step 0 with a specific known value, then
read a snapshot from a player who is neither the author nor the one who just rated WHILE THE STEP IS
STILL OPEN and assert `snapshot.roundEnd` is `null` (the score has not been revealed yet) and
`Object.keys(snapshot.ratingStep!)` still matches the exact same nine-key whitelist above (no new
field appeared once a rating was cast). Let this step's own deadline expire naturally (do not seed
an artificially short deadline mid-test) and assert the resulting `ROUND_END` snapshot's
`roundEnd.entries[0].score` equals exactly the known value that was rated — VOTE-05's
"revealed only once closed" half, proven with a real number, not just a null check.

VOTE-03 regression smoke-check (do not rebuild — `CANNOT_RATE_OWN` is already fully proven in
`ratingStep.integration.test.ts`): using the same room/session already built above, assert the
current step's author's own snapshot has `ratingStep.youAreAuthor === true` and
`ratingStep.youMayRate === false`, confirming the existing waiting-state contract Phase 2 shipped is
still intact after this plan's changes.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/ratingPrivacy.integration.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>npm --prefix server run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - ratingStep's wire shape carries no field beyond the nine documented keys at any point while a step is open, including immediately after a rating is cast
    - roundEnd is null for every snapshot taken while the current step is still open
    - once the step closes, roundEnd.entries[0].score equals exactly the rated value that was cast
    - the current step's author's own snapshot still shows youAreAuthor true and youMayRate false
    - npm --prefix server run test -- --run exits 0 with no regressions
  </acceptance_criteria>
  <done>VOTE-05's hidden-until-close guarantee is proven structurally (no individual rating value or per-rater identity ever reaches the wire while a step is open, and the real total is revealed only once it closes), and VOTE-03's existing author-exclusion behavior is confirmed unbroken by this plan's changes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created or changed by this plan (new to the codebase, or extending an existing Phase 3
symbol):

**New exports (`server/src/rooms/photos.ts`):** `assignPhotosFromEligiblePools`, `drawOnePhoto`.

**New Room members (`server/src/rooms/Room.ts`):** `photosSeenByPlayer`, `swapUsed`,
`eligiblePoolFor`, `markPhotoSeen`, `drawPhotosForRound`, `swapPhoto`, `SwapPhotoOutcome`.

**Protocol additions (`shared/protocol.ts`):** `ErrorCode` gains `"SWAP_ALREADY_USED"`;
`CLIENT_EVENTS` gains `swapPhoto`; `LobbySnapshot` gains `youCanSwapPhoto`; `RoundEndEntry` gains
`score`.

**Hebrew string keys added (`shared/messages.ts`):** `HEBREW_ERRORS.SWAP_ALREADY_USED`;
`HEBREW_UI.swapPhotoButton`, `HEBREW_UI.roundResultsPointsSuffix`.

**Client:** `.swap-photo-button`, `.round-end-score` CSS classes; `WritingPanel.tsx`'s swap button;
`RoundEndPanel.tsx`'s score-ranked entries.

**New file paths:** `server/test/photoSwap.integration.test.ts`,
`server/test/noRepeatPhotos.integration.test.ts`, `server/test/ratingPrivacy.integration.test.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client socket -> `swap-photo` handler | New in this plan; a client's only lever over `Room.swapPhoto` — the payload carries no fields to trust |
| `Room` -> per-round photo state | `photosSeenByPlayer`/`swapUsed` are server-only in-memory state; no client message ever sets either directly |
| `Room` -> per-player snapshot | `youCanSwapPhoto`/`yourPhotoUrl` still expose only the recipient's own state; `roundEnd.entries[*].score` reveals only the aggregate total, never who cast which individual rating |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `swap-photo` handler / `Room.swapPhoto` identity | low | mitigate | `swapPhoto(data.playerId)` reads the player to swap exclusively from `socket.data.playerId`; the payload has no identity field a client could forge, matching every other handler's identity rule |
| T-04-02 | Tampering | `Room.swapPhoto`'s phase/lock checks | medium | mitigate | D-02's "locks permanently on submit" and "exactly one swap per round" are both enforced server-side inside `swapPhoto` before any mutation — a client that keeps emitting `swap-photo` after submitting or after its first successful swap is refused every time, never a silent extra swap |
| T-04-03 | Information Disclosure | `RoundEndEntry.score` (new field) | low | accept | `score` is VOTE-06's intentional reveal of a meme's own point total, gated by the existing `roundEnd` phase check (populated only in `ROUND_END`) — it never exposes which individual rater cast which value, so it does not reopen the "showing ratings while a meme is still being rated" risk this project explicitly ruled out |
| T-04-04 | Information Disclosure | `ratingStep` view during RATING (VOTE-05 regression risk) | high | mitigate | `snapshotFor`'s RATING branch is unchanged by this plan and still exposes only `ratedCount`/`eligibleCount`, never a per-rater map or value; `ratingPrivacy.integration.test.ts` (Task 3) structurally asserts the exact key set on `ratingStep` so a future change that accidentally adds a ratings field fails this test |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new package — no `package.json`/lockfile appears in `files_modified` |
</threat_model>

<verification>
- `npm --prefix server run test -- --run` exits 0
- `npm --prefix server run typecheck` exits 0
- `npm --prefix client run build` exits 0
- `server/test/photoSwap.integration.test.ts` proves the swap works end to end, locks correctly, and round results are ranked by real score
- `server/test/noRepeatPhotos.integration.test.ts` proves the no-repeat guarantee holds across more rounds than photos, with graceful reset afterward
- `server/test/ratingPrivacy.integration.test.ts` proves VOTE-05's hidden-until-close guarantee structurally and confirms VOTE-03 unbroken
</verification>

<success_criteria>
- A player can swap their assigned photo exactly once per round, instantly, before submitting — never after
- No player repeats a photo within the same game until they have seen every photo in the pool, after which repeats are allowed again
- A meme's author still cannot rate their own meme
- No individual rating value ever reaches another player's device while a step is open — only the aggregate total, revealed once the step closes
- The round-results screen ranks that round's memes by their real total score, highest first
</success_criteria>

<output>
Create `.planning/phases/04-full-round-rating-scoring-completion/04-01-SUMMARY.md` when done
</output>
