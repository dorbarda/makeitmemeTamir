---
phase: 04-full-round-rating-scoring-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/public/tamir-photos/.gitkeep
  - client/public/tamir-photos/01.svg
  - client/public/tamir-photos/02.svg
  - client/public/tamir-photos/03.svg
  - client/public/tamir-photos/04.svg
  - client/public/tamir-photos/05.svg
  - client/public/tamir-photos/06.svg
  - client/public/tamir-photos/07.svg
  - client/public/tamir-photos/08.svg
  - client/public/tamir-photos/09.svg
  - client/public/tamir-photos/10.svg
  - client/public/tamir-photos/11.svg
  - client/public/tamir-photos/12.svg
  - client/public/tamir-photos/13.svg
  - client/public/tamir-photos/14.svg
  - client/public/tamir-photos/15.svg
  - client/public/tamir-photos/16.svg
  - client/public/tamir-photos/17.svg
  - client/public/tamir-photos/18.svg
  - client/public/tamir-photos/19.svg
  - client/public/tamir-photos/20.svg
  - server/src/rooms/photoPool.ts
  - server/test/photoPool.test.ts
  - server/scripts/generate-placeholder-photos.mjs
autonomous: true
requirements: [ROUND-01, ROUND-02, ROUND-06]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Twenty distinct placeholder photo files exist under client/public/tamir-photos/, each loadable as a static image by the client and readable by the server's own directory scan."
    - "loadPhotoManifest() returns exactly the on-disk placeholder set, sorted deterministically, with any non-image file (e.g. a stray .gitkeep or .DS_Store) excluded."
    - "pickPhoto() returns an unused manifest entry whenever one exists, and only falls back to the least-recently-used entry once every entry has been used at least once (D-06)."
  artifacts:
    - client/public/tamir-photos/01.svg through 20.svg
    - server/src/rooms/photoPool.ts
    - server/test/photoPool.test.ts
  key_links:
    - "server/src/rooms/Room.ts (built in plan 04-02) will call photoPool.loadPhotoManifest() as the default value of its own photoManifest property — this plan's directory and this plan's loader are the only two things that link, and 04-02 depends on both existing."
---

<objective>
Lay the foundation the rest of Phase 4 assigns real photos from: a placeholder Tamir photo set
(D-08 — the real set is not yet in the repo, only `client/public/tamir-photos/.gitkeep`) and the
pure photo-pool selection logic (D-05/D-06) that plan 04-02 wires into the round engine.

This plan is deliberately NOT the phase's tracer — it produces no player-observable behavior on
its own (nothing reads these files yet). It is the prerequisite asset-and-algorithm layer plan
04-02's tracer task builds on top of, kept separate so the tracer task itself stays focused on
wiring rather than on generating fixtures. Per the phase's `TRACER_MODE=true` instruction, the
phase's actual `type="tracer"` task is plan 04-02's Task 1.

Purpose: unblock every other Phase 4 plan from D-08's "no real photos yet" constraint without
inventing a separate photo-upload feature (explicitly out of scope — CONT-01 is v2).
Output: 20 placeholder image files + `server/src/rooms/photoPool.ts` (manifest loader + pure
selection algorithm), both directly swappable for a real photo set later with zero code changes.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Twenty placeholder Tamir photos (D-08)</name>
  <files>client/public/tamir-photos/01.svg .. 20.svg, client/public/tamir-photos/.gitkeep</files>
  <read_first>
    client/public/tamir-photos/.gitkeep (confirm the directory is currently empty apart from this
    placeholder); .planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md D-08 (the
    "not yet in the repo" constraint and the "15-20 placeholder images" discretion note);
    .planning/phases/04-full-round-rating-scoring-completion/04-UI-SPEC.md "Notes for Executor" #1
    (photos load from `client/public/tamir-photos/{filename}`).
  </read_first>
  <action>
    Generate exactly 20 placeholder image files at client/public/tamir-photos/01.svg through
    client/public/tamir-photos/20.svg (two-digit zero-padded filenames, no other characters) —
    ROOM_CAPACITY is 20 (server/src/config.ts), so 20 distinct photos means round 1 never needs a
    same-round repeat even in a full room. Each file is a minimal, self-contained SVG image: a
    square viewBox "0 0 400 400", one rect covering the full square filled with
    hsl(H, 70%, 45%) where H = (n-1) * 18 for filename number n (so the 20 files sweep evenly
    around the color wheel and are visually distinguishable at a glance), and one centered bold
    text element showing the plain number n (no leading zero) in white, font-family sans-serif,
    font-size 160, text-anchor "middle", dominant-baseline "middle", positioned at the square's
    center (200, 200). These are placeholders only — D-08 is explicit that the real Tamir photos
    are a drop-in swap later, so nothing about their content matters beyond being distinct,
    loadable images. Write a small one-off generator script at
    server/scripts/generate-placeholder-photos.mjs that emits these 20 files programmatically (a
    single template string parameterized by n), run it once via node, and keep the script
    committed for reproducibility if the placeholder count or palette ever needs regenerating.
    Once the 20 real files exist, delete client/public/tamir-photos/.gitkeep — the directory no
    longer needs a keep-file.
  </action>
  <verify>
    <automated>test "$(ls client/public/tamir-photos/*.svg 2>/dev/null | wc -l)" -eq 20</automated>
    <fails_when>non-zero exit code — the count of *.svg files under client/public/tamir-photos/ is not exactly 20</fails_when>
    <automated>test ! -f client/public/tamir-photos/.gitkeep</automated>
    <fails_when>non-zero exit code — .gitkeep still exists in client/public/tamir-photos/</fails_when>
  </verify>
  <acceptance_criteria>
    - Files client/public/tamir-photos/01.svg through 20.svg all exist, each containing a `<svg`
      root element and a `<text` element (grep -l '<svg' and grep -l '<text' both match all 20
      files).
    - No two files are byte-identical (each has a distinct fill color and a distinct number).
    - client/public/tamir-photos/.gitkeep no longer exists.
  </acceptance_criteria>
  <done>20 distinct placeholder SVG photos exist under client/public/tamir-photos/, ready for the
  server's directory scan and the client's `<img>` tags to consume.</done>
</task>

<task type="auto">
  <name>Task 2: photoPool.ts — manifest loader + pure selection algorithm (D-05, D-06)</name>
  <files>server/src/rooms/photoPool.ts, server/test/photoPool.test.ts</files>
  <read_first>
    server/src/rooms/rotation.ts and server/src/rooms/gameSettings.ts (the "small pure module
    beside Room" pattern this file follows — plain functions, no class, no side effects beyond the
    one directory read); server/src/app.ts lines 1-13 (the CLIENT_DIST path-resolution pattern —
    `path.resolve` from `path.dirname(fileURLToPath(import.meta.url))` — this new file lives one
    directory deeper, at server/src/rooms/, so it needs three `..` segments to reach the repo root
    where app.ts's CLIENT_DIST needs two); server/src/config.ts (constant/comment conventions);
    .planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md D-05/D-06 (the used-set
    and least-recently-used fallback rules this module implements).
  </read_first>
  <action>
    Create server/src/rooms/photoPool.ts. Export a constant TAMIR_PHOTOS_DIR computed via
    path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../../../client/public/tamir-photos")
    — three ".." segments up from server/src/rooms/ to the repo root, then into
    client/public/tamir-photos, mirroring app.ts's own CLIENT_DIST resolution one level shallower.
    Export listPhotoFiles(dir: string): string[] using node:fs's readdirSync on dir, filtering to
    entries whose lowercased path.extname is one of .jpg/.jpeg/.png/.webp/.gif/.svg, sorted
    alphabetically; wrap the readdirSync call so a missing directory returns an empty array rather
    than throwing (a misconfigured deploy should fail loudly later, at the first pickPhoto call,
    not silently at module import time). Export loadPhotoManifest(): string[], memoized in a
    module-level variable so the directory is scanned exactly once per server process, delegating
    to listPhotoFiles(TAMIR_PHOTOS_DIR). Export pickPhoto(manifest: readonly string[],
    usedPhotoFiles: ReadonlySet&lt;string&gt;, lastUsedSeq: ReadonlyMap&lt;string, number&gt;): string
    implementing D-05/D-06: if any manifest entry is absent from usedPhotoFiles, return the first
    such entry in manifest order (deterministic — this is the "unused pool" D-05 describes);
    otherwise every entry has been used at least once, so return the entry with the lowest value in
    lastUsedSeq (an entry with no recorded seq defaults to -1, so it always wins against any entry
    that has been recorded at least once), breaking exact ties by manifest order. Throw a
    descriptive Error when manifest is empty — this is a startup/configuration invariant (an empty
    photo set), not a per-request client-triggered outcome, so it deliberately does not follow the
    fail-closed typed-outcome convention Room's own player-facing methods use (submitCaption,
    submitRating, etc.).
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run test/photoPool.test.ts</automated>
    <fails_when>vitest reports any failing test, or a non-zero process exit code</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>tsc exits non-zero, or output contains the string "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - listPhotoFiles filters out non-image extensions and returns a sorted array (proven against a
      temp directory built with node:fs's mkdtempSync, mixing recognized and unrecognized
      extensions).
    - loadPhotoManifest() called against the real TAMIR_PHOTOS_DIR returns exactly the 20 filenames
      Task 1 created (cross-checks Task 1's output from inside this same plan).
    - pickPhoto returns the first manifest entry not present in usedPhotoFiles when at least one
      such entry exists.
    - pickPhoto returns the exact filename with the lowest lastUsedSeq value once every manifest
      entry is present in usedPhotoFiles (a specific worked example with known seq values asserts
      the exact filename returned, not just "some filename").
    - pickPhoto throws for an empty manifest array.
  </acceptance_criteria>
  <done>server/src/rooms/photoPool.ts exports a working manifest loader and pickPhoto algorithm,
  fully unit-tested, ready for plan 04-02's Room.ts to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Filesystem -> server process | photoPool.ts reads a fixed, developer-controlled directory at server startup; no network or client input reaches this code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-00 | Tampering | photoPool.ts listPhotoFiles | low | accept | Filenames come exclusively from an `fs.readdirSync` scan of a directory shipped in the repo (never from client-supplied input), so there is no attacker-controlled path segment to sanitize — a bare filename from a real directory listing cannot contain "../" traversal sequences. |
| T-04-01 | Denial of Service | photoPool.ts pickPhoto | low | accept | An empty manifest throws at the first pick, which only happens if the placeholder/real photo set is misconfigured at deploy time (a build-time asset problem under developer control), not something a player can trigger at runtime. |
</threat_model>

<verification>
Run both automated verify commands above. Confirm the 20 SVG files exist and are visually
distinct (spot-check 2-3 by opening in a browser or image viewer). Confirm photoPool.test.ts
passes and typecheck is clean.
</verification>

<success_criteria>
- 20 placeholder photos committed under client/public/tamir-photos/, .gitkeep removed.
- server/src/rooms/photoPool.ts exports loadPhotoManifest/listPhotoFiles/pickPhoto/TAMIR_PHOTOS_DIR,
  all covered by server/test/photoPool.test.ts.
- Nothing in the running app yet reads these exports — that wiring is plan 04-02's job.
</success_criteria>

<output>
Create `.planning/phases/04-full-round-rating-scoring-completion/04-01-SUMMARY.md` when done.
</output>

## Artifacts This Plan Produces

- `client/public/tamir-photos/01.svg` .. `20.svg` — 20 placeholder photo files (D-08).
- `server/src/rooms/photoPool.ts` — new module exporting:
  - `TAMIR_PHOTOS_DIR: string` (constant)
  - `listPhotoFiles(dir: string): string[]`
  - `loadPhotoManifest(): string[]` (memoized)
  - `pickPhoto(manifest: readonly string[], usedPhotoFiles: ReadonlySet<string>, lastUsedSeq: ReadonlyMap<string, number>): string`
- `server/scripts/generate-placeholder-photos.mjs` — one-off generator script for the 20 placeholder files.
- `server/test/photoPool.test.ts` — new test file.
- `client/public/tamir-photos/.gitkeep` — removed.
