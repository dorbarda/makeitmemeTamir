---
phase: 04-full-round-rating-scoring-completion
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - client/src/screens/round/ScoreboardPanel.tsx
  - client/src/screens/round/BestOfNightPanel.tsx
  - client/src/screens/Round.tsx
  - client/src/index.css
autonomous: true
requirements: [SCORE-02, SCORE-03, SCORE-04, MEME-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A running scoreboard, sorted highest score first, is visible on the round-end screen after every round and again on the final screen (SCORE-02, SCORE-03)."
    - "The final screen highlights the single highest-scoring player as the winner, with the full scoreboard shown below it, plain and unadorned with no confetti or effects layer (SCORE-04, D-11)."
    - "A best-of-night list of the top scoring memes — photo, caption, author, score, round number — is visible on the final screen, and never appears before at least one round has finished (MEME-02, D-03)."
    - {statement: "The best-of-night list never crops entries even if ties push it past 3 items — the whole page scrolls, there is no fixed-height inner container to overflow.", verification: backstop}
    - "At least two distinct real phones have played one full round — join, write with a real photo, rate meme-by-meme, see round results and running score — with no stalls, crashes, or desyncs (D-12, the folded-in Phase 3 checkpoint)."
  artifacts:
    - client/src/screens/round/ScoreboardPanel.tsx
    - client/src/screens/round/BestOfNightPanel.tsx
    - client/src/screens/Round.tsx
    - client/src/index.css
  key_links:
    - "Round.tsx mounts ScoreboardPanel for ROUND_END and GAME_END, and BestOfNightPanel additionally for GAME_END only — snapshot.scoreboard/snapshot.bestOfNight are the only source, no client-side derivation of a total or a winner."
---

<objective>
Finish the phase: a between-rounds and final scoreboard, a final winner highlight, and the
best-of-night list — all rendered from fields plan 04-02 already put on the snapshot — then close
the phase with the lightweight real-phone playtest D-12 requires in place of the skipped Phase 3
checkpoint.

Design choice made here (Claude's discretion, per 04-CONTEXT.md and the UI-Spec's own ambiguity
between "shown once after game end" and "a separate view-highlights screen"): the final winner
section and the best-of-night section both render on the single `GAME_END` screen, stacked as two
distinctly-headed sections, rather than inventing new client-side navigation state that nothing in
CONTEXT.md or UI-SPEC.md specifies. This keeps the terminal screen a single, simple render of
whatever `snapshot.phase === "GAME_END"` already provides, consistent with D-11's "plain,
unadorned" instruction.

Purpose: complete SCORE-02/03/04 and MEME-02, and close out the whole phase with the D-12 playtest
gate before Phase 5 begins.
Output: two new small components, `Round.tsx` wired to mount them, and a completed real-phone
playtest.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md
@.planning/phases/04-full-round-rating-scoring-completion/04-UI-SPEC.md
@shared/protocol.ts
@shared/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreboardPanel — running totals between rounds and on the final screen (SCORE-02, SCORE-03, SCORE-04, D-11)</name>
  <files>client/src/screens/round/ScoreboardPanel.tsx, client/src/screens/Round.tsx</files>
  <read_first>
    client/src/screens/round/RoundEndPanel.tsx (the props/rendering pattern this new sibling
    component follows — a single `{ snapshot: LobbySnapshot }` prop, no local state); client/src/
    screens/Round.tsx (full file — where RoundEndPanel is currently mounted, and the exact
    conditional this task extends); shared/protocol.ts's `ScoreboardEntry` type (added by plan
    04-02); shared/messages.ts's `HEBREW_UI.scoreboardHeading`/`winnerLabel`/`pointsLabel`;
    .planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md D-02 and D-11;
    .planning/phases/04-full-round-rating-scoring-completion/04-UI-SPEC.md sections 5 and 6.
  </read_first>
  <action>
    Create client/src/screens/round/ScoreboardPanel.tsx: a `{ snapshot: LobbySnapshot }`-props
    component. Render a `&lt;section className="scoreboard-panel"&gt;` with heading
    `&lt;h2&gt;{HEBREW_UI.scoreboardHeading}&lt;/h2&gt;`. When `snapshot.phase === "GAME_END"`, render a
    winner highlight above the list: read `snapshot.scoreboard[0]` (already sorted score-descending
    by the server) and render `&lt;p className="winner-highlight"&gt;{HEBREW_UI.winnerLabel}:
    {winner.name} — {winner.score} {HEBREW_UI.pointsLabel}&lt;/p&gt;` (guard for an empty scoreboard —
    an empty room reaching GAME_END should never happen given `MIN_PLAYERS_TO_START`, but render
    nothing rather than throwing if `snapshot.scoreboard.length === 0`). Below that (or, on
    `ROUND_END`, as the only content), render `&lt;ul className="scoreboard-list"&gt;` mapping
    `snapshot.scoreboard` (already sorted — no client-side sort) to
    `&lt;li key={entry.playerId} className={entry.connected ? "scoreboard-row" : "scoreboard-row
    player--disconnected"}&gt;{entry.name} — {entry.score} {HEBREW_UI.pointsLabel}&lt;/li&gt;`, reusing
    the existing `player--disconnected` class for a departed player exactly as the roster list
    already does elsewhere. In client/src/screens/Round.tsx, import `ScoreboardPanel` and mount it
    inside the existing `{(snapshot.phase === "ROUND_END" || snapshot.phase === "GAME_END") &&
    (...)}` block, alongside `RoundEndPanel`, rendered after it (round results first, running
    totals second — matches the UI-Spec's own screen ordering).
  </action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>tsc exits non-zero, or output contains the string "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - ScoreboardPanel.tsx never sorts, filters, or recomputes `snapshot.scoreboard` — it is rendered
      in the order the server sent it.
    - The winner highlight only renders when `snapshot.phase === "GAME_END"`.
    - Round.tsx mounts `ScoreboardPanel` for both `ROUND_END` and `GAME_END`.
  </acceptance_criteria>
  <done>A running scoreboard appears after every round and on the final screen, with a winner
  highlight shown only at game end.</done>
</task>

<task type="auto">
  <name>Task 2: BestOfNightPanel — top scoring memes on the final screen (MEME-02, D-03)</name>
  <files>client/src/screens/round/BestOfNightPanel.tsx, client/src/screens/Round.tsx, client/src/index.css</files>
  <read_first>
    client/src/screens/round/RoundEndPanel.tsx (the `.round-end-entry`/`.round-photo--thumb`
    rendering pattern this component reuses); client/src/screens/Round.tsx (this task's own Task 1
    edit — where ScoreboardPanel now mounts); shared/protocol.ts's `BestOfNightEntry` type;
    shared/messages.ts's `HEBREW_UI.bestOfNightHeading`/`bestOfNightRoundPrefix`/`rankLabel`/
    `pointsLabel`; .planning/phases/04-full-round-rating-scoring-completion/04-CONTEXT.md D-03;
    .planning/phases/04-full-round-rating-scoring-completion/04-UI-SPEC.md section 7 and its "UI
    Considerations" empty/overflow rows.
  </read_first>
  <action>
    Create client/src/screens/round/BestOfNightPanel.tsx: a `{ snapshot: LobbySnapshot }`-props
    component. Return `null` immediately when `snapshot.bestOfNight.length === 0` (the UI-Spec's
    own empty-state rule — never show this screen before at least one round has finished with
    scores). Otherwise render `&lt;section className="best-of-night-panel"&gt;` with heading
    `&lt;h2&gt;{HEBREW_UI.bestOfNightHeading}&lt;/h2&gt;` and an `&lt;ul&gt;` mapping
    `snapshot.bestOfNight` (already the server's own top-3(+ties) merge, plan 04-02 — no
    client-side re-sort or re-truncation) to an `&lt;li key={\`${entry.authorId}-${entry.roundIndex}\`}
    className="round-end-entry"&gt;` (reusing the existing entry styling rather than inventing a new
    class, per the UI-Spec's own "maximize reuse" note) containing: an `&lt;img className="round-photo
    round-photo--thumb"&gt;` with `src={\`/tamir-photos/${entry.photoFile}\`}`; the `authorName` and
    `caption` paragraphs; and a line showing `{entry.score} {HEBREW_UI.pointsLabel} —
    {HEBREW_UI.bestOfNightRoundPrefix} {entry.roundIndex}`. Do not wrap the `&lt;ul&gt;` in any
    fixed-height or `overflow-y` container — the page's normal vertical scroll already satisfies
    "never crop, scrollable if needed" for a list that can occasionally grow past 3 entries on
    ties. In client/src/screens/Round.tsx, import `BestOfNightPanel` and mount it inside the same
    conditional block Task 1 edited, but gated additionally on `snapshot.phase === "GAME_END"`
    only (never on `ROUND_END`), rendered after `ScoreboardPanel` — per this plan's objective, the
    final winner and the best-of-night list share the single `GAME_END` screen as two stacked
    sections.

    In index.css, add the styling both this task's and Task 1's new components reference (Task 1
    ran first in this same plan and left these class names unstyled, exactly as plan 04-03's Task 1
    referenced `.round-photo` before its own Task 3 defined it): `.scoreboard-panel { margin: 1rem
    0; }`; `.scoreboard-list { margin: 0; }` (reuses the global `ul`/`li` rules for spacing —
    `.scoreboard-row` only needs to add the score text weight); `.scoreboard-row { font-weight:
    600; }`; `.winner-highlight { font-size: 1.25rem; font-weight: 700; margin: 0 0 1rem; }`;
    `.best-of-night-panel { margin: 1rem 0; }`. All four follow the same minimal, no-fixed-width
    pattern already established by `.writing-panel`/`.rating-panel`/`.round-end-panel`.
  </action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>tsc exits non-zero, or output contains the string "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>vite build (via tsc -b) exits non-zero, or the output contains the string "error"</fails_when>
    <human-check>
      D-12 — the folded-in Phase 3 checkpoint (Phase 3 was explicitly skipped for time; see
      ROADMAP.md and STATE.md's Roadmap Evolution log). On at least 2 distinct real phones (mixing
      makes/OS if available — this is a lighter-weight version of DEPLOY-03's later full rehearsal,
      not a substitute for it), play one full round start to finish: both phones join the same room
      by code; each writes a Hebrew caption for their own real assigned placeholder photo (swap at
      least once on one phone to confirm the swap button works on a real touch target); after
      writing closes, both phones watch the memes reveal one at a time and rate every meme they are
      not the author of, confirming the author's own phone shows the wait state with no buttons;
      confirm the round results screen shows a ranked list with visible scores; confirm the
      scoreboard appears with a running total. Pass condition: no stalls, crashes, or desyncs
      between the two phones at any point in the loop — join, write with a real photo (including
      one swap), rate meme-by-meme, see ranked round results and a running scoreboard.
    </human-check>
  </verify>
  <acceptance_criteria>
    - BestOfNightPanel.tsx returns `null` when `snapshot.bestOfNight` is empty — verified by reading
      the component's own early-return guard.
    - BestOfNightPanel is mounted in Round.tsx only for `GAME_END`, never `ROUND_END`.
    - No new fixed-height/`overflow-y` CSS rule was added for the best-of-night list.
    - index.css defines `.scoreboard-panel`, `.scoreboard-list`, `.scoreboard-row`,
      `.winner-highlight`, and `.best-of-night-panel`.
    - `npm --prefix client run build` succeeds.
    - The D-12 real-phone playtest (folded-in Phase 3 checkpoint) is confirmed with no stalls,
      crashes, or desyncs — this closes the phase's own success criterion #7.
  </acceptance_criteria>
  <done>The final screen shows the winner, the full scoreboard, and — once at least one round has
  finished — the best-of-night top-scoring memes list; the D-12 real-phone playtest confirms the
  whole loop works end to end, closing out the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server snapshot -> client render | ScoreboardPanel and BestOfNightPanel render `scoreboard`/`bestOfNight` arrays the server already computed and sorted (plan 04-02) — no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | ScoreboardPanel / BestOfNightPanel rendering | low | accept | Both components render server-provided arrays verbatim with no client-side recomputation of rank, score, or order — there is no code path here that could let a client display a fabricated winner or score even if the underlying data were somehow malformed. |
</threat_model>

<verification>
Run both tasks' automated verify commands. Then perform Task 2's embedded human-check (the D-12
real-phone playtest) on real hardware — this cannot be automated and is the phase's own closing
gate. Per `workflow.human_verify_mode = end-of-phase`, this human-check is harvested at end-of-phase
verification into `{phase_num}-UAT.md` rather than pausing mid-execution.
</verification>

<success_criteria>
- A running scoreboard is visible between rounds and on the final screen, sorted highest first.
- The final screen highlights the winner and shows the full scoreboard, plain and unadorned.
- The best-of-night list appears only after at least one round has finished, showing the top
  scoring memes with photo, caption, author, score, and round number.
- The D-12 real-phone playtest has been completed and confirmed with no stalls, crashes, or
  desyncs — closing the phase's own folded-in Phase 3 success criterion.
</success_criteria>

<output>
Create `.planning/phases/04-full-round-rating-scoring-completion/04-04-SUMMARY.md` when done.
</output>

## Artifacts This Plan Produces

- `client/src/screens/round/ScoreboardPanel.tsx` — new component.
- `client/src/screens/round/BestOfNightPanel.tsx` — new component.
- `client/src/screens/Round.tsx`: mounts `ScoreboardPanel` (ROUND_END, GAME_END) and
  `BestOfNightPanel` (GAME_END only).
- `client/src/index.css`: `.scoreboard-panel`, `.scoreboard-list`, `.scoreboard-row`,
  `.winner-highlight`, `.best-of-night-panel`.
