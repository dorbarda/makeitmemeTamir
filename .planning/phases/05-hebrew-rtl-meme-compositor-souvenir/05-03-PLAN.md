---
phase: 05-hebrew-rtl-meme-compositor-souvenir
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - client/src/canvas/hitTest.ts
  - client/src/canvas/hitTest.test.ts
  - client/src/screens/round/WritingPanel.tsx
  - shared/messages.ts
  - client/src/index.css
autonomous: true
requirements: [MEME-01]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A player can add up to 3 caption boxes (MEME-01/D-05) and drag each one, via pointer events with setPointerCapture, to any position on the canvas; a 4th add is impossible — the add button disappears once canAddBox returns false, never silently ignored or erroring."
    - "Two caption boxes may fully overlap; there is no collision/merge logic — hit-testing and rendering both resolve overlap purely by array order, with the LAST box in the array always drawn on top and always the one a tap/drag lands on (MEME-01/adjacency edge, resolved for the specless-probe fallback)."
    - "Caption boxes are drawn and hit-tested in stable array order; a newly added box is always appended last (renders on top); dragging a box repositions it in place without reordering the array — a tie in visual overlap is therefore always deterministic, never randomized (MEME-01/ordering edge, resolved)."
    - "At least one caption box always remains in the editor's UI — the remove button is disabled once only one box is left — so 'zero non-empty caption boxes' (Plan 05-02's submit-time refusal) is the only path to an empty-submission error, never a 'zero boxes at all' UI state."
  artifacts:
    - path: "client/src/canvas/hitTest.ts"
      provides: "MAX_CAPTION_BOXES, getBoxAtPoint, clampBoxPosition, nextBoxPosition, canAddBox — the pure hit-testing/placement primitives WritingPanel's pointer handlers use"
      exports: ["MAX_CAPTION_BOXES", "getBoxAtPoint", "clampBoxPosition", "nextBoxPosition", "canAddBox"]
    - path: "client/src/screens/round/WritingPanel.tsx"
      provides: "pointer-event drag-and-drop over the canvas, an add-box button gated on canAddBox, and a remove button per box gated on boxes.length > 1"
  key_links:
    - from: "client/src/screens/round/WritingPanel.tsx"
      to: "client/src/canvas/hitTest.ts"
      via: "onPointerDown scales client coordinates by canvas.width/rect.width before calling getBoxAtPoint, then setPointerCapture keeps the drag alive even if the pointer leaves the canvas"
      pattern: "getBoxAtPoint\\(boxes"
    - from: "client/src/screens/round/WritingPanel.tsx"
      to: "client/src/canvas/compositor.ts"
      via: "the boxes array Plan 05-02 already threads through drawFrame/redraw needs no shape change — this plan only adds more elements to it and moves them"
      pattern: "boxes.map"
---

<objective>
Expand Plan 05-02's proven single-box tracer to the full MEME-01/D-05 scope: up to 3 caption boxes,
each independently draggable via touch/pointer events, with deterministic hit-testing when boxes
overlap. This is the phase's own highest real-phone risk (CONTEXT.md: "the highest-risk-of-
looking-fine-in-code-but-broken-on-a-real-phone feature built so far") — isolated here, on top of an
already-proven rasterize/transmit/render pipeline, rather than built simultaneously with it.

Purpose: MEME-01's full scope (multiple positioned caption boxes, chosen by the user as the more
ambitious of the options originally offered) is not met by Plan 05-02's single fixed box alone. This
plan is what actually delivers "the player can add MULTIPLE caption text boxes on one photo, and DRAG
each one to any position" from REQUIREMENTS.md's own MEME-01 scope note.

Output: a player can tap "add caption," type into up to 3 independent boxes, and drag each one
anywhere on their photo before submitting — the exact editor D-01 replaces the writing phase with.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-CONTEXT.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-RESEARCH.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-PATTERNS.md

@client/src/canvas/compositor.ts
@client/src/screens/round/WritingPanel.tsx
@shared/messages.ts
@client/src/index.css

<interfaces>
Existing contracts this plan extends — stated as they stand AFTER Plan 05-02 (this plan's own
`depends_on: ["05-02"]` guarantees that ordering).

From `client/src/canvas/compositor.ts` (after Plan 05-02): `CaptionBox = { id: string; text: string;
x: number; y: number }`, `CANVAS_WIDTH = 600`, `CANVAS_HEIGHT = 800`, `BOX_WIDTH = 360`,
`BOX_HEIGHT = 100`, and `drawFrame(ctx, photo, boxes)` — already loops over the FULL `boxes` array in
array order (drawing each box's semi-transparent backing then its RTL text), so it needs NO change for
multiple boxes; this plan only ever grows the array `WritingPanel` passes to it.

From `client/src/screens/round/WritingPanel.tsx` (after Plan 05-02): `const [boxes, setBoxes] =
useState<CaptionBox[]>([{ id: "box-1", text: "", x: ..., y: ... }]);` (exactly one box, fixed
position, no drag), a single text `<input>` bound to `boxes[0].text`, a `redraw()` function called
from two `useEffect`s (`[snapshot.yourPhotoUrl]` and `[boxes]`), and `handleSubmit`'s
`boxes.some((b) => b.text.trim().length > 0)` empty-guard (unchanged by this plan — it already works
correctly for N boxes with no modification, since `.some` already checks the whole array).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure hit-testing and placement module</name>
  <reversibility rating="reversible">Array-order-wins tiebreaking and the fixed per-box offset placement are easily retunable implementation choices behind a small pure module — no architecture depends on the exact numbers.</reversibility>
  <files>client/src/canvas/hitTest.ts, client/src/canvas/hitTest.test.ts</files>
  <read_first>
    - `client/src/canvas/compositor.ts` — `CaptionBox`, `BOX_WIDTH`, `BOX_HEIGHT`, `CANVAS_WIDTH`, `CANVAS_HEIGHT` exactly as Plan 05-02 wrote them
    - `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-RESEARCH.md`'s "Hit-Testing: Selecting Which Box a Pointer Targets" section — the reverse-iteration pattern this module implements
  </read_first>
  <behavior>
    - getBoxAtPoint, given a point inside exactly one box, returns that box's id
    - getBoxAtPoint, given a point inside two overlapping boxes, returns the LAST box in the array (the topmost, per draw order) — never the first, never both
    - getBoxAtPoint, given a point outside every box, returns null
    - getBoxAtPoint, given a point exactly on a box's edge (x === box.x, or x === box.x + BOX_WIDTH), still returns that box's id (inclusive bounds)
    - clampBoxPosition, given a position beyond the canvas's right/bottom edge, clamps to exactly (CANVAS_WIDTH - BOX_WIDTH, CANVAS_HEIGHT - BOX_HEIGHT)
    - clampBoxPosition, given a negative position, clamps to exactly (0, 0)
    - nextBoxPosition returns a strictly different (offset) position for existingCount 0, 1, and 2, and every returned position is itself within clampBoxPosition's own bounds
    - canAddBox returns true for 0, 1, and 2 existing boxes and false for 3 (MAX_CAPTION_BOXES)
  </behavior>
  <action>
Create `client/src/canvas/hitTest.ts`:
```
import type { CaptionBox } from "./compositor";
import { BOX_WIDTH, BOX_HEIGHT, CANVAS_WIDTH, CANVAS_HEIGHT } from "./compositor";

export const MAX_CAPTION_BOXES = 3; // D-05

/** Reverse array order — the last-drawn (topmost) box wins a tap on overlapping
 * boxes. No collision/merge logic: boxes may fully overlap (MEME-01/adjacency
 * edge case). */
export function getBoxAtPoint(boxes: CaptionBox[], x: number, y: number): string | null {
  for (let i = boxes.length - 1; i >= 0; i--) {
    const box = boxes[i];
    if (x >= box.x && x <= box.x + BOX_WIDTH && y >= box.y && y <= box.y + BOX_HEIGHT) {
      return box.id;
    }
  }
  return null;
}

export function clampBoxPosition(x: number, y: number): { x: number; y: number } {
  return {
    x: Math.max(0, Math.min(x, CANVAS_WIDTH - BOX_WIDTH)),
    y: Math.max(0, Math.min(y, CANVAS_HEIGHT - BOX_HEIGHT)),
  };
}

/** A newly added box starts near center, offset from each prior box so boxes
 * added in sequence are visibly distinct before the player drags them apart. */
export function nextBoxPosition(existingCount: number): { x: number; y: number } {
  const OFFSET = 32;
  const baseX = (CANVAS_WIDTH - BOX_WIDTH) / 2;
  const baseY = (CANVAS_HEIGHT - BOX_HEIGHT) / 2;
  return clampBoxPosition(baseX + existingCount * OFFSET, baseY + existingCount * OFFSET);
}

export function canAddBox(currentCount: number): boolean {
  return currentCount < MAX_CAPTION_BOXES;
}
```

Create `client/src/canvas/hitTest.test.ts` covering every behavior line above, using small hand-built
`CaptionBox[]` fixtures (no canvas, no DOM — this module has none).
  </action>
  <verify>
    <automated>npm --prefix client run test -- --run hitTest.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - client/src/canvas/hitTest.ts exports MAX_CAPTION_BOXES, getBoxAtPoint, clampBoxPosition, nextBoxPosition, canAddBox
    - getBoxAtPoint resolves overlapping boxes to the last array element, never the first
    - canAddBox(3) is false; canAddBox(0/1/2) is true
    - npm --prefix client run test -- --run hitTest.test.ts passes
  </acceptance_criteria>
  <done>The hit-testing/placement math is proven correct in isolation, including the adjacency (full-overlap) and ordering (stable, array-order-wins) edge cases, before any pointer-event wiring touches real DOM state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pointer-event drag-and-drop, add/remove boxes, into WritingPanel</name>
  <files>client/src/screens/round/WritingPanel.tsx, shared/messages.ts, client/src/index.css</files>
  <read_first>
    - `client/src/screens/round/WritingPanel.tsx` exactly as Plan 05-02 wrote it (quoted in this plan's `<interfaces>` block)
    - `client/src/canvas/hitTest.ts` exactly as written by Task 1
    - `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-RESEARCH.md`'s "Touch Drag-and-Drop: Implementation Pattern" section — the `setPointerCapture`/coordinate-scaling pattern this task implements, including its own documented gotcha: a CSS-scaled canvas (`width: 100%` in this codebase's own `.meme-canvas` rule) has an internal pixel resolution DIFFERENT from `getBoundingClientRect()`'s CSS size, so every pointer coordinate must be scaled by `canvas.width / rect.width` (and the `height` equivalent) before being compared against box positions, which are always in the canvas's own intrinsic pixel space
    - `shared/messages.ts`'s existing `HEBREW_UI` comment-grouped-by-plan convention
  </read_first>
  <action>
In `client/src/screens/round/WritingPanel.tsx`: import `MAX_CAPTION_BOXES, getBoxAtPoint,
clampBoxPosition, nextBoxPosition, canAddBox` from `../../canvas/hitTest.js`.

Add `const [draggingId, setDraggingId] = useState<string | null>(null);` and
`const dragOffsetRef = useRef({ x: 0, y: 0 });`.

Add a coordinate-scaling helper:
```
function getCanvasPoint(e: React.PointerEvent<HTMLCanvasElement>): { x: number; y: number } {
  const canvas = e.currentTarget;
  const rect = canvas.getBoundingClientRect();
  const scaleX = canvas.width / rect.width;
  const scaleY = canvas.height / rect.height;
  return { x: (e.clientX - rect.left) * scaleX, y: (e.clientY - rect.top) * scaleY };
}
```

Add three pointer handlers:
```
function handlePointerDown(e: React.PointerEvent<HTMLCanvasElement>) {
  const point = getCanvasPoint(e);
  const hitId = getBoxAtPoint(boxes, point.x, point.y);
  if (!hitId) return;
  const box = boxes.find((b) => b.id === hitId)!;
  dragOffsetRef.current = { x: point.x - box.x, y: point.y - box.y };
  setDraggingId(hitId);
  e.currentTarget.setPointerCapture(e.pointerId);
}

function handlePointerMove(e: React.PointerEvent<HTMLCanvasElement>) {
  if (!draggingId) return;
  const point = getCanvasPoint(e);
  const offset = dragOffsetRef.current;
  const { x, y } = clampBoxPosition(point.x - offset.x, point.y - offset.y);
  setBoxes((prev) => prev.map((b) => (b.id === draggingId ? { ...b, x, y } : b)));
}

function handlePointerUp(e: React.PointerEvent<HTMLCanvasElement>) {
  if (draggingId) e.currentTarget.releasePointerCapture(e.pointerId);
  setDraggingId(null);
}
```
Wire these onto the `<canvas>` element: `onPointerDown={handlePointerDown}
onPointerMove={handlePointerMove} onPointerUp={handlePointerUp} onPointerCancel={handlePointerUp}`.

Replace the single `<input>` bound to `boxes[0].text` with a `boxes.map((box, i) => (...))` list, one
row per box, each containing: a text `<input>` bound to `box.text`,
`onChange={(e) => setBoxes(boxes.map((b) => (b.id === box.id ? { ...b, text:
clampCaptionForInput(e.target.value) } : b)))}`; a `<span>{graphemesRemaining(box.text)}</span>`
counter; and, only when `boxes.length > 1`, a remove `<button type="button">` with
`onClick={() => setBoxes(boxes.filter((b) => b.id !== box.id))}` and text
`{HEBREW_UI.deleteCaptionButton}` (removing is never offered for the last remaining box — the editor
always shows at least one).

Add an "add caption" `<button type="button">`, rendered only when `canAddBox(boxes.length)` is true,
`onClick={() => setBoxes([...boxes, { id: \`box-${Date.now()}\`, text: "",
...nextBoxPosition(boxes.length) }])}`, text `{HEBREW_UI.addCaptionBoxButton}`. Add one short
instruction line above the canvas, `<p className="meme-editor-instructions">{HEBREW_UI.memeEditorInstructions}</p>`.

In `shared/messages.ts`: add three new keys to `HEBREW_UI`, grouped under a new comment
`// plan 05-03 — multi-box drag editor`, after the existing `swapPhotoButton`/
`roundResultsPointsSuffix` group: `memeEditorInstructions: "גררו כל כיתוב למקום הרצוי על התמונה"`,
`addCaptionBoxButton: "הוספת כיתוב"`, `deleteCaptionButton: "מחיקה"`.

In `client/src/index.css`: add `touch-action: none;` to the existing `.meme-canvas` rule (CRITICAL —
without this, a touch-drag on a real phone fights the browser's own scroll gesture instead of moving
the box). Add a `.meme-editor-instructions` rule right after `.meme-canvas`:
`font-size: 0.875rem; opacity: 0.75; margin: 0 0 0.5rem;`. Add a `.caption-box-row` rule (wrapping each
box's input+counter+remove-button row) matching `label`'s existing flex-column pattern but laid out as
a row with `display: flex; align-items: center; gap: 0.5rem; margin: 0 0 0.5rem;`, and a
`.caption-box-remove` rule for the small remove button: `width: auto; min-width: 2.75rem;
background: transparent; border: 1px solid currentColor; color: inherit;` (outlined secondary style,
same convention as `.swap-photo-button`).
  </action>
  <verify>
    <automated>npm --prefix client run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
    <automated>grep -c "setPointerCapture" client/src/screens/round/WritingPanel.tsx</automated>
    <fails_when>output is 0, meaning drag capture was not wired onto the canvas</fails_when>
    <automated>grep -c "touch-action: none" client/src/index.css</automated>
    <fails_when>output is 0, meaning the touch-drag-vs-scroll conflict fix documented as CRITICAL was dropped</fails_when>
  </verify>
  <acceptance_criteria>
    - WritingPanel.tsx renders one input+counter row per element of boxes, plus an add-caption button gated on canAddBox and a per-box remove button gated on boxes.length > 1
    - The canvas has onPointerDown/onPointerMove/onPointerUp/onPointerCancel handlers using setPointerCapture and getCanvasPoint's scale-corrected coordinates
    - .meme-canvas has touch-action: none
    - npm --prefix client run test -- --run, typecheck, and build all exit 0
  </acceptance_criteria>
  <done>A player can add up to 3 caption boxes, type independent text into each, and drag each one anywhere on the canvas via touch or mouse — MEME-01's full multi-box, drag-to-position scope is implemented on top of Plan 05-02's proven single-box pipeline.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created or changed by this plan:

**New module (`client/src/canvas/hitTest.ts`):** `MAX_CAPTION_BOXES`, `getBoxAtPoint`,
`clampBoxPosition`, `nextBoxPosition`, `canAddBox`.

**WritingPanel.tsx changes:** pointer-event drag handlers (`handlePointerDown`/`handlePointerMove`/
`handlePointerUp`, `getCanvasPoint`), a per-box input row, an add-caption-box button, a per-box
remove button.

**Hebrew string keys added (`shared/messages.ts` `HEBREW_UI`):** `memeEditorInstructions`,
`addCaptionBoxButton`, `deleteCaptionButton`.

**CSS:** `touch-action: none` added to `.meme-canvas`; new `.meme-editor-instructions`,
`.caption-box-row`, `.caption-box-remove` rules.

**New file paths:** `client/src/canvas/hitTest.ts`, `client/src/canvas/hitTest.test.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input -> box position state | Entirely client-side, local to the authoring player's own browser; no server round-trip until submit, so a malicious drag sequence can only ever corrupt the author's own in-progress meme, never another player's data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | client-side box count | low | mitigate | `canAddBox`/`MAX_CAPTION_BOXES` bound the UI to 3 boxes, but even if a player bypassed the UI and somehow composed more, the server never inspects box count — it only bounds the FINAL rasterized image's byte size (Plan 05-01's `MEME_MAX_BASE64_CHARS`), so an oversized composition is still caught at submit time regardless of how many boxes produced it |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new package — no `package.json`/lockfile appears in `files_modified` |
</threat_model>

<verification>
- `npm --prefix client run test -- --run` exits 0 (includes hitTest.test.ts)
- `npm --prefix client run typecheck` exits 0
- `npm --prefix client run build` exits 0
- A real-device touch-drag check is still required before this phase is considered done (Plan 05-05)
</verification>

<success_criteria>
- A player can add up to 3 caption boxes and drag each independently
- Overlapping boxes resolve deterministically (last-in-array on top, both for drawing and hit-testing)
- At least one caption box is always present in the UI
- touch-action: none prevents the browser's own scroll gesture from fighting a drag on a real phone
</success_criteria>

<output>
Create `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-03-SUMMARY.md` when done
</output>
