---
phase: 05-hebrew-rtl-meme-compositor-souvenir
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - client/src/share/saveOrShareMeme.ts
  - client/src/share/saveOrShareMeme.test.ts
  - client/src/screens/round/SaveMemeFallback.tsx
  - client/src/screens/round/SaveShareButton.tsx
  - client/src/screens/round/RatingPanel.tsx
  - client/src/screens/round/GameEndPanel.tsx
  - shared/messages.ts
  - client/src/index.css
autonomous: true
requirements: [MEME-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every player — not just best-of-night winners — gets a real chance to save or share their own meme: a save/share button appears on RatingPanel while a player's own meme is on screen (youAreAuthor), the one point in the whole loop where an author already sits and looks at their own finished meme for a real dwell time (the host-configurable ratingSeconds preset, 8-15s, plus the reveal break) rather than a 3-second transient ROUND_END screen (Claude's Discretion, filling a gap CONTEXT.md left open — MEME-03)."
    - "At GAME_END, any player can tap save/share on any of the top-3 best-of-night entries — including ones authored by someone else — and run the same save/share flow on that exact finished image; this never mutates the original entry, its score, or any protocol/scoring state (D-03, view/export only)."
    - "[N/A, MEME-03/concurrency edge] Save/share is a local, single-device, client-only action with no server round-trip — there is no concurrent-access scenario to guard against."
    - "[Resolved, MEME-03/idempotency edge] Tapping save/share twice simply re-runs the same local flow (opens the native share sheet again, or re-shows the same fallback modal) — saveOrShareMeme has no persisted state and no server call, so a repeat tap can never duplicate a submission or corrupt state."
  artifacts:
    - path: "client/src/share/saveOrShareMeme.ts"
      provides: "saveOrShareMeme(meme) — the three-branch (shared/dismissed/fallback) save-or-share flow, mirroring shareJoinLink.ts's proven shape"
      exports: ["saveOrShareMeme", "SaveShareResult"]
    - path: "client/src/screens/round/SaveShareButton.tsx"
      provides: "the one reusable button+fallback-modal component both RatingPanel and GameEndPanel mount"
  key_links:
    - from: "client/src/screens/round/RatingPanel.tsx"
      to: "client/src/screens/round/SaveShareButton.tsx"
      via: "the youAreAuthor branch renders <SaveShareButton meme={ratingStep.meme} />, giving every author a save/share chance during their own meme's real (8-15s) rating-step dwell time"
      pattern: "youAreAuthor.*SaveShareButton|SaveShareButton.*ratingStep.meme"
    - from: "client/src/screens/round/GameEndPanel.tsx"
      to: "client/src/screens/round/SaveShareButton.tsx"
      via: "each best-of-night <li> renders <SaveShareButton meme={entry.meme} /> — D-03's view/export reuse, additive only, no scoring/protocol change"
      pattern: "best-of-entry.*SaveShareButton|SaveShareButton.*entry.meme"
---

<objective>
Deliver MEME-03 (save/share) and D-03 (best-of-night reuse) on top of the now-complete compositor/
editor pipeline (Plans 05-01 through 05-03). Resolves a real gap CONTEXT.md left open: it locks the
save/share MECHANISM (`navigator.share` -> long-press fallback, from CLAUDE.md's existing research)
but never specifies WHERE in the UI a player who never makes best-of-night gets to save their own
meme. `ROUND_END` is a fixed 3-second pacing beat (`BETWEEN_PHASES_MS`, non-configurable) — too
transient for a real tap. The chosen answer: `RatingPanel`'s existing `youAreAuthor` waiting state,
which already dwells for the host-configurable `ratingSeconds` preset (8-15s) plus a reveal break,
is the one point every author already sits looking at their own finished meme with nothing else to
do. `GameEndPanel`'s best-of-night list (D-03) gets the same button for view/export of others' memes.

Purpose: without this plan, MEME-03 is only reachable by the ~3 players whose memes reach best-of-
night each game — everyone else would have composed a souvenir they can never actually keep, directly
contradicting the phase's own goal ("every player can produce and keep... as their souvenir").

Output: a save/share button reachable by every author, every round, plus the same button on every
best-of-night entry at game end.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-CONTEXT.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-RESEARCH.md
@.claude/CLAUDE.md

@client/src/share/shareJoinLink.ts
@client/src/share/shareJoinLink.test.ts
@client/src/screens/round/RatingPanel.tsx
@client/src/screens/round/GameEndPanel.tsx
@shared/messages.ts
@client/src/index.css

<interfaces>
Existing contracts this plan extends — stated as they stand AFTER Plan 05-03.

From `client/src/share/shareJoinLink.ts` (today, unrelated feature, same codebase — the exact pattern
this plan's `saveOrShareMeme.ts` copies): a three-branch discriminated union result
(`{ outcome: "shared" } | { outcome: "dismissed" } | { outcome: "fallback"; ... }`), `navigator.share`
tried first inside a `try`/`catch`, an `AbortError` `DOMException` treated as `"dismissed"` (not a
failure), and every other failure or absence of `navigator.share` degrading to `"fallback"`. Tested
via `vi.stubGlobal("navigator", {...})` — no real browser needed.

From `client/src/screens/round/RatingPanel.tsx` (after Plan 05-02): the `youAreAuthor` branch renders
only `<p>{HEBREW_UI.yourMemeWaiting}</p>`. `ratingStep.meme` (base64 PNG) is already present on the
snapshot this component receives — nothing new needs to travel over the wire for this plan.

From `client/src/screens/round/GameEndPanel.tsx` (after Plan 05-02): each best-of-night `<li>` renders
`<img className="meme-photo" src={memeDataUrl(entry.meme)} alt={HEBREW_UI.photoAlt} />` followed by the
author/score line. `entry.meme` is already present — nothing new needs to travel over the wire for
this plan either.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The save/share flow — base64 decode, native share, long-press fallback</name>
  <reversibility rating="reversible">A pure client-side flow mirroring an already-shipped pattern (shareJoinLink.ts) — swapping its UI placement or fallback copy later touches nothing else.</reversibility>
  <files>client/src/share/saveOrShareMeme.ts, client/src/share/saveOrShareMeme.test.ts, client/src/screens/round/SaveMemeFallback.tsx, shared/messages.ts</files>
  <read_first>
    - `client/src/share/shareJoinLink.ts` and `shareJoinLink.test.ts` — the exact three-branch pattern and `vi.stubGlobal("navigator", ...)` test technique this task copies
    - `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-RESEARCH.md`'s "Web Share API Level 2" and "Navigator.share with Fallback" sections
    - `.claude/CLAUDE.md`'s "Image + Text Compositing" table — the locked `navigator.canShare({ files })` -> long-press fallback chain this task implements, not re-litigates
  </read_first>
  <behavior>
    - Given a navigator whose canShare({ files }) returns true and whose share() resolves, saveOrShareMeme returns { outcome: "shared" }
    - Given a navigator whose share() rejects with a DOMException named "AbortError", saveOrShareMeme returns { outcome: "dismissed" }
    - Given a navigator with no canShare function at all, saveOrShareMeme returns { outcome: "fallback", blob } without ever calling share()
    - Given a navigator whose canShare returns false, saveOrShareMeme returns { outcome: "fallback", blob } without ever calling share()
    - The returned fallback blob, decoded back to base64, equals the original meme string that was passed in (round-trip integrity)
    - saveOrShareMeme never throws, under any of the branches above
  </behavior>
  <action>
Create `client/src/share/saveOrShareMeme.ts`:
```
function base64ToBlob(base64: string, mimeType: string): Blob {
  const binary = atob(base64);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
  return new Blob([bytes], { type: mimeType });
}

export type SaveShareResult =
  | { outcome: "shared" }
  | { outcome: "dismissed" }
  | { outcome: "fallback"; blob: Blob };

/**
 * Three-branch save/share, never throwing to its caller — mirrors
 * shareJoinLink.ts's own proven shape:
 * 1. navigator.canShare({ files }) is true and share() succeeds -> "shared"
 * 2. share() exists but the user dismisses the native sheet (AbortError) ->
 *    "dismissed", not treated as a failure
 * 3. canShare is absent/false, or share() fails for any other reason ->
 *    "fallback" with the decoded Blob, for a full-screen long-press-to-save UI
 */
export async function saveOrShareMeme(meme: string): Promise<SaveShareResult> {
  const blob = base64ToBlob(meme, "image/png");
  const file = new File([blob], "tamir-meme.png", { type: "image/png" });
  const nav = typeof navigator === "undefined" ? undefined : navigator;

  if (nav && typeof nav.canShare === "function" && nav.canShare({ files: [file] })) {
    try {
      await nav.share({ files: [file], title: "תמונה ממסיבת תמיר" });
      return { outcome: "shared" };
    } catch (err) {
      if (err instanceof DOMException && err.name === "AbortError") {
        return { outcome: "dismissed" };
      }
      return { outcome: "fallback", blob };
    }
  }

  return { outcome: "fallback", blob };
}
```

Create `client/src/share/saveOrShareMeme.test.ts` covering every behavior line above, using
`vi.stubGlobal("navigator", {...})` exactly as `shareJoinLink.test.ts` does (a real base64 PNG is not
needed — any short base64 string, e.g. `Buffer.from("x").toString("base64")`, exercises the decode
path).

Create `client/src/screens/round/SaveMemeFallback.tsx` — a full-screen modal shown only in the
`"fallback"` branch:
```
type SaveMemeFallbackProps = { blob: Blob; onDone: () => void };

export function SaveMemeFallback({ blob, onDone }: SaveMemeFallbackProps) {
  const [url, setUrl] = useState<string | null>(null);

  useEffect(() => {
    const objectUrl = URL.createObjectURL(blob);
    setUrl(objectUrl);
    return () => URL.revokeObjectURL(objectUrl);
  }, [blob]);

  if (!url) return null;

  return (
    <div className="save-meme-fallback">
      <img src={url} alt={HEBREW_UI.photoAlt} />
      <p>{HEBREW_UI.longPressInstructions}</p>
      <button type="button" onClick={onDone}>{HEBREW_UI.doneButton}</button>
    </div>
  );
}
```

In `shared/messages.ts`: add three new keys to `HEBREW_UI`, grouped under a new comment
`// plan 05-04 — save/share`, after the existing `deleteCaptionButton` entry:
`saveMemeButton: "שמירה או שיתוף"`, `longPressInstructions: "לחצו לחיצה ארוכה על התמונה ובחרו 'שמירת תמונה'"`,
`doneButton: "סיום"`.
  </action>
  <verify>
    <automated>npm --prefix client run test -- --run saveOrShareMeme.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - client/src/share/saveOrShareMeme.ts exports saveOrShareMeme and SaveShareResult
    - saveOrShareMeme resolves "shared", "dismissed", or "fallback" per the behavior list above, and never throws
    - client/src/screens/round/SaveMemeFallback.tsx renders the decoded image and long-press instructions
    - npm --prefix client run test -- --run saveOrShareMeme.test.ts passes
  </acceptance_criteria>
  <done>The save/share mechanism works correctly in isolation — native share, dismissal, and the long-press fallback all resolve to the right outcome — proven without needing a real device for the branching logic itself (the actual iOS Safari behavior still needs Plan 05-05's real-device check, per CLAUDE.md's own flagged LOW-confidence note).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the save/share button into RatingPanel (every author) and GameEndPanel (best-of-night, D-03)</name>
  <files>client/src/screens/round/SaveShareButton.tsx, client/src/screens/round/RatingPanel.tsx, client/src/screens/round/GameEndPanel.tsx, client/src/index.css</files>
  <read_first>
    - `client/src/share/saveOrShareMeme.ts` and `SaveMemeFallback.tsx` exactly as written by Task 1
    - `client/src/screens/round/RatingPanel.tsx`'s `youAreAuthor` branch and `GameEndPanel.tsx`'s best-of-night `<li>` exactly as they stand after Plan 05-02 (quoted in this plan's `<interfaces>` block)
    - `client/src/index.css`'s `.swap-photo-button` rule — the outlined-secondary-button convention this task's `.save-share-button` follows
  </read_first>
  <action>
Create `client/src/screens/round/SaveShareButton.tsx` — the one reusable component both render
surfaces below mount:
```
type SaveShareButtonProps = { meme: string };

export function SaveShareButton({ meme }: SaveShareButtonProps) {
  const [fallbackBlob, setFallbackBlob] = useState<Blob | null>(null);

  async function handleClick() {
    const result = await saveOrShareMeme(meme);
    if (result.outcome === "fallback") {
      setFallbackBlob(result.blob);
    }
  }

  return (
    <>
      <button type="button" className="save-share-button" onClick={handleClick}>
        {HEBREW_UI.saveMemeButton}
      </button>
      {fallbackBlob && (
        <SaveMemeFallback blob={fallbackBlob} onDone={() => setFallbackBlob(null)} />
      )}
    </>
  );
}
```

In `client/src/screens/round/RatingPanel.tsx`: import `SaveShareButton` from `./SaveShareButton`.
Change the `youAreAuthor` branch from `<p>{HEBREW_UI.yourMemeWaiting}</p>` to
`<><p>{HEBREW_UI.yourMemeWaiting}</p><SaveShareButton meme={ratingStep.meme} /></>` (MEME-03 — every
author gets a real chance to save their own meme during their own meme's real rating-step dwell time,
not just best-of-night winners at game end).

In `client/src/screens/round/GameEndPanel.tsx`: import `SaveShareButton` from `./SaveShareButton`.
Inside the best-of-night `<li>`, immediately after the existing
`<p className="best-of-meta">{entry.authorName} — {entry.score} {HEBREW_UI.roundResultsPointsSuffix}</p>`
line, add `<SaveShareButton meme={entry.meme} />` (D-03 — reachable for EVERY best-of-night entry,
including ones authored by someone other than the viewer; purely additive, no change to `entry`'s
shape, scoring, or any protocol type).

In `client/src/index.css`: add a `.save-share-button` rule after `.swap-photo-button`, same
outlined-secondary style: `background: transparent; border: 1px solid currentColor; color: inherit;
margin: 0.5rem 0 0;`. Add a `.save-meme-fallback` rule for the full-screen modal:
`position: fixed; inset: 0; z-index: 20; display: flex; flex-direction: column;
align-items: center; justify-content: center; gap: 1rem; padding: 1.25rem;
background: rgba(0, 0, 0, 0.9); color: #fff;` and a nested `.save-meme-fallback img` rule:
`max-width: 100%; max-height: 70vh; border-radius: 0.5rem;`.
  </action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
    <automated>grep -c "SaveShareButton" client/src/screens/round/RatingPanel.tsx client/src/screens/round/GameEndPanel.tsx</automated>
    <fails_when>either file's count is 0, meaning the button was not wired into both render surfaces</fails_when>
  </verify>
  <acceptance_criteria>
    - RatingPanel.tsx's youAreAuthor branch renders SaveShareButton with meme={ratingStep.meme}
    - GameEndPanel.tsx's best-of-night list renders SaveShareButton per entry with meme={entry.meme}
    - .save-share-button and .save-meme-fallback exist in client/src/index.css
    - npm --prefix client run typecheck and npm --prefix client run build both exit 0
  </acceptance_criteria>
  <done>Every author gets a save/share chance while their own meme is being rated, and every player can save/share any of the game's real top-3 best-of-night memes at GAME_END — MEME-03 is reachable by every player, not only best-of-night winners, and D-03's view/export reuse is delivered as purely additive.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created or changed by this plan:

**New module (`client/src/share/saveOrShareMeme.ts`):** `saveOrShareMeme`, `SaveShareResult`.

**New components:** `client/src/screens/round/SaveMemeFallback.tsx`,
`client/src/screens/round/SaveShareButton.tsx`.

**Rendering changes:** `RatingPanel.tsx`'s `youAreAuthor` branch gains `SaveShareButton`;
`GameEndPanel.tsx`'s best-of-night entries each gain `SaveShareButton`.

**Hebrew string keys added (`shared/messages.ts` `HEBREW_UI`):** `saveMemeButton`,
`longPressInstructions`, `doneButton`.

**CSS:** `.save-share-button`, `.save-meme-fallback`, `.save-meme-fallback img`.

**New file paths:** `client/src/share/saveOrShareMeme.ts`, `client/src/share/saveOrShareMeme.test.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `saveOrShareMeme` -> `navigator.share`/local Blob URL | Entirely client-local; no server round-trip, no new data leaves the device beyond what the OS share sheet itself does when the player explicitly chooses a destination |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | `SaveMemeFallback`'s `URL.createObjectURL(blob)` | low | mitigate | The object URL is revoked (`URL.revokeObjectURL`) in the same `useEffect`'s cleanup, so it is never left reachable after the modal closes or the component unmounts |
| T-05-08 | Repudiation/none | best-of-night save/share of another author's meme (D-03) | low | accept | View/export only — this plan changes no scoring, no protocol field, and no server state; the original entry, its author, and its score are untouched by any number of other players saving a copy |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new package — no `package.json`/lockfile appears in `files_modified` |
</threat_model>

<verification>
- `npm --prefix client run test -- --run` exits 0 (includes saveOrShareMeme.test.ts)
- `npm --prefix client run typecheck` exits 0
- `npm --prefix client run build` exits 0
- Every player, not only best-of-night winners, has a save/share entry point during normal play
</verification>

<success_criteria>
- A save/share button is reachable by every meme's author during their own rating-step dwell time
- A save/share button is reachable on every best-of-night entry at GAME_END, including others' memes
- The native-share/dismiss/fallback branching is proven correct without a real device
- The actual iOS Safari save/share behavior still requires Plan 05-05's real-device check
</success_criteria>

<output>
Create `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-04-SUMMARY.md` when done
</output>
