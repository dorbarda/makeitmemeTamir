---
phase: 05-hebrew-rtl-meme-compositor-souvenir
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - client/package.json
  - client/src/canvas/compositor.ts
  - client/src/canvas/compositor.test.ts
  - client/src/screens/round/WritingPanel.tsx
  - client/src/screens/round/RatingPanel.tsx
  - client/src/screens/round/RoundEndPanel.tsx
  - client/src/screens/round/GameEndPanel.tsx
  - client/src/index.css
autonomous: true
requirements: [MEME-01, HEB-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The simplest possible meme (one caption box, no drag) composes on a canvas in WritingPanel, rasterizes to a base64 PNG on submit, and that exact PNG is what RatingPanel/RoundEndPanel/GameEndPanel render as an <img> — proving the rasterize-and-transmit pipeline end to end before any drag/multi-box complexity is added (MEME-01, D-01, D-02)."
    - "Submit is refused client-side (no socket emit at all) when the caption box's trimmed text is empty — the enforcement point for 'at least one non-empty caption' moves entirely client-side under the rasterize architecture, since a rasterized image is always non-empty bytes even when its text is blank and the server can no longer distinguish the two (MEME-01/empty edge, resolved for the specless-probe fallback)."
    - "A caption box's text is capped by grapheme count (Intl.Segmenter), not UTF-16 code units — the same convention this codebase already uses for names and the old single caption field (MEME-01/encoding edge, resolved)."
    - "[FLAGGED ASSUMPTION, HEB-03] A realistic caption mixing Hebrew, digits, and a Latin word is only PROVEN correct right-to-left by visual inspection of the actual rendered image on a real device — jsdom has no real Canvas 2D implementation, so this plan's own automated tests can only prove the caption STRING reaches ctx.fillText unmodified (no truncation, no reordering at the data level), never that the browser's bidi/glyph shaping renders it correctly. Plan 05-05's real-device check is where this is actually confirmed."
  artifacts:
    - path: "client/src/canvas/compositor.ts"
      provides: "CaptionBox, drawFrame, rasterize, blobToBase64, and memeDataUrl — the pure, canvas-injectable composition primitives every render surface and Wave 3's drag editor build on"
      exports: ["CaptionBox", "drawFrame", "rasterize", "blobToBase64", "memeDataUrl", "CANVAS_WIDTH", "CANVAS_HEIGHT", "BOX_WIDTH", "BOX_HEIGHT"]
    - path: "client/src/screens/round/WritingPanel.tsx"
      provides: "the canvas-based single-box compositor replacing the plain <textarea>, gated on document.fonts.ready before any fillText call"
  key_links:
    - from: "client/src/screens/round/WritingPanel.tsx"
      to: "client/src/canvas/compositor.ts"
      via: "handleSubmit awaits document.fonts.ready, calls drawFrame then rasterize then blobToBase64, and emits { meme } with the result"
      pattern: "blobToBase64\\(await rasterize"
    - from: "client/src/screens/round/RatingPanel.tsx"
      to: "client/src/canvas/compositor.ts"
      via: "renders <img src={memeDataUrl(ratingStep.meme)}> — the exact bytes the author's WritingPanel produced, never re-rendered client-side"
      pattern: "memeDataUrl\\(ratingStep.meme\\)"
---

<objective>
Prove the phase's central architecture change (D-01, D-02, RESEARCH.md's rasterize-and-transmit
decision) end to end with the simplest possible case: ONE caption box, no drag, no add/remove, no
3-box limit — composed on a canvas in `WritingPanel`, rasterized to a base64 PNG at submit time, sent
over the wire as `meme` (Plan 05-01's contract), and rendered identically as a plain `<img>` in
`RatingPanel`, `RoundEndPanel`, and `GameEndPanel`. This is the tracer: it validates the riskiest new
architecture (client-side rasterization + a single flattened image replacing structured caption+photo
data across four render surfaces) in isolation from the touch-interaction risk Wave 3 adds next.

Purpose: every later Phase 5 plan (multi-box drag, save/share, best-of-night reuse) extends this same
pipeline rather than inventing a new one. Proving it now, on the smallest possible surface, means a
fundamental pipeline defect is caught after one plan, not after the drag editor is also built on top
of it.

Output: a player can type one Hebrew caption during WRITING, see it composed live on their own photo,
submit, and have every other player's RatingPanel/RoundEndPanel — and later GameEndPanel — show the
exact same composited image.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-CONTEXT.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-RESEARCH.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-PATTERNS.md

@shared/protocol.ts
@shared/messages.ts
@client/src/screens/round/WritingPanel.tsx
@client/src/screens/round/RatingPanel.tsx
@client/src/screens/round/RoundEndPanel.tsx
@client/src/screens/round/GameEndPanel.tsx
@client/src/socket/connection.ts
@client/src/index.css
@client/package.json

<interfaces>
Existing contracts this plan builds on — stated as they stand AFTER Plan 05-01 (this plan's own
`depends_on: ["05-01"]` guarantees that ordering).

From `shared/protocol.ts` (after Plan 05-01): `RatingStepView.meme: string`,
`RoundEndEntry.meme: string`, `BestOfEntry.meme: string` (all base64-encoded PNG, no `caption`/
`photoUrl` remaining on any of the three), `CLIENT_EVENTS.submitCaption` payload is
`{ meme: string }`. `LobbySnapshot.yourPhotoUrl: string | null` is UNCHANGED by Plan 05-01 — it still
carries this player's own assigned photo URL during WRITING, and this plan's `WritingPanel` still
needs it (as the `<img>`/`Image()` source the canvas draws before compositing captions on top).

From `client/src/screens/round/WritingPanel.tsx` (today, before this plan): a plain `<textarea>`
bound to a single `caption` string, the `toGraphemes`/`graphemesRemaining`/`clampCaptionForInput`
grapheme-counting trio (`Intl.Segmenter("he", { granularity: "grapheme" })`), and `handleSubmit`'s
`socket.once(error)`/`socket.once(state)` round-trip pattern emitting
`{ text: caption }` — this plan changes the payload to `{ meme }` and replaces the textarea with a
canvas + text input, but keeps the grapheme-counting trio (renamed constant, smaller cap — see
Task 1) and the exact same socket round-trip shape.

From `client/src/screens/round/RatingPanel.tsx`/`RoundEndPanel.tsx`/`GameEndPanel.tsx` (today): each
renders `photo`/`caption` (or `photoUrl`/`caption`) as two separate elements — an `<img>` and a `<p>`.
This plan replaces both with a single `<img>` per entry, sourced from `memeDataUrl(x.meme)`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The tracer — single-box canvas compositor in WritingPanel, rasterize, submit as meme</name>
  <reversibility rating="reversible">CANVAS_WIDTH/HEIGHT and BOX_WIDTH/HEIGHT are implementation constants behind a pure, injectable module (compositor.ts) — easy to retune once real device testing (Plan 05-05) shows how they look on an actual phone.</reversibility>
  <files>client/package.json, client/src/canvas/compositor.ts, client/src/canvas/compositor.test.ts, client/src/screens/round/WritingPanel.tsx</files>
  <read_first>
    - `client/src/screens/round/WritingPanel.tsx` — the exact current textarea/grapheme/handleSubmit shape quoted in this plan's `<interfaces>` block
    - `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-RESEARCH.md`'s "Code Examples" section (`composeAndRasterize`, the RTL `ctx.direction`/`ctx.textAlign` combination) and its "Font Loading" section (`document.fonts.ready` discipline)
    - `client/src/socket/connection.ts` — `getSocket()`, the only way this task's submit reaches the server
    - `.claude/CLAUDE.md`'s "Image + Text Compositing" table — the locked Canvas 2D API + `@fontsource/heebo` + `document.fonts.ready` decision this task implements, not re-litigates
  </read_first>
  <action>
Run `npm --prefix client install @fontsource/heebo` (approved in RESEARCH.md's Package Legitimacy
Audit table — no checkpoint needed). Confirm `client/package.json`'s `dependencies` now lists
`@fontsource/heebo`.

Create `client/src/canvas/compositor.ts` — a pure module with no React/DOM-lifecycle code, only
canvas-injectable functions so they are unit-testable without a real browser canvas:
```
export type CaptionBox = { id: string; text: string; x: number; y: number };

export const CANVAS_WIDTH = 600;
export const CANVAS_HEIGHT = 800;
export const BOX_WIDTH = 360;
export const BOX_HEIGHT = 100;

export function drawFrame(
  ctx: CanvasRenderingContext2D,
  photo: CanvasImageSource,
  boxes: CaptionBox[],
): void {
  ctx.clearRect(0, 0, CANVAS_WIDTH, CANVAS_HEIGHT);
  ctx.drawImage(photo, 0, 0, CANVAS_WIDTH, CANVAS_HEIGHT);
  for (const box of boxes) {
    if (box.text.trim().length === 0) continue;
    ctx.fillStyle = "rgba(0, 0, 0, 0.6)"; // D-04 — semi-transparent dark backing
    ctx.fillRect(box.x, box.y, BOX_WIDTH, BOX_HEIGHT);
    ctx.fillStyle = "#ffffff";
    ctx.font = "28px Heebo, sans-serif";
    ctx.direction = "rtl"; // CRITICAL — HEB-03's correctness depends entirely on this line
    ctx.textAlign = "right";
    ctx.textBaseline = "top";
    ctx.fillText(box.text, box.x + BOX_WIDTH - 12, box.y + 12, BOX_WIDTH - 24);
  }
}

export function rasterize(canvas: HTMLCanvasElement): Promise<Blob> {
  return new Promise((resolve, reject) => {
    canvas.toBlob((blob) => {
      if (!blob) {
        reject(new Error("canvas.toBlob failed"));
        return;
      }
      resolve(blob);
    }, "image/png");
  });
}

export function blobToBase64(blob: Blob): Promise<string> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => resolve((reader.result as string).split(",")[1] ?? "");
    reader.onerror = () => reject(reader.error ?? new Error("FileReader failed"));
    reader.readAsDataURL(blob);
  });
}

export function memeDataUrl(meme: string): string {
  return `data:image/png;base64,${meme}`;
}
```

Create `client/src/canvas/compositor.test.ts` covering: `drawFrame` calls `ctx.drawImage` exactly
once with the photo; `drawFrame` skips `fillRect`/`fillText` entirely for a box whose `text.trim()`
is empty; `drawFrame` sets `ctx.direction` to `"rtl"` and calls `fillText` with the box's exact,
unmodified text (a realistic mixed string, e.g. `"מחיר: 50% הנחה ל-VIP"`, proving the string reaches
`fillText` byte-for-byte, which is the one thing this jsdom-based test CAN prove about HEB-03 — real
bidi rendering correctness cannot be tested here, see this plan's flagged assumption) for a
non-empty box (build a minimal fake `ctx` object with `vi.fn()` methods and plain property assignment
for `fillStyle`/`font`/`direction`/`textAlign`/`textBaseline`, cast through `unknown as
CanvasRenderingContext2D`); `rasterize` resolves with whatever `Blob` a stubbed `canvas.toBlob`
callback provides, and rejects when that callback provides `null`; `blobToBase64`, given a real
`Blob` (jsdom supports `Blob`/`FileReader` natively), resolves with the exact base64 encoding of that
blob's bytes; `memeDataUrl("abc")` returns exactly `"data:image/png;base64,abc"`.

Rewrite `client/src/screens/round/WritingPanel.tsx`:
  - Add `import '@fontsource/heebo/400.css';` as the first import.
  - Import `CaptionBox, drawFrame, rasterize, blobToBase64, BOX_WIDTH, BOX_HEIGHT, CANVAS_WIDTH,
    CANVAS_HEIGHT` from `../../canvas/compositor.js`.
  - Rename the existing `MAX_CAPTION_GRAPHEMES` local constant to `MAX_CAPTION_BOX_GRAPHEMES = 60`
    (down from 120 — a caption box drawn onto a fixed-size image needs to actually fit visually,
    unlike the old free-standing text field). Update its doc comment: it no longer mirrors any server
    constant (the server has no text-length concept for `meme` at all after Plan 05-01) — it is now a
    purely client-side UX cap on how much text one caption box can reasonably hold. Keep
    `toGraphemes`/`graphemesRemaining`/`clampCaptionForInput` exactly as they are, just referencing
    the renamed constant.
  - Replace the `const [caption, setCaption] = useState("");` state with
    `const [boxes, setBoxes] = useState<CaptionBox[]>([{ id: "box-1", text: "", x: (CANVAS_WIDTH -
    BOX_WIDTH) / 2, y: (CANVAS_HEIGHT - BOX_HEIGHT) / 2 }]);` (a single, centered, fixed-position box
    — Wave 3 adds drag/add/remove on top of this same array shape).
  - Add `const canvasRef = useRef<HTMLCanvasElement>(null);`, `const photoRef =
    useRef<HTMLImageElement | null>(null);`, and `const [submitting, setSubmitting] =
    useState(false);`.
  - Add a `useEffect` keyed on `[snapshot.yourPhotoUrl]`: if `yourPhotoUrl` is set, create
    `new Image()`, set `.onload = () => { photoRef.current = img; redraw(); }`, and set `.src =
    snapshot.yourPhotoUrl`.
  - Add a `redraw()` function: reads `canvasRef.current`/`photoRef.current`, returns early if either
    is null, gets the 2D context (`canvas.getContext("2d")`), returns early if null, and calls
    `drawFrame(ctx, photo, boxes)`.
  - Add a `useEffect` keyed on `[boxes]` that calls `redraw()` — live preview as the player types.
  - Render `<canvas ref={canvasRef} width={CANVAS_WIDTH} height={CANVAS_HEIGHT} className="meme-canvas" />`
    in place of the old `<img className="meme-photo" ... />` line (the canvas IS the photo display now
    — it draws the photo itself via `drawFrame`).
  - Render a text `<input>` (not `<textarea>` — a single line fits a caption box) bound to
    `boxes[0].text`, `onChange={(e) => setBoxes([{ ...boxes[0], text: clampCaptionForInput(e.target.value)
    }])}`, with a `<span>{graphemesRemaining(boxes[0].text)}</span>` character-remaining indicator,
    matching the existing label/placeholder/counter layout.
  - Rewrite `handleSubmit` as an `async` function: `e.preventDefault(); setError(undefined);` then
    `if (!boxes.some((b) => b.text.trim().length > 0)) { setError(HEBREW_ERRORS.CAPTION_REQUIRED);
    return; }` (MEME-01's empty-caption edge — enforced here because a rasterized image is always
    non-empty bytes even with blank text, so the server can no longer catch this). Then
    `setSubmitting(true)`, `await document.fonts.ready` (CRITICAL — HEB-03 depends on this running
    before any `fillText`), read `canvasRef.current`/`photoRef.current` (return early with an error if
    either is somehow still null), call `redraw()` once more to guarantee the just-typed text is on
    the canvas, then `try { const blob = await rasterize(canvas); const meme = await
    blobToBase64(blob); /* existing socket.once round-trip, emitting { meme } instead of { text:
    caption } */ } finally { setSubmitting(false); }`. Keep the exact same `socket.once(error)`/
    `socket.once(state)` cleanup shape this component already has.
  - Disable the submit button while `submitting` is true (`<button type="submit" disabled={submitting}>`)
    so a slow phone's `canvas.toBlob()` (RESEARCH.md: ~50-150ms) cannot be double-tapped into two
    submissions.
  - Leave `handleSwap`, the swap-photo button, the `youSubmitted` success view, and the progress/
    roster list completely unchanged.
  </action>
  <verify>
    <automated>npm --prefix client run test -- --run compositor.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "0 passed"</fails_when>
    <automated>grep -c "document.fonts.ready" client/src/screens/round/WritingPanel.tsx</automated>
    <fails_when>output is 0, meaning the font-readiness gate documented as CRITICAL in this task's action was dropped</fails_when>
    <automated>grep -c "meme" client/src/screens/round/WritingPanel.tsx</automated>
    <fails_when>output is 0, meaning the submit payload was not migrated off { text: caption }</fails_when>
  </verify>
  <acceptance_criteria>
    - client/src/canvas/compositor.ts exports CaptionBox, drawFrame, rasterize, blobToBase64, memeDataUrl, CANVAS_WIDTH, CANVAS_HEIGHT, BOX_WIDTH, BOX_HEIGHT
    - WritingPanel.tsx renders a <canvas> (not a plain <img>) as the photo/caption composition surface
    - handleSubmit refuses to emit anything when every box's trimmed text is empty
    - handleSubmit awaits document.fonts.ready before drawing/rasterizing
    - handleSubmit emits CLIENT_EVENTS.submitCaption with a { meme } payload, never { text }
    - npm --prefix client run test -- --run compositor.test.ts passes
  </acceptance_criteria>
  <done>A player can type one Hebrew caption during WRITING, see it composed live on their own photo inside a canvas, and submitting rasterizes that canvas to a base64 PNG sent as `meme` — the full new pipeline's authoring half works end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Downstream rendering — RatingPanel, RoundEndPanel, GameEndPanel show the composited meme</name>
  <files>client/src/screens/round/RatingPanel.tsx, client/src/screens/round/RoundEndPanel.tsx, client/src/screens/round/GameEndPanel.tsx, client/src/index.css</files>
  <read_first>
    - `client/src/screens/round/RatingPanel.tsx`, `RoundEndPanel.tsx`, `GameEndPanel.tsx` — the exact current `photo`/`caption` two-element rendering quoted in this plan's `<interfaces>` block
    - `client/src/canvas/compositor.ts` exactly as written by Task 1 (`memeDataUrl`)
    - `client/src/index.css`'s existing `.meme-photo` rule — the class this task's new `.meme-canvas` rule sits beside
  </read_first>
  <action>
In `client/src/screens/round/RatingPanel.tsx`: import `memeDataUrl` from `../../canvas/compositor.js`.
Replace the two lines
`<img className="meme-photo" src={ratingStep.photoUrl} alt={HEBREW_UI.photoAlt} />` and
`<p>{ratingStep.caption}</p>` with one line:
`<img className="meme-photo" src={memeDataUrl(ratingStep.meme)} alt={HEBREW_UI.photoAlt} />`.

In `client/src/screens/round/RoundEndPanel.tsx`: import `memeDataUrl` from `../../canvas/compositor.js`.
Replace `<p>{entry.caption}</p>` with
`<img className="meme-photo" src={memeDataUrl(entry.meme)} alt={HEBREW_UI.photoAlt} />`, placed where
the old `<p>` sat (immediately after `<p>{entry.authorName}</p>`).

In `client/src/screens/round/GameEndPanel.tsx`: import `memeDataUrl` from `../../canvas/compositor.js`.
Replace the two lines
`<img className="meme-photo" src={entry.photoUrl} alt={HEBREW_UI.photoAlt} />` and
`<p>{entry.caption}</p>` with one line:
`<img className="meme-photo" src={memeDataUrl(entry.meme)} alt={HEBREW_UI.photoAlt} />`.

In `client/src/index.css`: add a `.meme-canvas` rule immediately after the existing `.meme-photo`
rule, identical sizing so the canvas and the downstream `<img>` read as the same visual element on
every screen: `display: block; width: 100%; max-width: 24rem; height: auto; margin: 0 auto 0.75rem;
border-radius: 0.5rem;` — no fixed pixel width, matching this file's own established convention
(`.meme-photo`, `.qr-code`).
  </action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS" (catches a leftover .caption/.photoUrl reference on the now-changed types)</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
    <automated>grep -rc "memeDataUrl" client/src/screens/round/RatingPanel.tsx client/src/screens/round/RoundEndPanel.tsx client/src/screens/round/GameEndPanel.tsx</automated>
    <fails_when>any of the three per-file counts is 0, meaning that render surface still shows the old caption/photoUrl split instead of the composited meme</fails_when>
    <automated>grep -c "entry.caption\|entry.photoUrl\|ratingStep.caption\|ratingStep.photoUrl" client/src/screens/round/RatingPanel.tsx client/src/screens/round/RoundEndPanel.tsx client/src/screens/round/GameEndPanel.tsx</automated>
    <fails_when>output is not exactly 0, meaning a leftover reference to the removed caption/photoUrl fields survives</fails_when>
  </verify>
  <acceptance_criteria>
    - RatingPanel.tsx, RoundEndPanel.tsx, and GameEndPanel.tsx each render a single <img src={memeDataUrl(...)}> in place of the old photo+caption pair
    - No file in this task references .caption or .photoUrl on ratingStep/roundEnd entries/bestOfNight entries
    - .meme-canvas exists in client/src/index.css with the same relative sizing as .meme-photo
    - npm --prefix client run typecheck and npm --prefix client run build both exit 0
  </acceptance_criteria>
  <done>Every downstream render surface (RatingPanel, RoundEndPanel, GameEndPanel) shows the exact composited image the author's WritingPanel produced — the tracer's full loop (compose -> submit -> store -> render identically everywhere) is complete and typechecks/builds clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created or changed by this plan:

**New module (`client/src/canvas/compositor.ts`):** `CaptionBox`, `CANVAS_WIDTH`, `CANVAS_HEIGHT`,
`BOX_WIDTH`, `BOX_HEIGHT`, `drawFrame`, `rasterize`, `blobToBase64`, `memeDataUrl`.

**Client dependency:** `@fontsource/heebo` added to `client/package.json`.

**WritingPanel.tsx changes:** canvas-based single-box compositor replacing the plain `<textarea>`;
`MAX_CAPTION_BOX_GRAPHEMES` (renamed/resized from the old `MAX_CAPTION_GRAPHEMES`); submit payload
is `{ meme }`.

**Rendering changes:** `RatingPanel.tsx`, `RoundEndPanel.tsx`, `GameEndPanel.tsx` each render
`<img src={memeDataUrl(x.meme)}>` in place of the old photo+caption pair.

**CSS:** `.meme-canvas` in `client/src/index.css`.

**New file paths:** `client/src/canvas/compositor.ts`, `client/src/canvas/compositor.test.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `WritingPanel`'s canvas -> submitted `meme` | The author's own browser produces the base64 payload; nothing server-side re-validates its pixel content (Plan 05-01's documented trade-off) |
| server `meme` -> `<img src>` on every other player's device | Rendering surface for data Plan 05-01 already bounded by size/shape; this plan is the first place that data is actually displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | `<img src={memeDataUrl(x.meme)}>` | low | accept | React sets `src` as a plain attribute value, never `dangerouslySetInnerHTML`; the `data:image/png;base64,` prefix is a fixed literal the code controls, so even a payload that is valid base64 but not a real PNG can only ever be interpreted as an (possibly broken) image resource, never as markup or script |
| T-05-05 | Information Disclosure | canvas-drawn photo + caption before submit | low | accept | Only the authoring player's own browser ever holds the pre-submit canvas state — nothing is transmitted until `handleSubmit` rasterizes and emits, matching D-14's existing "nothing leaks before the round closes" discipline |
| T-05-SC | Tampering | npm/pip/cargo installs | low | mitigate | `@fontsource/heebo` is the only new package this plan installs; it is pre-approved in RESEARCH.md's Package Legitimacy Audit table (OK/Approved, 4 yrs old, 2.5M weekly downloads) — no blocking checkpoint required |
</threat_model>

<verification>
- `npm --prefix client run test -- --run compositor.test.ts` exits 0
- `npm --prefix client run typecheck` exits 0
- `npm --prefix client run build` exits 0
- A one-box meme composed in WritingPanel and submitted appears identically as an `<img>` in RatingPanel, RoundEndPanel, and GameEndPanel
</verification>

<success_criteria>
- The rasterize-and-transmit pipeline works end to end for the simplest possible case
- Submit is refused client-side when the caption box is empty
- document.fonts.ready gates every canvas text draw
- Every downstream render surface shows the exact same composited image, never a re-render of structured data
</success_criteria>

<output>
Create `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-02-SUMMARY.md` when done
</output>
