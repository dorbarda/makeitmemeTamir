---
phase: 05-hebrew-rtl-meme-compositor-souvenir
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified: []
autonomous: false
requirements: [MEME-01, MEME-03, HEB-03]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The complete Phase 5 feature set (single-box compose, multi-box drag, save/share, best-of-night reuse) passes the full automated suite together, with no regression introduced by any one plan against another's work."
    - "[Closes the flagged HEB-03 assumption from Plan 05-02] A realistic caption mixing Hebrew, digits, and a Latin word has been visually confirmed rendering correctly right-to-left on the actual composited image on a real phone — not just proven at the string level in an automated test."
    - "Touch-drag of a caption box has been confirmed working on at least one real phone — this phase's own highest real-phone risk (CONTEXT.md), never previously exercised outside jsdom's pointer-event simulation."
    - "The save/share flow's actual behavior has been confirmed on a real iPhone in Safari specifically (the LOW-confidence item CLAUDE.md and RESEARCH.md both flag), not assumed from the automated branching tests alone."
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 5: run the full automated regression across every plan's work together (Plans 05-01
through 05-04 were each verified independently; this plan is the first point everything is checked as
one coherent whole), then perform the real-device check every phase in this project has needed to
catch a bug invisible in code review (Phase 1's duplicate roster, Phase 3's socket-auth callback) —
this phase's own version of that pattern, and explicitly the highest-risk one yet built
(CONTEXT.md's own framing of the drag-and-drop editor).

Purpose: a green test suite alone has never been sufficient in this project to call a UI feature done.
Canvas rendering, touch-drag, and iOS Safari's `navigator.share` behavior are three of exactly the
kinds of things that can look correct in code and jsdom tests while being subtly wrong on an actual
phone — this plan is where that gap gets closed for Phase 5, per its own CONTEXT.md-flagged risk.

Output: a fully green automated suite across `server` and `client`, plus a completed real-device
checklist covering touch-drag, HEB-03's RTL rendering, and MEME-03's save/share flow.
</objective>

<execution_context>
@/home/user/makeitmemeTamir/.claude/gsd-core/workflows/execute-plan.md
@/home/user/makeitmemeTamir/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-CONTEXT.md
@.planning/phases/03-core-loop-checkpoint-real-phones-end-to-end/03-01-SUMMARY.md
@.planning/phases/04-full-round-rating-scoring-completion/04-02-SUMMARY.md

@server/src/rooms/joinUrl.ts
@server/src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full cross-plan regression</name>
  <files>none — this task runs the existing suites; no source changes expected</files>
  <read_first>
    - `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-01-PLAN.md` through `05-04-PLAN.md`'s own `<verification>` sections — this task confirms all four still hold together, not individually
  </read_first>
  <action>
Run, in order, and fix any root cause before proceeding to Task 2 — never present the human-check
below with a broken verification environment: `npm --prefix server run test -- --run`,
`npm --prefix server run typecheck`, `npm --prefix client run test -- --run`,
`npm --prefix client run typecheck`, `npm --prefix client run build`. All five must exit 0. If a fix
is genuinely required, it belongs in the plan whose scope it falls under (protocol/Room -> 05-01,
compositor/tracer -> 05-02, drag editor -> 05-03, save/share -> 05-04) — note which plan's SUMMARY the
fix should be attributed to.
  </action>
  <verify>
    <automated>npm --prefix server run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
    <automated>npm --prefix client run test -- --run</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
    <automated>npm --prefix server run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run typecheck</automated>
    <fails_when>non-zero exit, or any output line begins with "error TS"</fails_when>
    <automated>npm --prefix client run build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or the vite summary never prints "built in"</fails_when>
  </verify>
  <acceptance_criteria>
    - All five commands above exit 0 with no failing tests and no type errors
  </acceptance_criteria>
  <done>The entire Phase 5 feature set — server contract, tracer, drag editor, save/share — passes the complete automated suite together, not just plan by plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Real-device check — touch-drag, HEB-03 RTL rendering, and iOS save/share</name>
  <files>none — this task starts the already-built server for a live check; no source changes</files>
  <read_first>
    - `server/src/rooms/joinUrl.ts` — `resolveOrigin`'s LAN-IP-friendly origin resolution, already proven for on-device testing in Phase 1 and Phase 3
    - `server/src/config.ts` — `SERVER_PORT` (defaults to 3001)
  </read_first>
  <action>
Start the server (`npm --prefix server run start`, backgrounded, using the client build Task 1 just
produced) so a real device on the same WiFi network can reach it — the server already serves the
built client and the Socket.IO endpoint from the same port, and `resolveOrigin` already builds a
correct join URL/QR code from whatever LAN IP the connecting device's own request carries, with no
code change needed. Confirm the server is up by checking its `/health` route. Determine and report the
machine's LAN IP and the port so the exact `http://<lan-ip>:<port>` address is ready to hand to a real
phone.
  </action>
  <verify>
    <automated>node -e "fetch('http://localhost:3001/health').then((r) => r.json()).then((j) => { if (!j.ok) process.exit(1); }).catch(() => process.exit(1));"</automated>
    <fails_when>non-zero exit, meaning the server is not reachable at /health on the expected port</fails_when>
  </verify>
  <what-built>
    The complete Phase 5 meme compositor and souvenir flow: a multi-box (up to 3), drag-to-position
    Hebrew caption editor replacing the plain WRITING textarea; rasterize-and-transmit so every
    viewer sees the exact composited image; native-share-with-long-press-fallback save/share reachable
    by every author during their own rating-step dwell time and on every best-of-night entry at
    GAME_END. The server is running at the LAN address this task's own output reports, reachable from
    any device on the same WiFi network.
  </what-built>
  <how-to-verify>
    On at least one real phone connected to the SAME WiFi network as this machine (an iPhone
    specifically for step 6 below, matching CLAUDE.md's flagged LOW-confidence save/share item —
    an Android phone too if available):
    1. Open a browser and navigate to the http://&lt;lan-ip&gt;:&lt;port&gt; address reported above.
    2. Create a room, join with a second device if available, and start a game.
    3. During WRITING: type a caption mixing Hebrew, a number, and a Latin word into a caption box
       (e.g. "מחיר: 50% הנחה ל-VIP") and confirm it reads correctly right-to-left ON THE COMPOSITED
       IMAGE on the canvas — not reversed, not out of order, not showing a fallback font in place of
       Hebrew glyphs (HEB-03 — this is the actual visual confirmation the automated tests in Plan
       05-02 could only approximate at the string level).
    4. Add a second and third caption box (the "add caption" button), and drag each one — including
       dragging two boxes so they fully overlap — with a finger, confirming the drag tracks the
       finger smoothly with no lag, no jump, and no fight against the page trying to scroll
       (MEME-01/D-05 — this phase's own highest real-phone risk).
    5. Submit and confirm the SAME composited image (same photo, same captions, same positions)
       appears identically on a second device's RatingPanel during rating.
    6. While your own meme is on screen being rated (the youAreAuthor waiting state), tap the
       save/share button. On the iPhone specifically: confirm either the native share sheet appears
       with a genuine "Save Image"/"Add to Photos" option, or the long-press fallback screen appears
       with an image you can long-press and save (MEME-03 — CLAUDE.md's own flagged
       version-dependent iOS behavior).
    7. Play to GAME_END and, on the best-of-night list, tap save/share on an entry NOT authored by
       you and confirm it runs the same save/share flow without altering that entry, its score, or
       anyone's roster/scoreboard (D-03).
    8. Confirm nothing froze, crashed, showed a blank screen, or showed mismatched meme content
       between devices at any step.
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 steps above are confirmed, or describe exactly what broke (which step, which device) so the fix lands in the right plan's follow-up</resume-signal>
  <acceptance_criteria>
    - The server responds {"ok":true} at /health
    - A realistic mixed Hebrew/digit/Latin caption was confirmed rendering correctly right-to-left on the actual composited image on a real phone
    - Dragging up to 3 caption boxes, including a full overlap, was confirmed working smoothly on a real phone touchscreen
    - Save/share was confirmed working on a real iPhone in Safari, either via native share sheet or the long-press fallback
    - Best-of-night save/share of another player's meme was confirmed working without side effects
  </acceptance_criteria>
  <done>Phase 5's three previously-unverifiable-in-code risks — HEB-03's actual RTL rendering, the drag editor's real-phone touch behavior, and iOS Safari's actual save/share behavior — are each confirmed working on a real device, closing out the phase per this project's own established pattern of never trusting a UI change to a green test suite alone.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new symbols — this plan is verification-only, closing out Plans 05-01 through 05-04's combined
work.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs existing code against real devices; it introduces no new trust boundary of its own |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new package — `files_modified` is empty |
</threat_model>

<verification>
- `npm --prefix server run test -- --run`, `npm --prefix server run typecheck`, `npm --prefix client run test -- --run`, `npm --prefix client run typecheck`, and `npm --prefix client run build` all exit 0
- The real-device checklist in Task 2 is fully confirmed (HEB-03 RTL rendering, drag editor, iOS save/share, D-03 best-of-night reuse)
</verification>

<success_criteria>
- The entire Phase 5 automated suite is green together
- A realistic mixed-script caption renders correctly RTL on an actual phone
- Touch-drag of up to 3 overlapping caption boxes works smoothly on a real phone
- Save/share is confirmed on a real iPhone in Safari
</success_criteria>

<output>
Create `.planning/phases/05-hebrew-rtl-meme-compositor-souvenir/05-05-SUMMARY.md` when done
</output>
